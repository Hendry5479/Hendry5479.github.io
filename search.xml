<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title></title>
      <link href="2021/04/06/interview/zw-inter/(2)promise/"/>
      <url>2021/04/06/interview/zw-inter/(2)promise/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Angulat-Note</title>
      <link href="2021/04/05/angular/note/"/>
      <url>2021/04/05/angular/note/</url>
      
        <content type="html"><![CDATA[<h3 id="ng常用命令"><a href="#ng常用命令" class="headerlink" title="ng常用命令"></a>ng常用命令</h3><p>ng new project-name<br>ng serve –open 构建应用并监听文件<br>–open 标志会打开浏览器<br>–host 0.0.0.0 指定host地址<br>ng g component name</p><pre><code class="js">import { Component, OnInit } from '@angular/core';@Component({  selector: 'app-heroes',  templateUrl: './heroes.component.html',  styleUrls: ['./heroes.component.css']})</code></pre><p>导入 Component 符号，并为组件类加上 @Component 装饰器，用于为该组件指定 Angular 所需的元数据<br>里面是CLI 自动生成了三个元数据属性<br>selector— 组件的选择器（CSS 元素选择器）<br>templateUrl— 组件模板文件的位置。<br>styleUrls— 组件私有 CSS 样式表文件的位置。</p><h3 id="UppercasePipe-格式化"><a href="#UppercasePipe-格式化" class="headerlink" title="UppercasePipe 格式化"></a>UppercasePipe 格式化</h3><p><code>&lt;h2&gt;{{hero.name | uppercase}} Details&lt;/h2&gt;</code><br> uppercase 位于管道操作符（ | ）的右边，用来调用内置管道 UppercasePipe<br>管道 是格式化字符串、金额、日期和其它显示数据的好办法</p><h3 id="双向绑定"><a href="#双向绑定" class="headerlink" title="双向绑定"></a>双向绑定</h3><p><code>&lt;input [(ngModel)]="hero.name" placeholder="name"/&gt;</code></p><p>[(ngModel)] 是 Angular 的双向数据绑定语法<br>这里把 hero.name 属性绑定到了 HTML 的 textbox 元素上</p><p>ngModel 在默认情况下是不可用，它属于一个可选模块 FormsModule，必须添加此模块才能使用该指令<br>在 AppModule 中，从 @angular/forms 库中导入 FormsModule， 添加到 @NgModule 元数据的 imports 数组</p><h3 id="NgModule"><a href="#NgModule" class="headerlink" title="@NgModule"></a>@NgModule</h3><p>Angular 需要知道如何把应用程序的各部分组合到一起，这些信息被称为元数据（metadata）</p><p>有些元数据位于 @Component 装饰器中，你会把它加到组件类上。<br>另一些关键性的元数据位于 @NgModule 装饰器中<br>最重要的 @NgModule 装饰器位于顶层类 AppModule 上</p><h3 id="声明-Component"><a href="#声明-Component" class="headerlink" title="声明 Component"></a>声明 Component</h3><p>Component 需要声明在AppModule的 @NgModule.declarations 数组中</p><h3 id="ngFor"><a href="#ngFor" class="headerlink" title="*ngFor"></a>*ngFor</h3><p><code>&lt;li *ngFor="let hero of heroes"&gt;</code></p><p>*ngFor 是一个复写器（repeater）指令，它会为列表中的每项数据复写它的宿主元素</p><li> 就是 *ngFor 的宿主元素，<h3 id="组件样式"><a href="#组件样式" class="headerlink" title="组件样式"></a>组件样式</h3><p>@Component 元数据中指定的样式和样式表都是局限于该组件的</p><h3 id="click-事件绑定"><a href="#click-事件绑定" class="headerlink" title="click 事件绑定"></a>click 事件绑定</h3><p><code>&lt;li *ngFor="let hero of heroes" (click)="onSelect(hero)"&gt;</code><br>click 外面的圆括号会让 Angular 监听这个 </p></li><li> 元素的 click 事件。 当用户点击 </li><li> 时，Angular 就会执行表达式 onSelect(hero)。<p></p><h3 id="ngIf"><a href="#ngIf" class="headerlink" title="*ngIf"></a>*ngIf</h3><p><code>&lt;div *ngIf="selectedHero"&gt;</code><br>selectedHero为undefined时不进行显示，避免报错</p><h3 id="CSS-类绑定机制"><a href="#CSS-类绑定机制" class="headerlink" title="CSS 类绑定机制"></a>CSS 类绑定机制</h3><pre><code class="js">[class.classname]="some-condition" eg: [class.selected]="hero === selectedHero"</code></pre><h3 id="Input"><a href="#Input" class="headerlink" title="@Input()"></a>@Input()</h3><p>import { Component, OnInit, Input } from ‘@angular/core’;<br>@Input() hero: Hero;<br>从hero-com传入某个hero信息到hero-detail中<br>hero 必须是带有 @Input() 装饰器的输入属性，因为外部的 HeroesComponent 将会绑定到它<br>&lt;app-hero-detail [hero]=”selectedHero”&gt;<br>[hero]=”selectedHero” 是属性绑定语法，单向数据绑定，selectedHero 属性绑定到目标元素的 hero 属性</p><h3 id="创建服务"><a href="#创建服务" class="headerlink" title="创建服务"></a>创建服务</h3><p>用Angular 的依赖注入机制将service注入组件构造函数中<br>创建 HeroService：ng generate service hero，生成如下：</p><pre><code class="js">import { Injectable } from '@angular/core';@Injectable({  providedIn: 'root',})export class HeroService {  constructor() { }}</code></pre><p>@Injectable() 装饰器会接受该服务的元数据对象，就像 @Component() 对组件类的作用一样。<br>注入器是一个对象，负责当应用要求获取它的实例时选择和注入该提供者。</p><h3 id="注入-HeroService"><a href="#注入-HeroService" class="headerlink" title="注入 HeroService"></a>注入 HeroService</h3><p>在component中：<br>constructor(private heroService: HeroService) { }<br>在创建组件时，依赖注入系统把service设置为组件的单例对象</p><h3 id="可观察（Observable）数据"><a href="#可观察（Observable）数据" class="headerlink" title="可观察（Observable）数据"></a>可观察（Observable）数据</h3><p>HeroService.getHeroes() 是异步函数，需要使用HttpClient.get获取数据，并返回Observable对象<br>Observable 是 RxJS 库中的一个关键类</p><p>使用RxJS的of( )模拟服务器返回数据，hero.service中：</p><pre><code>import { Observable, of } from 'rxjs';getHeroes(): Observable&lt;Hero[]&gt; {  return of(HEROES);}</code></pre><p>of(HEROES) 会返回一个 Observable&lt;Hero[]&gt;，它会发出单个值，这里是一个英雄数组</p><h3 id="在-Component-中订阅Service"><a href="#在-Component-中订阅Service" class="headerlink" title="在 Component 中订阅Service"></a>在 Component 中订阅Service</h3><p>getHeroes(): void {<br>  this.heroService.getHeroes()<br>      .subscribe(heroes =&gt; this.heroes = heroes);<br>}</p><p>在模板中绑定的组件属性必须是公共属性</p><h3 id="添加AppRoutingModule"><a href="#添加AppRoutingModule" class="headerlink" title="添加AppRoutingModule"></a>添加AppRoutingModule</h3><p>ng generate module app-routing –flat –module=app<br>–flat 把文件放进src/app 中，而不是单独的目录中。<br>–module=app 告诉 CLI 把它注册到 AppModule 的 imports 数组中</p><p>Module代码：</p><pre><code class="js">import { NgModule } from '@angular/core';import { RouterModule, Routes } from '@angular/router';import { HeroesComponent } from './heroes/heroes.component';const routes: Routes = [  { path: 'heroes', component: HeroesComponent }];@NgModule({  imports: [RouterModule.forRoot(routes)],  exports: [RouterModule]})export class AppRoutingModule { }</code></pre><p>导入 RouterModule 和 Routes，以便该应用具有路由功能<br>Routes属性有两个参数，path和component</p><p>RouterModule.forRoot()<br>@NgModule 元数据会初始化路由器，并监听地址变化<br>forRoot() 会提供路由所需的服务提供者和指令，还会基于浏览器的当前 URL 执行首次导航</p><h3 id="添加路由出口-RouterOutlet"><a href="#添加路由出口-RouterOutlet" class="headerlink" title="添加路由出口 RouterOutlet"></a>添加路由出口 RouterOutlet</h3><p>AppComponent 的模板中添加 <router-outlet> 元素<br><router-outlet> 会告诉路由器要在哪里显示路由的视图，</router-outlet></router-outlet></p><h3 id="添加路由链接-routerLink"><a href="#添加路由链接-routerLink" class="headerlink" title="添加路由链接 (routerLink)"></a>添加路由链接 (routerLink)</h3><p>添加</p><nav> 元素，并在其中放 <a> 元素，点击时触发导航<p></p></a><nav><a>  </a><a routerlink="/heroes">Heroes</a></nav><p>routerLink 的值为 “/heroes”，路由器用它匹配指向 HeroesComponent 的路由<br>routerLink 是 RouterLink 指令的选择器，它会把用户的点击转换为路由器的导航操作。 它是 RouterModule 中的另一个公共指令。</p><h3 id="添加默认路由"><a href="#添加默认路由" class="headerlink" title="添加默认路由"></a>添加默认路由</h3><p>{ path: ‘’, redirectTo: ‘/dashboard’, pathMatch: ‘full’ },</p><h3 id="抽离hero-detail组件"><a href="#抽离hero-detail组件" class="headerlink" title="抽离hero-detail组件"></a>抽离hero-detail组件</h3><p>app-routing.module.ts<br>{ path: ‘detail/:id’, component: HeroDetailComponent },<br>path 中的冒号（:）表示 :id 是一个占位符，表示特定id</p><h3 id="支持路由的-HeroDetail"><a href="#支持路由的-HeroDetail" class="headerlink" title="支持路由的 HeroDetail"></a>支持路由的 HeroDetail</h3><p>从一种新途径获取要显示的英雄<br>获取创建本组件的路由，从路由提取 id，通过 HeroService 获取这个 id 的英雄数据<br>hero-detail.component.ts代码如下：</p><pre><code class="js">import { ActivatedRoute } from '@angular/router';import { Location } from '@angular/common';import { HeroService } from '../hero.service';// 将三个服务注入构造函数constructor(  private route: ActivatedRoute,  private heroService: HeroService,  private location: Location) { }</code></pre><p>ActivatedRoute 保存路由信息，location 是Angular 服务，用来与浏览器打交道<br>从路由参数中提取 id：<br>const id = +this.route.snapshot.paramMap.get(‘id’);<br>route.snapshot 是一个路由信息的静态快照，抓取自组件刚创建完毕时<br>paramMap 是从 URL 提取的路由参数值的字典</p><p>hero.service.ts代码如下：</p><pre><code class="js">getHero(id: number): Observable&lt;Hero&gt; {  this.messageService.add(`HeroService: fetched hero id=${id}`);  return of(HEROES.find(hero =&gt; hero.id === id));}</code></pre><h3 id="回到原路"><a href="#回到原路" class="headerlink" title="回到原路"></a>回到原路</h3><pre><code class="js">&lt;button (click)="goBack()"&gt;go back&lt;/button&gt;goBack(): void {  this.location.back();}</code></pre><h3 id="启用-HTTP-服务"><a href="#启用-HTTP-服务" class="headerlink" title="启用 HTTP 服务"></a>启用 HTTP 服务</h3><p>HttpClient 是 Angular 通过 HTTP 与远程服务器通讯的机制<br>把HttpClient添加到根模块 AppModule 中</p><pre><code class="js">import { HttpClientModule } from '@angular/common/http';@NgModule({  imports: [    HttpClientModule,  ],})</code></pre><h3 id="模拟数据服务器-In-memory-Web-API"><a href="#模拟数据服务器-In-memory-Web-API" class="headerlink" title="模拟数据服务器 In-memory Web API"></a>模拟数据服务器 In-memory Web API</h3><p>npm install angular-in-memory-web-api –save</p><p>在 AppModule 中导入 HttpClientInMemoryWebApiModule 和 InMemoryDataService 类</p><p>将 WebApiModule 添加到 AppModule 的 imports 数组中，并以 InMemoryDataService 为参数进行配置<br>HttpClientInMemoryWebApiModule.forRoot(<br>  InMemoryDataService, { dataEncapsulation: false }<br>)<br>forRoot() 配置方法接收一个 InMemoryDataService 类来初始化内存数据库</p><h3 id="模拟Http请求"><a href="#模拟Http请求" class="headerlink" title="模拟Http请求"></a>模拟Http请求</h3><p>hero.service.ts代码如下：<br>import { HttpClient, HttpHeaders } from ‘@angular/common/http’;<br>constructor(private http: HttpClient,) { }</p><p>将 RxJS 的 of( ) 模拟转换成使用 HttpClient ，两者都返回Observable&lt;Hero[]&gt;<br>getHeroes(): Observable&lt;Hero[]&gt; {<br>  return of(HEROES);<br>}</p><p>getHeroes(): Observable&lt;Hero[]&gt; {<br>  return this.http.get&lt;Hero[]&gt;(this.heroesUrl)<br>}</p><p>所有的 HttpClient 方法都会返回某个值的 RxJS Observable</p><p>HttpClient.get() 返回响应数据<br>get() 默认情况把响应体当做无类型的 JSON 对象进行返回。 如果指定了可选的模板类型 &lt;Hero[]&gt;，就会给返回你一个类型化的对象。</p><p>API 可能在返回对象中深埋着你想要的数据，要借助 RxJS 的 map() 对结果处理</p><h3 id="错误处理"><a href="#错误处理" class="headerlink" title="错误处理"></a>错误处理</h3><p>捕获错误使用 RxJS 的 catchError() 建立对 Observable 结果的处理管道（pipe）<br>/hero.service.ts代码如下：<br>import { catchError, map, tap } from ‘rxjs/operators’;</p><p>getHeroes( ): Observable&lt;Hero[]&gt; {<br>  return this.http.get&lt;Hero[]&gt;(this.heroesUrl)<br>    .pipe(<br>      catchError(this.handleError&lt;Hero[]&gt;(‘getHeroes’, [ ]))<br>    );<br>}</p><p>catchError( ) 拦截失败的 Observable，把错误对象传给错误处理器</p><h3 id="handleError通用化"><a href="#handleError通用化" class="headerlink" title="handleError通用化"></a>handleError通用化</h3><p>不直接处理错误，而是返回给 catchError 一个错误处理函数</p><pre><code>private handleError&lt;T&gt;(operation = 'operation', result?: T) {  // 匿名函数  return (error: any): Observable&lt;T&gt; =&gt; {    console.error(error); // log to console instead    this.log(`${operation} failed: ${error.message}`);    return of(result as T);  };}</code></pre><h3 id="窥探-Observable"><a href="#窥探-Observable" class="headerlink" title="窥探 Observable"></a>窥探 Observable</h3><p>使用 RxJS 的 tap( ) 查看 使用 传出Observable 的值，但不会改变值本身<br>hero.service.ts代码如下：<br>getHeroes( ): Observable&lt;Hero[ ]&gt; {<br>  return this.http.get&lt;Hero[]&gt;(this.heroesUrl)<br>    .pipe(<br>      tap(_ =&gt; this.log(‘fetched heroes’)),<br>      catchError(this.handleError&lt;Hero[]&gt;(‘getHeroes’, []))<br>    );<br>}</p><h3 id="通过-id-获取英雄"><a href="#通过-id-获取英雄" class="headerlink" title="通过 id 获取英雄"></a>通过 id 获取英雄</h3><p>hero.service.ts代码如下：<br>getHero(id: number): Observable<hero> {<br>  const url = <code>${this.heroesUrl}/${id}</code>;  // 构建url</hero></p><p>  return this.http.get<hero>(url).pipe(<br>    tap(_ =&gt; this.log(<code>fetched hero id=${id}</code>)),<br>    catchError(this.handleError<hero>(<code>getHero id=${id}</code>))<br>  );<br>}</hero></hero></p><h3 id="修改英雄"><a href="#修改英雄" class="headerlink" title="修改英雄"></a>修改英雄</h3><p>httpOptions = {<br>  headers: new HttpHeaders({ ‘Content-Type’: ‘application/json’ })<br>};</p><p>updateHero(hero: Hero): Observable<any> {<br>  return this.http.put(this.heroesUrl, hero, this.httpOptions).pipe(<br>    tap(_ =&gt; this.log(<code>updated hero id=${hero.id}</code>)),<br>    catchError(this.handleError<any>(‘updateHero’))<br>  );<br>}</any></any></p><h3 id="添加新英雄"><a href="#添加新英雄" class="headerlink" title="添加新英雄"></a>添加新英雄</h3><p>return this.http.post<hero>(this.heroesUrl, hero, this.httpOptions).pipe()</hero></p><h3 id="删除某个英雄"><a href="#删除某个英雄" class="headerlink" title="删除某个英雄"></a>删除某个英雄</h3><p>heroes.component.ts代码如下：<br>delete(hero: Hero): void {<br>  // 去掉被删除数据后的列表<br>  this.heroes = this.heroes.filter(h =&gt; h !== hero);<br>  this.heroService.deleteHero(hero).subscribe();<br>}</p><p>hero.service.ts<br>deleteHero(hero: Hero | number): Observable<hero> {<br>  const id = typeof hero === ‘number’ ? hero : hero.id;<br>  const url = <code>${this.heroesUrl}/${id}</code>;</hero></p><p>  return this.http.delete<hero>(url, this.httpOptions).pipe(<br>    tap(_ =&gt; this.log(<code>deleted hero id=${id}</code>)),<br>    catchError(this.handleError<hero>(‘deleteHero’))<br>  );<br>}</hero></hero></p><h3 id="根据名字搜索"><a href="#根据名字搜索" class="headerlink" title="根据名字搜索"></a>根据名字搜索</h3><p> 当用户在搜索框中输入名字时，会不断发送HTTP 请求，目标是发出尽可能少的必要请求<br>hero.service.ts</p><pre><code>searchHeroes(term: string): Observable&lt;Hero[]&gt; {  if (!term.trim()) {    // if not search term, return empty hero array.    return of([]);  }  return this.http.get&lt;Hero[]&gt;(`${this.heroesUrl}/?name=${term}`).pipe(    tap(x =&gt; x.length ?       this.log(`found heroes matching "${term}"`) :       this.log(`no heroes matching "${term}"`)),    catchError(this.handleError&lt;Hero[]&gt;('searchHeroes', []))  );}</code></pre><p>展示搜索结果的列表<br>hero-search.component.html</p><div id="search-component">  <h4><label for="search-box">Hero Search</label></h4><p>  &lt;input #searchBox id=”search-box” (input)=”search(searchBox.value)” /&gt;</p>  <ul class="search-result">    <li *ngfor="let hero of heroes$ | async">      <a routerlink="/detail/">              </a>    </li>  </ul></div>$ 是一个约定，表示 heroes$ 是一个 Observable 而不是数组由于 *ngFor 不能直接使用 Observable，要使用管道字符（|），后面跟着 async表示 AsyncPipe 管道，它会自动订阅 Observable，组件就不再需要订阅heroes$<h3 id="RxJS-Subject-类型的-searchTerms"><a href="#RxJS-Subject-类型的-searchTerms" class="headerlink" title="RxJS Subject 类型的 searchTerms"></a>RxJS Subject 类型的 searchTerms</h3><pre><code class="js">private searchTerms = new Subject&lt;string&gt;( );search(term: string): void {  this.searchTerms.next(term);}</code></pre><p>Subject 既是Observable对象的数据源，本身也是 Observable<br>通过调用next(value) 往 Observable 中推送一些值</p><h3 id="串联-RxJS-操作符"><a href="#串联-RxJS-操作符" class="headerlink" title="串联 RxJS 操作符"></a>串联 RxJS 操作符</h3><p>ngOnInit( ) 往 searchTerms 的处理管道中加入 RxJS 操作符，缩减对 searchHeroes( ) 的调用次数，并返回一个搜索结果的可观察对象（Hero[ ] ）</p><pre><code class="js">this.heroes$ = this.searchTerms.pipe(  // 传出最终字符串之前，debounceTime(300) 将会等待直至300内无新事件  debounceTime(300),  // 只在过滤条件变化时发送请求  distinctUntilChanged(),  // 取消丢弃旧的数据，保留最新的  switchMap((term: string) =&gt; this.heroService.searchHeroes(term)),);</code></pre></nav></li>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> front </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="2021/03/29/visual/(1)%E5%8F%AF%E8%A7%86%E5%8C%96%E5%89%8D%E7%AB%AF%E6%8A%80%E6%9C%AF%E9%80%89%E5%9E%8B/"/>
      <url>2021/03/29/visual/(1)%E5%8F%AF%E8%A7%86%E5%8C%96%E5%89%8D%E7%AB%AF%E6%8A%80%E6%9C%AF%E9%80%89%E5%9E%8B/</url>
      
        <content type="html"><![CDATA[<h3 id="Skia"><a href="#Skia" class="headerlink" title="Skia"></a>Skia</h3><p>chrome和安卓的底层2D绘图引擎，c++编程</p><h3 id="OpenGL"><a href="#OpenGL" class="headerlink" title="OpenGL"></a>OpenGL</h3><p>2D,3D图像渲染库</p><h3 id="Chrome"><a href="#Chrome" class="headerlink" title="Chrome"></a>Chrome</h3><p>Chrome使用Skia，向上岑开放canvas，svg，webgl，html等绘图能力</p><h3 id="canvas"><a href="#canvas" class="headerlink" title="canvas"></a>canvas</h3><p>html5的新特性，使用canvas可以在网页上通过js绘制图像</p><p>使用流程：canvas标签，获取canvas对象，设置绘图属性，调用绘图api</p><h3 id=""><a href="#" class="headerlink" title=""></a></h3>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="2021/03/29/interview/zw-inter/(1)html-css-browser/"/>
      <url>2021/03/29/interview/zw-inter/(1)html-css-browser/</url>
      
        <content type="html"><![CDATA[<h3 id="1-图片格式"><a href="#1-图片格式" class="headerlink" title="1.图片格式"></a>1.图片格式</h3><p>最流行的是jpeg格式，提供11级压缩级别</p><h3 id="2-css盒子模型"><a href="#2-css盒子模型" class="headerlink" title="2.css盒子模型"></a>2.css盒子模型</h3><p>margin - border - padding - content<br>默认情况，盒子的width指content的宽度<br>盒子真正的宽是content + padding + border + margin</p><h3 id="3-视频-音频标签的使用"><a href="#3-视频-音频标签的使用" class="headerlink" title="3.视频/音频标签的使用"></a>3.视频/音频标签的使用</h3><p>width/height<br>autoplay自动播放<br>controls显示控制条<br>poster占位图片<br>loop是否循环<br>perload预加载视频，与autoplay冲突，设置了autoplay后perload失效<br>muted静音模式</p><h3 id="4-html5新增内容"><a href="#4-html5新增内容" class="headerlink" title="4.html5新增内容"></a>4.html5新增内容</h3><p>语义化标签、表单类型元素、表单属性、表单时间、多媒体标签</p><h3 id="5-语义化标签"><a href="#5-语义化标签" class="headerlink" title="5.语义化标签"></a>5.语义化标签</h3><p>优点：可访问性、seo、结构清晰可维护</p><pre><code class="html">&lt;header&gt;&lt;nav&gt;&lt;section&gt;&lt;article&gt;&lt;aside&gt;&lt;figcaption&gt;&lt;figure&gt;&lt;footer&gt;</code></pre><p><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210329104015.png"></p><h3 id="6-css3新增特性"><a href="#6-css3新增特性" class="headerlink" title="6.css3新增特性"></a>6.css3新增特性</h3><p>border：<br>  radius<br>  shadow<br>  image-source资源路径<br>  image-slice偏移<br>  image-width<br>  image-outset超出边框的量<br>  image-repeat是否平铺，repeat / round<br>background<br>  size<br>  origin位置所相对的定点位置<br>  clip绘制区域，paddding-box, border-box, content-box<br>gradient<br>  linear-gradient<br>  radial-gradient<br>文本效果<br>  word-break如何换行<br>  word-wrap允许自动换行<br>  text-overflow文本溢出处理<br>  text-shadow文本阴影<br>transform转换<br>  transform-origin<br>  transform-style<br>2D转换<br>  rotate<br>  translate<br>  scale<br>3D转换<br>  perspective(n)<br>transition过渡<br>  property属性名<br>  duration过渡时间<br>  timing-function过渡速度<br>  delay切换时间<br>animation动画<br>  name为@keyframes动画名称<br>  duration<br>  timing-funciton<br>  delay<br>  iteration-count播放次数<br>  direction轮流反向播放动画</p><h3 id="7-清除浮动及优点"><a href="#7-清除浮动及优点" class="headerlink" title="7.清除浮动及优点"></a>7.清除浮动及优点</h3><p>  父元素高度塌陷，因为子浮动而且父不设高度<br>  深入剖析原因</p><p>  清除浮动<br>  1.父设置高度，不能响应式<br>  2.父overflow: hidden; zoom:1; 超出部分被隐藏，需要注意<br>  3.浮动元素后面加空标签，clear: both; height: 0; overflow: hidden; 增加空标签<br>  4.父overflow: auto; 内部宽高超过父div时，出现滚动条<br>  5.万能清除法</p><pre><code class="css">.father:after {  content: '';  clear: both;  display: block;  height: 0;  overflow: hidden;  visibility: hidden;}</code></pre><h3 id="8-定位position"><a href="#8-定位position" class="headerlink" title="8.定位position"></a>8.定位position</h3><p>relative：不脱离，相对自己<br>absolute：脱离，相对父级<br>fixed：脱离，相对窗口<br>static：默认</p><h3 id="9-子元素在父元素居中"><a href="#9-子元素在父元素居中" class="headerlink" title="9.子元素在父元素居中"></a>9.子元素在父元素居中</h3><p>水平居中<br>1.子父宽度固定，子设置margin:auto，并且不能设置浮动<br>2.子父宽度固定，父设置text-align: center，子设置displat:inline-block;并不设置浮动</p><p>水平垂直居中<br>子绝父相，子top，left50%，margin-top, left减去子宽高的一半<br>子绝父相，子top，left50%，transform: translate(-50%, -50%)<br>子绝父相，子上下左右全0，margin：auto<br>父display:table-cell; vertical-align: middle，子margin: auto;<br>子相，top, left50%, transform: translate(-50%, -50%)<br>父弹性盒子，display: flex; justify-content: center; align-item: center;</p><h3 id="10-border-box和contetn-box"><a href="#10-border-box和contetn-box" class="headerlink" title="10.border-box和contetn-box"></a>10.border-box和contetn-box</h3><p>content-box的width不包括padding和border，border包括</p><h3 id="11-元素垂直居中"><a href="#11-元素垂直居中" class="headerlink" title="11.元素垂直居中"></a>11.元素垂直居中</h3><p>1.设置子元素和父元素行高一致<br>2.子元素设为inline-block，加上vertical-align: middle;<br>3.已知父高度，子相，子top50%，transform： translateY(-50%)<br>4.不知父高度，子绝父相，子top50%，transform： translateY(-50%)<br>5.创建隐藏节点，设置height<br>6.父display:table, 子display:table-cell，vertical-align: middle<br>7.父添加伪元素<br>8.父弹性盒子，子align-self:center;</p><h3 id="12-让chrome显示小于12px的文章"><a href="#12-让chrome显示小于12px的文章" class="headerlink" title="12.让chrome显示小于12px的文章"></a>12.让chrome显示小于12px的文章</h3><p>添加谷歌私有属性 -webkit-text-size-adjust:none;<br>现在 -webkit-transform: scale()</p><h3 id="13-css选择器，哪些属性可以继承，优先级如何计算，css3新增伪类有哪些"><a href="#13-css选择器，哪些属性可以继承，优先级如何计算，css3新增伪类有哪些" class="headerlink" title="13.css选择器，哪些属性可以继承，优先级如何计算，css3新增伪类有哪些"></a>13.css选择器，哪些属性可以继承，优先级如何计算，css3新增伪类有哪些</h3><p>css2选择器：元素，id，群组，类，*通配符，后代选择器<br>css2伪类选择器：a:link/visited/hover/active<br>css3选择器：空格 &gt; +相邻兄弟选择器 ~通用选择器</p><p>结构伪类选择器：<br>查找第几个nth-child(n)<br>查找同类的第几个nth-of-type<br>查找唯一类型 only-pf-type<br>属性选择器：根绝标签属性查找 [attr=value]<br>:root<br>:empty</p><p>目标伪类选择器（表单）<br>:enabled<br>:disabled<br>:checked</p><p>伪元素选择器<br>::selection设置玄宗文本内容的高亮显示<br>否定选择器 not()<br>语言选择器 lang(取值)</p><p>优先级：<br>元素，伪元素1<br>class伪类属性10<br>id和内联100<br>继承0</p><p>可继承属性：子节点默认使用父节点的样式属性<br>主要是文本方面的<br>可以继承的属性很少，只有颜色，文字，字体间距行高对齐方式，和列表的样式可以继承</p><p>所有元素可继承：visibility和cursor<br>块级元素可继承：text-indent和text-align<br>列表元素可继承：</p><h3 id="14-图片加载优化"><a href="#14-图片加载优化" class="headerlink" title="14.图片加载优化"></a>14.图片加载优化</h3><p>1.懒加载，不可视区域添加滚动事件，判断图片位置和浏览器顶端和页面的距离，如果前者小于后者，优先加载<br>2.预加载，将当前展示的前后两张优先下载<br>3.雪碧图csssprite或者svgsprite</p><h3 id="15-行内元素-块级元素"><a href="#15-行内元素-块级元素" class="headerlink" title="15.行内元素/块级元素"></a>15.行内元素/块级元素</h3><p>行内：相邻的行内元素会排列同行，宽高无效 span<br>块级：独占一行，宽高有效，div<br>可变：根据上下文决定块级还是内联</p><p>块级：div,h,p,ul,ol,table,from, menu<br>行内元素：a,br,i,imginput,select,span,sub,sup,textarea<br>可变：button,del,iframe,ins</p><h3 id="16-浏览器的标准模式和怪异模式"><a href="#16-浏览器的标准模式和怪异模式" class="headerlink" title="16.浏览器的标准模式和怪异模式"></a>16.浏览器的标准模式和怪异模式</h3><p>标准：浏览器按照w3c解析代码<br>怪异：浏览器根据自己的方式解析<br>区别：盒模型，图片的垂直对齐方式</p><h3 id=""><a href="#" class="headerlink" title=""></a></h3><h3 id="-1"><a href="#-1" class="headerlink" title=""></a></h3><h3 id="-2"><a href="#-2" class="headerlink" title=""></a></h3><h3 id="-3"><a href="#-3" class="headerlink" title=""></a></h3><h3 id="-4"><a href="#-4" class="headerlink" title=""></a></h3><h3 id="-5"><a href="#-5" class="headerlink" title=""></a></h3><h3 id="-6"><a href="#-6" class="headerlink" title=""></a></h3><h3 id="-7"><a href="#-7" class="headerlink" title=""></a></h3><h3 id="-8"><a href="#-8" class="headerlink" title=""></a></h3>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>(20)设计模式</title>
      <link href="2021/03/29/yck/(20)%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
      <url>2021/03/29/yck/(20)%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/</url>
      
        <content type="html"><![CDATA[<h1 id="30-设计模式"><a href="#30-设计模式" class="headerlink" title="30.设计模式"></a>30.设计模式</h1><p>设计模式总的来说是一个抽象的概念，前人通过无数次的实践总结出的一套写代码的方式，通过这种方式写的代码可以让别人更加容易阅读、维护以及复用。</p><h3 id="简单工厂模式"><a href="#简单工厂模式" class="headerlink" title="简单工厂模式"></a>简单工厂模式</h3><pre><code class="javascript">class Man {  constructor(name) {    this.name = name  }  alertName() {    alert(this.name)  }}class Factory {  static create(name) {    return new Man(name)  }}Factory.create('yck').alertName()</code></pre><p>当然工厂模式并不仅仅是用来 new 出实例。</p><p>可以想象一个场景。假设有一份很复杂的代码需要用户去调用，但是用户并不关心这些复杂的代码，只需要你提供给我一个接口去调用，用户只负责传递需要的参数，至于这些参数怎么使用，内部有什么逻辑是不关心的，只需要你最后返回我一个实例。这个构造过程就是工厂。</p><p>工厂起到的作用就是隐藏了创建实例的复杂度，只需要提供一个接口，简单清晰。</p><p>在 Vue 源码中，你也可以看到工厂模式的使用，比如创建异步组件</p><pre><code class="javascript">export function createComponent (  Ctor: Class&lt;Component&gt; | Function | Object | void,  data: ?VNodeData,  context: Component,  children: ?Array&lt;VNode&gt;,  tag?: string): VNode | Array&lt;VNode&gt; | void {        // 逻辑处理...    const vnode = new VNode(    `vue-component-${Ctor.cid}${name ? `-${name}` : ''}`,    data, undefined, undefined, undefined, context,    { Ctor, propsData, listeners, tag, children },    asyncFactory  )  return vnode}</code></pre><p>在上述代码中，我们可以看到我们只需要调用 createComponent 传入参数就能创建一个组件实例，但是创建这个实例是很复杂的一个过程，工厂帮助我们隐藏了这个复杂的过程，只需要一句代码调用就能实现功能。</p><h3 id="单例模式"><a href="#单例模式" class="headerlink" title="单例模式"></a>单例模式</h3><p>单例模式很常用，比如全局缓存、全局状态管理等等这些只需要一个对象，就可以使用单例模式。</p><p>单例模式的核心就是保证全局只有一个对象可以访问。因为 JS 是门无类的语言，所以别的语言实现单例的方式并不能套入 JS 中，我们只需要用一个变量确保实例只创建一次就行，以下是如何实现单例模式的例子</p><pre><code class="javascript">class Singleton {  constructor() {}}Singleton.getInstance = (function() {  let instance  return function() {    if (!instance) {      instance = new Singleton()    }    return instance  }})()let s1 = Singleton.getInstance()let s2 = Singleton.getInstance()console.log(s1 === s2) // true</code></pre><p>在 Vuex 源码中，你也可以看到单例模式的使用，虽然它的实现方式不大一样，通过一个外部变量来控制只安装一次 Vuex</p><pre><code class="javascript">let Vue // bind on installexport function install (_Vue) {  if (Vue &amp;&amp; _Vue === Vue) {    // 如果发现 Vue 有值，就不重新创建实例了    return  }  Vue = _Vue  applyMixin(Vue)}</code></pre><h3 id="适配器模式"><a href="#适配器模式" class="headerlink" title="适配器模式"></a>适配器模式</h3><p>适配器用来解决两个接口不兼容的情况，不需要改变已有的接口，通过包装一层的方式实现两个接口的正常协作。</p><p>以下是如何实现适配器模式的例子</p><pre><code class="javascript">class Plug {  getName() {    return '港版插头'  }}class Target {  constructor() {    this.plug = new Plug()  }  getName() {    return this.plug.getName() + ' 适配器转二脚插头'  }}let target = new Target()target.getName() // 港版插头 适配器转二脚插头</code></pre><p>在 Vue 中，我们其实经常使用到适配器模式。比如父组件传递给子组件一个时间戳属性，组件内部需要将时间戳转为正常的日期显示，一般会使用 computed 来做转换这件事情，这个过程就使用到了适配器模式。</p><h3 id="装饰模式"><a href="#装饰模式" class="headerlink" title="装饰模式"></a>装饰模式</h3><p>装饰模式不需要改变已有的接口，作用是给对象添加功能。就像我们经常需要给手机戴个保护套防摔一样，不改变手机自身，给手机添加了保护套提供防摔功能。</p><p>以下是如何实现装饰模式的例子，使用了 ES7 中的装饰器语法</p><pre><code class="javascript">function readonly(target, key, descriptor) {  descriptor.writable = false  return descriptor}class Test {  @readonly  name = 'yck'}let t = new Test()t.yck = '111' // 不可修改在 React 中，装饰模式其实随处可见import { connect } from 'react-redux'class MyComponent extends React.Component {    // ...}export default connect(mapStateToProps)(MyComponent)</code></pre><h3 id="代理模式"><a href="#代理模式" class="headerlink" title="代理模式"></a>代理模式</h3><p>代理是为了控制对对象的访问，不让外部直接访问到对象。在现实生活中，也有很多代理的场景。比如你需要买一件国外的产品，这时候你可以通过代购来购买产品。</p><p>在实际代码中其实代理的场景很多，也就不举框架中的例子了，比如事件代理就用到了代理模式。</p><pre><code class="javascript">&lt;ul id="ul"&gt;    &lt;li&gt;1&lt;/li&gt;    &lt;li&gt;2&lt;/li&gt;    &lt;li&gt;3&lt;/li&gt;    &lt;li&gt;4&lt;/li&gt;    &lt;li&gt;5&lt;/li&gt;&lt;/ul&gt;&lt;script&gt;    let ul = document.querySelector('#ul')    ul.addEventListener('click', (event) =&gt; {        console.log(event.target);    })&lt;/script&gt;</code></pre><p>因为存在太多的 li，不可能每个都去绑定事件。这时候可以通过给父节点绑定一个事件，让父节点作为代理去拿到真实点击的节点。</p><h3 id="发布-订阅模式"><a href="#发布-订阅模式" class="headerlink" title="发布-订阅模式"></a>发布-订阅模式</h3><p>发布-订阅模式也叫做观察者模式。通过一对一或者一对多的依赖关系，当对象发生改变时，订阅方都会收到通知。在现实生活中，也有很多类似场景，比如我需要在购物网站上购买一个产品，但是发现该产品目前处于缺货状态，这时候我可以点击有货通知的按钮，让网站在产品有货的时候通过短信通知我。</p><p>在实际代码中其实发布-订阅模式也很常见，比如我们点击一个按钮触发了点击事件就是使用了该模式</p><pre><code class="javascript">&lt;ul id="ul"&gt;&lt;/ul&gt;&lt;script&gt;    let ul = document.querySelector('#ul')    ul.addEventListener('click', (event) =&gt; {        console.log(event.target);    })&lt;/script&gt;</code></pre><p>在 Vue 中，如何实现响应式也是使用了该模式。对于需要实现响应式的对象来说，在 get 的时候会进行依赖收集，当改变了对象的属性时，就会触发派发更新。</p><h3 id="外观模式"><a href="#外观模式" class="headerlink" title="外观模式"></a>外观模式</h3><p>外观模式提供了一个接口，隐藏了内部的逻辑，更加方便外部调用。</p><p>举个例子来说，我们现在需要实现一个兼容多种浏览器的添加事件方法</p><pre><code class="javascript">function addEvent(elm, evType, fn, useCapture) {  if (elm.addEventListener) {    elm.addEventListener(evType, fn, useCapture)    return true  } else if (elm.attachEvent) {    var r = elm.attachEvent("on" + evType, fn)    return r  } else {    elm["on" + evType] = fn  }}</code></pre><p>对于不同的浏览器，添加事件的方式可能会存在兼容问题。如果每次都需要去这样写一遍的话肯定是不能接受的，所以我们将这些判断逻辑统一封装在一个接口中，外部需要添加事件只需要调用 addEvent 即可。</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(19)输入 URL 到页面渲染的整个流程</title>
      <link href="2021/03/29/yck/(19)%E8%BE%93%E5%85%A5URL%E5%88%B0%E9%A1%B5%E9%9D%A2%E6%B8%B2%E6%9F%93%E7%9A%84%E8%BF%87%E7%A8%8B/"/>
      <url>2021/03/29/yck/(19)%E8%BE%93%E5%85%A5URL%E5%88%B0%E9%A1%B5%E9%9D%A2%E6%B8%B2%E6%9F%93%E7%9A%84%E8%BF%87%E7%A8%8B/</url>
      
        <content type="html"><![CDATA[<h1 id="29-输入-URL-到页面渲染的整个流程"><a href="#29-输入-URL-到页面渲染的整个流程" class="headerlink" title="29.输入 URL 到页面渲染的整个流程"></a>29.输入 URL 到页面渲染的整个流程</h1><h3 id="大致过程"><a href="#大致过程" class="headerlink" title="大致过程"></a>大致过程</h3><p>DNS解析、TCP握手建立连接、TLS握手开始传输数据、浏览器判断状态码、解析文件；<br>构建DOM树和CSSOM树，遇到script标签就去请求js文件并执行，生成Render树，调用GPU绘制</p><h3 id="详细过程"><a href="#详细过程" class="headerlink" title="详细过程"></a>详细过程</h3><p>首先是 DNS 查询，如果这一步做了智能 DNS 解析的话，会提供访问速度最快的 IP 地址回来，这部分的内容之前没有写过，所以就在这里讲解下。</p><h3 id="DNS"><a href="#DNS" class="headerlink" title="DNS"></a>DNS</h3><p>DNS 的作用就是通过域名查询到具体的 IP。</p><p>因为 IP 存在数字和英文的组合（IPv6），很不利于人类记忆，所以就出现了域名。你可以把域名看成是某个 IP 的别名，DNS 就是去查询这个别名的真正名称是什么。</p><p>在 TCP 握手之前就已经进行了 DNS 查询，这个查询是操作系统自己做的。当你在浏览器中想访问 <a href="http://www.google.com/">www.google.com</a> 时，会进行一下操作：</p><p>操作系统会首先在本地缓存中查询 IP<br>没有的话会去系统配置的 DNS 服务器中查询<br>如果这时候还没得话，会直接去 DNS 根服务器查询，这一步查询会找出负责 com 这个一级域名的服务器<br>然后去该服务器查询 google 这个二级域名<br>接下来三级域名的查询其实是我们配置的，你可以给 www 这个域名配置一个 IP，然后还可以给别的三级域名配置一个 IP<br>以上介绍的是 DNS 迭代查询，还有种是递归查询，区别就是前者是由客户端去做请求，后者是由系统配置的 DNS 服务器做请求，得到结果后将数据返回给客户端。</p><p>PS：DNS 是基于 UDP 做的查询，大家也可以考虑下为什么之前不考虑使用 TCP 去实现。</p><h3 id="TCP-握手-网络结构"><a href="#TCP-握手-网络结构" class="headerlink" title="TCP 握手, 网络结构"></a>TCP 握手, 网络结构</h3><p>应用层会下发数据给传输层，这里 TCP 协议会指明两端的端口号，然后下发给网络层。网络层中的 IP 协议会确定 IP 地址，并且指示了数据传输中如何跳转路由器。然后包会再被封装到数据链路层的数据帧结构中，最后就是物理层面的传输了。</p><p>在这一部分中，可以详细说下 TCP 的握手情况以及 TCP 的一些特性。</p><h3 id="TLS传输"><a href="#TLS传输" class="headerlink" title="TLS传输"></a>TLS传输</h3><p>当 TCP 握手结束后就会进行 TLS 握手，然后就开始正式的传输数据。</p><p>在这一部分中，可以详细说下 TLS 的握手情况以及两种加密方式的内容。</p><h3 id="负载均衡"><a href="#负载均衡" class="headerlink" title="负载均衡"></a>负载均衡</h3><p>数据在进入服务端之前，可能还会先经过负责负载均衡的服务器，它的作用就是将请求合理的分发到多台服务器上，这时假设服务端会响应一个 HTML 文件。</p><h3 id="浏览器判断状态码"><a href="#浏览器判断状态码" class="headerlink" title="浏览器判断状态码"></a>浏览器判断状态码</h3><p>首先浏览器会判断状态码是什么，如果是 200 那就继续解析，如果 400 或 500 的话就会报错，如果 300 的话会进行重定向，这里会有个重定向计数器，避免过多次的重定向，超过次数也会报错。</p><h3 id="解析文件"><a href="#解析文件" class="headerlink" title="解析文件"></a>解析文件</h3><p>浏览器开始解析文件，如果是 gzip 格式的话会先解压一下，然后通过文件的编码格式知道该如何去解码文件。</p><p>文件解码成功后会正式开始渲染流程，先会根据 HTML 构建 DOM 树，有 CSS 的话会去构建 CSSOM 树。如果遇到 script 标签的话，会判断是否存在 async 或者 defer ，前者会并行进行下载并执行 JS，后者会先下载文件，然后等待 HTML 解析完成后顺序执行。</p><p>如果以上都没有，就会阻塞住渲染流程直到 JS 执行完毕。遇到文件下载的会去下载文件，这里如果使用 HTTP/2 协议的话会极大的提高多图的下载效率。</p><p>CSSOM 树和 DOM 树构建完成后会开始生成 Render 树，这一步就是确定页面元素的布局、样式等等诸多方面的东西</p><p>在生成 Render 树的过程中，浏览器就开始调用 GPU 绘制，合成图层，将内容显示在屏幕上了。</p><p>这一部分就是渲染原理中讲解到的内容，可以详细的说明下这一过程。并且在下载文件时，也可以说下通过 HTTP/2 协议可以解决队头阻塞的问题。</p><p>总的来说这一章节就是带着大家从 DNS 查询开始到渲染出画面完整的了解一遍过程，将之前学习到的内容连接起来。</p><p>当来这一过程远远不止这些内容，但是对于大部分人能答出这些内容已经很不错了，你如果想了解更加详细的过程，可以阅读这篇文章。</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(18)HTTP</title>
      <link href="2021/03/29/yck/(18)Http/"/>
      <url>2021/03/29/yck/(18)Http/</url>
      
        <content type="html"><![CDATA[<h1 id="27-HTTP-及-TLS"><a href="#27-HTTP-及-TLS" class="headerlink" title="27.HTTP 及 TLS"></a>27.HTTP 及 TLS</h1><p>这一章节我们将来学习 HTTP 及 TLS 协议中的内容。</p><h3 id="HTTP-请求中的内容"><a href="#HTTP-请求中的内容" class="headerlink" title="HTTP 请求中的内容"></a>HTTP 请求中的内容</h3><p>HTTP 请求由三部分构成，分别为：</p><p>请求行<br>首部<br>实体<br>请求行大概长这样 GET /images/logo.gif HTTP/1.1，基本由请求方法、URL、协议版本组成，这其中值得一说的就是请求方法了。</p><p>请求方法分为很多种，最常用的也就是 Get 和 Post 了。虽然请求方法有很多，但是更多的是传达一个语义，而不是说 Post 能做的事情 Get 就不能做了。如果你愿意，都使用 Get 请求或者 Post 请求都是可以的。更多请求方法的语义描述可以阅读 文档。</p><p>常考面试题：Post 和 Get 的区别？</p><p>首先先引入副作用和幂等的概念。</p><p>副作用指对服务器上的资源做改变，搜索是无副作用的，注册是副作用的。</p><p>幂等指发送 M 和 N 次请求（两者不相同且都大于 1），服务器上资源的状态一致，比如注册 10 个和 11 个帐号是不幂等的，对文章进行更改 10 次和 11 次是幂等的。因为前者是多了一个账号（资源），后者只是更新同一个资源。</p><p>在规范的应用场景上说，Get 多用于无副作用，幂等的场景，例如搜索关键字。Post 多用于副作用，不幂等的场景，例如注册。</p><p>在技术上说：</p><p>Get 请求能缓存，Post 不能<br>Post 相对 Get 安全一点点，因为Get 请求都包含在 URL 里（当然你想写到 body 里也是可以的），且会被浏览器保存历史纪录。Post 不会，但是在抓包的情况下都是一样的。<br>URL有长度限制，会影响 Get 请求，但是这个长度限制是浏览器规定的，不是 RFC 规定的<br>Post 支持更多的编码类型且不对数据类型限制</p><h4 id="首部"><a href="#首部" class="headerlink" title="首部"></a>首部</h4><p>首部分为请求首部和响应首部，并且部分首部两种通用，接下来我们就来学习一部分的常用首部。</p><p>通用首部</p><p>通用字段    作用<br><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210113221721.png"></p><p>请求首部<br><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210113221744.png"></p><p>响应首部<br><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210113221838.png"></p><p>实体首部<br><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210113221920.png"></p><h4 id="常见状态码"><a href="#常见状态码" class="headerlink" title="常见状态码"></a>常见状态码</h4><p>状态码表示了响应的一个状态，可以让我们清晰的了解到这一次请求是成功还是失败，如果失败的话，是什么原因导致的，当然状态码也是用于传达语义的。如果胡乱使用状态码，那么它存在的意义就没有了。</p><p>状态码通常也是一道常考题。</p><p><strong>2XX 成功</strong></p><p>200 OK，请求被正确处理</p><p>204 No content，请求成功，但响应报文不含主体</p><p>205 Reset Content，请求成功，但响应报文不含主体，要求请求方重置内容</p><p>206 Partial Content，进行范围请求</p><p><strong>3XX 重定向</strong></p><p>301 moved permanently，永久性重定向</p><p>302 found，临时性重定向</p><p>303 see other，资源存在另一个 URL，应使用 GET 获取</p><p>304 not modified，允许访问，但请求未满足条件</p><p><strong>4XX 客户端错误</strong></p><p>400 bad request，请求报文语法错误</p><p>401 unauthorized，请求需要HTTP认证</p><p>403 forbidden，访问被服务器拒绝</p><p>404 not found，服务器没有找到请求资源</p><p>5XX 服务器错误</p><p>500 internal sever error，服务器端在执行请求时发生错误<br>501 Not Implemented，服务器不支持请求<br>503 service unavailable，服务器超负载，无法处理请求</p><h3 id="TLS"><a href="#TLS" class="headerlink" title="TLS"></a>TLS</h3><p>HTTPS 还是通过了 HTTP 来传输信息，但是信息通过 TLS 协议进行了加密。</p><p>TLS 协议位于传输层之上，应用层之下。首次进行 TLS 协议传输需要两个 RTT ，接下来可以通过 Session Resumption 减少到一个 RTT。</p><p>在 TLS 中使用了两种加密技术，分别为：对称加密和非对称加密。</p><h4 id="对称加密："><a href="#对称加密：" class="headerlink" title="对称加密："></a>对称加密：</h4><p>对称加密就是两边拥有相同的秘钥，两边都知道如何将密文加密解密。</p><p>这种加密方式固然很好，但是问题就在于如何让双方知道秘钥。因为传输数据都是走的网络，如果将秘钥通过网络的方式传递的话，一旦秘钥被截获就没有加密的意义的。</p><h4 id="非对称加密："><a href="#非对称加密：" class="headerlink" title="非对称加密："></a>非对称加密：</h4><p>有公钥私钥之分，公钥所有人都可以知道，可以将数据用公钥加密，但是将数据解密必须使用私钥解密，私钥只有分发公钥的一方才知道。</p><p>这种加密方式就可以完美解决对称加密存在的问题。假设现在两端需要使用对称加密，那么在这之前，可以先使用非对称加密交换秘钥。</p><p>简单流程如下：首先服务端将公钥公布出去，那么客户端也就知道公钥了。接下来客户端创建一个秘钥，然后通过公钥加密并发送给服务端，服务端接收到密文以后通过私钥解密出正确的秘钥，这时候两端就都知道秘钥是什么了。</p><h4 id="TLS-握手过程如下图："><a href="#TLS-握手过程如下图：" class="headerlink" title="TLS 握手过程如下图："></a>TLS 握手过程如下图：</h4><p>客户端发送一个随机值以及需要的协议和加密方式。</p><p>服务端收到客户端的随机值，自己也产生一个随机值，并根据客户端需求的协议和加密方式来使用对应的方式，并且发送自己的证书（如果需要验证客户端证书需要说明）</p><p>客户端收到服务端的证书并验证是否有效，验证通过会再生成一个随机值，通过服务端证书的公钥去加密这个随机值并发送给服务端，如果服务端需要验证客户端证书的话会附带证书</p><p>服务端收到加密过的随机值并使用私钥解密获得第三个随机值，这时候两端都拥有了三个随机值，可以通过这三个随机值按照之前约定的加密方式生成密钥，接下来的通信就可以通过该密钥来加密解密</p><p>通过以上步骤可知，在 TLS 握手阶段，两端使用非对称加密的方式来通信，但是因为非对称加密损耗的性能比对称加密大，所以在正式传输数据时，两端使用对称加密的方式通信。</p><p>PS：以上说明的都是 TLS 1.2 协议的握手情况，在 1.3 协议中，首次建立连接只需要一个 RTT，后面恢复连接不需要 RTT 了。</p><h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>总结一下内容：</p><p>HTTP 经常考到的内容包括：请求方法、首部的作用以及状态码的含义<br>TLS 中经常考到的内容包括：两种加密方式以及握手的流程</p><h1 id="28-HTTP-2-及-HTTP-3"><a href="#28-HTTP-2-及-HTTP-3" class="headerlink" title="28.HTTP/2 及 HTTP/3"></a>28.HTTP/2 及 HTTP/3</h1><h3 id="HTTP-2"><a href="#HTTP-2" class="headerlink" title="HTTP/2"></a>HTTP/2</h3><p>HTTP/2 相比于 HTTP/1，提高了网页的性能(多路复用、二进制编码、Header压缩)</p><p>在 HTTP/1 中，为了性能考虑，我们会引入雪碧图、将小图内联、使用多个域名等等的方式。这一切都是因为浏览器限制了同一个域名下的请求数量（Chrome 下一般是限制六个连接），当页面中需要请求很多资源的时候，队头阻塞（Head of line blocking）会导致在达到最大请求数量时，剩余的资源需要等待其他资源请求完成后才能发起请求。</p><p>在 HTTP/2 中引入了多路复用的技术，这个技术可以只通过一个 TCP 连接就可以传输所有的请求数据。多路复用很好的解决了浏览器限制同一个域名下的请求数量的问题，同时也接更容易实现全速传输，毕竟新开一个 TCP 连接都需要慢慢提升传输速度。</p><p>大家可以通过 该链接 感受下 HTTP/2 比 HTTP/1 到底快了多少。</p><p>在 HTTP/1 中，因为队头阻塞的原因，你会发现发送请求是长这样的</p><p>在 HTTP/2 中，因为可以复用同一个 TCP 连接，你会发现发送请求是长这样的</p><h3 id="二进制传输"><a href="#二进制传输" class="headerlink" title="二进制传输"></a>二进制传输</h3><p>HTTP/2 中所有加强性能的核心点在于此。在之前的 HTTP 版本中，我们是通过文本的方式传输数据。在 HTTP/2 中引入了新的编码机制，所有传输的数据都会被分割，并采用二进制格式编码。</p><h3 id="多路复用"><a href="#多路复用" class="headerlink" title="多路复用"></a>多路复用</h3><p>在 HTTP/2 中，有两个非常重要的概念，分别是帧（frame）和流（stream）。</p><p>帧代表着最小的数据单位，每个帧会标识出该帧属于哪个流，流也就是多个帧组成的数据流。</p><p>多路复用，就是在一个 TCP 连接中可以存在多条流。换句话说，也就是可以发送多个请求，对端可以通过帧中的标识知道属于哪个请求。通过这个技术，可以避免 HTTP 旧版本中的队头阻塞问题，极大的提高传输性能。</p><h3 id="Header-压缩"><a href="#Header-压缩" class="headerlink" title="Header 压缩"></a>Header 压缩</h3><p>在 HTTP/1 中，我们使用文本的形式传输 header，在 header 携带 cookie 的情况下，可能每次都需要重复传输几百到几千的字节。</p><p>在 HTTP /2 中，使用了 HPACK 压缩格式对传输的 header 进行编码，减少了 header 的大小。并在两端维护了索引表，用于记录出现过的 header ，后面在传输过程中就可以传输已经记录过的 header 的键名，对端收到数据后就可以通过键名找到对应的值。</p><h3 id="服务端-Push"><a href="#服务端-Push" class="headerlink" title="服务端 Push"></a>服务端 Push</h3><p>在 HTTP/2 中，服务端可以在客户端某个请求后，主动推送其他资源。</p><p>可以想象以下情况，某些资源客户端是一定会请求的，这时就可以采取服务端 push 的技术，提前给客户端推送必要的资源，这样就可以相对减少一点延迟时间。当然在浏览器兼容的情况下你也可以使用 prefetch 。</p><h3 id="HTTP-3"><a href="#HTTP-3" class="headerlink" title="HTTP/3"></a>HTTP/3</h3><p>虽然 HTTP/2 解决了很多之前旧版本的问题，但是它还是存在一个巨大的问题，虽然这个问题并不是它本身造成的，而是底层支撑的 TCP 协议的问题。</p><p>因为 HTTP/2 使用了多路复用，一般来说同一域名下只需要使用一个 TCP 连接。当这个连接中出现了丢包的情况，那就会导致 HTTP/2 的表现情况反倒不如 HTTP/1 了。</p><p>因为在出现丢包的情况下，整个 TCP 都要开始等待重传，也就导致了后面的所有数据都被阻塞了。但是对于 HTTP/1 来说，可以开启多个 TCP 连接，出现这种情况反到只会影响其中一个连接，剩余的 TCP 连接还可以正常传输数据。</p><p>那么可能就会有人考虑到去修改 TCP 协议，其实这已经是一件不可能完成的任务了。因为 TCP 存在的时间实在太长，已经充斥在各种设备中，并且这个协议是由操作系统实现的，更新起来不大现实。</p><p>基于这个原因，Google 就更起炉灶搞了一个基于 UDP 协议的 QUIC 协议，并且使用在了 HTTP/3 上，当然 HTTP/3 之前名为 HTTP-over-QUIC，从这个名字中我们也可以发现，HTTP/3 最大的改造就是使用了 QUIC，接下来我们就来学习关于这个协议的内容。</p><h3 id="QUIC"><a href="#QUIC" class="headerlink" title="QUIC"></a>QUIC</h3><p>之前我们学习过 UDP 协议的内容，知道这个协议虽然效率很高，但是并不是那么的可靠。QUIC 虽然基于 UDP，但是在原本的基础上新增了很多功能，比如多路复用、0-RTT、使用 TLS1.3 加密、流量控制、有序交付、重传等等功能。这里我们就挑选几个重要的功能学习下这个协议的内容。</p><h3 id="多路复用-1"><a href="#多路复用-1" class="headerlink" title="多路复用"></a>多路复用</h3><p>虽然 HTTP/2 支持了多路复用，但是 TCP 协议终究是没有这个功能的。QUIC 原生就实现了这个功能，并且传输的单个数据流可以保证有序交付且不会影响其他的数据流，这样的技术就解决了之前 TCP 存在的问题。</p><p>并且 QUIC 在移动端的表现也会比 TCP 好。因为 TCP 是基于 IP 和端口去识别连接的，这种方式在多变的移动端网络环境下是很脆弱的。但是 QUIC 是通过 ID 的方式去识别一个连接，不管你网络环境如何变化，只要 ID 不变，就能迅速重连上。</p><h3 id="0-RTT"><a href="#0-RTT" class="headerlink" title="0-RTT"></a>0-RTT</h3><p>通过使用类似 TCP 快速打开的技术，缓存当前会话的上下文，在下次恢复会话的时候，只需要将之前的缓存传递给服务端验证通过就可以进行传输了。</p><h3 id="纠错机制"><a href="#纠错机制" class="headerlink" title="纠错机制"></a>纠错机制</h3><p>假如说这次我要发送三个包，那么协议会算出这三个包的异或值并单独发出一个校验包，也就是总共发出了四个包。</p><p>当出现其中的非校验包丢包的情况时，可以通过另外三个包计算出丢失的数据包的内容。</p><p>当然这种技术只能使用在丢失一个包的情况下，如果出现丢失多个包就不能使用纠错机制了，只能使用重传的方式了。</p><h3 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h3><p>总结一下内容：</p><p>HTTP/2 通过多路复用、二进制流、Header 压缩等等技术，极大地提高了性能，但是还是存在着问题的<br>QUIC 基于 UDP 实现，是 HTTP/3 中的底层支撑协议，该协议基于 UDP，又取了 TCP 中的精华，实现了即快又可靠的协议</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(17)UDP 与 TCP</title>
      <link href="2021/03/29/yck/(17)UDP%E5%92%8CTCP/"/>
      <url>2021/03/29/yck/(17)UDP%E5%92%8CTCP/</url>
      
        <content type="html"><![CDATA[<h1 id="25-UDP"><a href="#25-UDP" class="headerlink" title="25.UDP"></a>25.UDP</h1><p><strong>常考面试题：UDP 与 TCP 的区别是什么？</strong></p><p>首先 UDP 协议是面向无连接的，也就是说不需要在正式传递数据之前先连接起双方。然后 UDP 协议只是数据报文的搬运工，不保证有序且不丢失的传递到对端，并且UDP 协议也没有任何控制流量的算法，总的来说 UDP 相较于 TCP 更加的轻便。</p><h3 id="面向无连接"><a href="#面向无连接" class="headerlink" title="面向无连接"></a>面向无连接</h3><p>UDP 不需要和 TCP 一样在发送数据前进行三次握手建立连接的，想发数据就可以开始发送了</p><p>并且也只是数据报文的搬运工，不会对数据报文进行任何拆分和拼接操作。</p><p>具体来说就是：</p><ul><li>在发送端，应用层将数据传递给传输层的 UDP 协议，UDP 只会给数据增加一个 UDP 头标识下是 UDP 协议，然后就传递给网络层了</li><li>在接收端，网络层将数据传递给传输层，UDP 只去除 IP 报文头就传递给应用层，不会任何拼接操作<h3 id="不可靠性"><a href="#不可靠性" class="headerlink" title="不可靠性"></a>不可靠性</h3></li><li>无连接，通信不需要建立连接，想发就发肯定不可靠<br>收到什么数据就传递什么数据，并且不会备份数据，发送数据也不会关心对方是否接收到数据</li><li>网络环境时好时坏，UDP 没有拥塞控制，会以恒定速度发送数据。在网络条件不好的情况下会导致丢包，但是优点也很明显，在实时性要求高的场景（比如电话会议）就需要使用 UDP</li></ul><h3 id="高效"><a href="#高效" class="headerlink" title="高效"></a>高效</h3><p>因为UDP不可靠，所以也就没有 TCP 那么复杂，需要保证数据不丢失且有序到达。<br>因此 UDP 的头部开销小，只有八字节，相比 TCP 的至少二十字节要少得多，在传输数据报文时是很高效的。</p><p>UDP 头部包含了以下几个数据</p><ul><li>两个十六位的端口号，分别为源端口（可选字段）和目标端口</li><li>整个数据报文的长度</li><li>整个数据报文的检验和（IPv4 可选 字段），该字段用于发现头部信息和数据中的错误<h3 id="传输方式"><a href="#传输方式" class="headerlink" title="传输方式"></a>传输方式</h3>UDP 支持一对一传输，同样支持一对多，多对多，多对一的方式，也就是说 UDP 提供了单播，多播，广播的功能。</li></ul><h1 id="26-TCP"><a href="#26-TCP" class="headerlink" title="26.TCP"></a>26.TCP</h1><p><strong>常考面试题：UDP 与 TCP 的区别是什么？</strong></p><p>TCP 基本是和 UDP 反着来，建立连接断开连接都需要先需要进行握手。在传输数据的过程中，通过各种算法保证数据的可靠性，当然带来的问题就是相比 UDP 来说不那么的高效。</p><h3 id="头部"><a href="#头部" class="headerlink" title="头部"></a>头部</h3><p><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210113221312.png"><br>对于 TCP 头部来说，以下几个字段是很重要的</p><ul><li><p>Sequence number，保证了 TCP 传输的报文都是有序的，对端可以通过序号顺序的拼接报文</p></li><li><p>Acknowledgement Number，表示接收端期望接收的下一个字节的编号是多少，同时表示上一个序号已经收到</p></li><li><p>Window Size，窗口大小，表示还能接收多少字节的数据，用于流量控制</p></li><li><p>标识符</p><ul><li>URG=1：<strong>数据包含紧急信息，是高优先级数据报文</strong>，此时紧急指针有效。紧急数据一定位于当前数据包数据部分的最前面，紧急指针标明了紧急数据的尾部。</li><li>ACK=1：<strong>确认号字段有效</strong>。此外，TCP 还规定在连接建立后传送的所有报文段都必须把 ACK 置为一</li><li>PSH=1：<strong>接收端应该立即将数据 push 给应用层</strong>，而不是等到缓冲区满后再提交。</li><li>RST=1：<strong>TCP 连接出现严重问题</strong>，可能需要重新建立 TCP 连接，也可以用于拒绝非法的报文段和拒绝连接请求。</li><li>SYN=1：当SYN=1，ACK=0时，表示当前报文段是<strong>连接请求报文</strong>。当SYN=1，ACK=1时，表示当前报文段是<strong>同意建立连接的应答报文</strong>。</li><li>FIN=1：表示此报文段是释放连接的请求报文。</li></ul></li></ul><h3 id="状态机"><a href="#状态机" class="headerlink" title="状态机"></a>状态机</h3><p>TCP 的状态机是很复杂的，并且与建立断开连接时的握手息息相关，接下来就来详细描述下两种握手。</p><p>在这之前需要了解一个重要的性能指标 RTT。该指标表示发送端发送数据到接收到对端数据所需的往返时间。</p><h4 id="建立连接三次握手"><a href="#建立连接三次握手" class="headerlink" title="建立连接三次握手"></a>建立连接三次握手</h4><p>起初，两端都为 CLOSED 状态。在通信开始前，双方都会创建 TCB。 服务器创建完 TCB 后便进入 LISTEN 状态，此时开始等待客户端发送数据。</p><p>第一次握手：客户端发送连接请求，包含自身的数据通讯初始序号。客户端进入 SYN-SENT </p><p>第二次握手：服务端发送应答，包含自身初始序号，进入 SYN-RECEIVED</p><p>第三次握手：客户端发送确认报文，进入 ESTABLISHED ，服务端收到应答后也进入 ESTABLISHED 状态，连接建立成功。</p><p>PS：第三次握手中可以包含数据，通过快速打开（TFO）技术就可以实现这一功能。其实只要涉及到握手的协议，都可以使用类似 TFO 的方式，客户端和服务端存储相同的 cookie，下次握手时发出 cookie 达到减少 RTT 的目的。</p><p><strong>常考面试题：为什么 TCP 建立连接需要三次握手，明明两次就可以建立起连接</strong></p><p>为了防止失效的连接请求报文段被服务端接收的情况</p><p>客户端发送了连接请求 A，但是因为网络原因造成了超时，这时 TCP 启动超时重传的机制发送一个连接请求 B。此时请求顺利到达服务端，服务端应答完就建立了请求，然后接收数据后释放了连接。</p><p>假设连接请求 A 在两端关闭后抵达了服务端，此时服务端会认为客户端又需要建立 TCP 连接，从而应答了该请求并进入 ESTABLISHED 状态。但是客户端其实是 CLOSED 状态，就会导致服务端一直等待，造成资源的浪费</p><p>PS：在建立连接中，任意一端掉线，TCP 都会重发 SYN 包，一般会重试五次，在建立连接中可能会遇到 SYN Flood 攻击。遇到这种情况你可以选择调低重试次数或者干脆在不能处理的情况下拒绝请求。</p><h4 id="断开链接四次握手"><a href="#断开链接四次握手" class="headerlink" title="断开链接四次握手"></a>断开链接四次握手</h4><p>TCP 是全双工的，在断开连接时两端都需要发送 FIN 和 ACK</p><p>第一次握手：客户端 A 认为数据发送完成，向服务端 B 发送连接释放请求</p><p>第二次握手：B 收到请求后，会告诉应用层要释放 TCP 链接。然后发送 ACK 包，并进入 CLOSE_WAIT ，此时 A 到 B 的连接释放，B 不再接收 A 的数据。但B 仍旧可以发送数据给 A</p><p>第三次握手：B 发送完毕后会向 A 发送连接释放请求，然后 B 便进入 LAST-ACK </p><p>PS：通过延迟确认的技术（通常有时间限制，否则对方会误认为需要重传），可以将第二次和第三次握手合并，延迟 ACK 包的发送。</p><p>第四次握手<br>A 收到释放请求后，向 B 发送确认应答，A 进入 TIME-WAIT，该状态会持续 2MSL（最大段生存期） 时间，若该时间段内没有 B 的重发请求，就进入 CLOSED 。当 B 收到确认应答后，也进入 CLOSED</p><p><strong>为什么 A 要进入 TIME-WAIT ，等待 2MSL 时间后才进入 CLOSED ？</strong></p><p><strong>为了保证 B 能收到 A 的确认应答</strong>。若 A 发完确认应答后直接进入 CLOSED ，如果确认应答因为网络问题一直没有到达，那么会造成 B 不能正常关闭。</p><h3 id="ARQ-协议"><a href="#ARQ-协议" class="headerlink" title="ARQ 协议"></a>ARQ 协议</h3><p>ARQ 协议也就是超时重传机制。通过确认和超时机制保证了数据的正确送达，ARQ 协议包含停止等待 ARQ 和连续 ARQ 两种协议。</p><h4 id="停止等待-ARQ"><a href="#停止等待-ARQ" class="headerlink" title="停止等待 ARQ"></a>停止等待 ARQ</h4><p>正常传输过程</p><p>只要 A 向 B 发送一段报文，都要停止发送并启动一个定时器，等待对端回应，在定时器时间内接收到对端应答就取消定时器并发送下一段报文。</p><p>报文丢失或出错</p><p>在报文传输的过程中可能会出现丢包。这时候超过定时器设定的时间就会再次发送丢失的数据直到对端响应，所以需要每次都备份发送的数据。</p><p>即使报文正常的传输到对端，也可能出现在传输过程中报文出错的问题。这时候对端会抛弃该报文并等待 A 端重传。</p><p>PS：一般定时器设定的时间都会大于一个 RTT 的平均时间。</p><p>ACK 超时或丢失</p><p>对端传输的应答也可能出现丢失或超时的情况。那么超过定时器时间 A 端照样会重传报文。这时候 B 端收到相同序号的报文会丢弃该报文并重传应答，直到 A 端发送下一个序号的报文。</p><p>在超时的情况下也可能出现应答很迟到达，这时 A 端会判断该序号是否已经接收过，如果接收过只需要丢弃应答即可。</p><p>从上面的描述中大家肯定可以发现这肯定不是一个高效的方式。假设在良好的网络环境中，每次发送数据都需要等待片刻肯定是不能接受的。那么既然我们不能接受这个不那么高效的协议，就来继续学习相对高效的协议吧。</p><h4 id="连续-ARQ"><a href="#连续-ARQ" class="headerlink" title="连续 ARQ"></a>连续 ARQ</h4><p>在连续 ARQ 中，发送端拥有一个发送窗口，可以在没有收到应答的情况下持续发送窗口内的数据，这样相比停止等待 ARQ 协议来说减少了等待时间，提高了效率。</p><p>累计确认<br>连续 ARQ 中，接收端会持续不断收到报文。如果和停止等待 ARQ 中接收一个报文就发送一个应答一样，就太浪费资源了。通过累计确认，可以在收到多个报文以后统一回复一个应答报文。报文中的 ACK 标志位可以用来告诉发送端这个序号之前的数据已经全部接收到了，下次请发送这个序号后的数据。</p><p>但是累计确认也有一个弊端。在连续接收报文时，可能会遇到接收到序号 5 的报文后，并未接收到序号 6 的报文，然而序号 7 以后的报文已经接收。遇到这种情况时，ACK 只能回复 6，这样就会造成发送端重复发送数据的情况。</p><h3 id="滑动窗口"><a href="#滑动窗口" class="headerlink" title="滑动窗口"></a>滑动窗口</h3><p>在上面小节中讲到了发送窗口。在 TCP 中，两端其实都维护着窗口：分别为发送端窗口和接收端窗口。</p><p>发送端窗口包含已发送但未收到应答的数据和可以发送但是未发送的数据。</p><p>发送端窗口是由接收窗口剩余大小决定的。接收方会把当前接收窗口的剩余大小写入应答报文，发送端收到应答后根据该值和当前网络拥塞情况设置发送窗口的大小，所以发送窗口的大小是不断变化的。</p><p>当发送端接收到应答报文后，会随之将窗口进行滑动</p><p>滑动窗口是一个很重要的概念，它帮助 TCP 实现了流量控制的功能。接收方通过报文告知发送方还可以发送多少数据，从而保证接收方能够来得及接收数据，防止出现接收方带宽已满，但是发送方还一直发送数据的情况。</p><h4 id="Zero-窗口"><a href="#Zero-窗口" class="headerlink" title="Zero 窗口"></a>Zero 窗口</h4><p>在发送报文的过程中，可能会遇到对端出现零窗口的情况。在该情况下，发送端会停止发送数据，并启动 persistent timer 。该定时器会定时发送请求给对端，让对端告知窗口大小。在重试次数超过一定次数后，可能会中断 TCP 链接。</p><h3 id="拥塞处理"><a href="#拥塞处理" class="headerlink" title="拥塞处理"></a>拥塞处理</h3><p>拥塞处理和流量控制不同，后者是作用于接收方，保证接收方来得及接受数据。而前者是作用于网络，防止过多的数据拥塞网络，避免出现网络负载过大的情况。</p><p>拥塞处理包括了四个算法，分别为：慢开始，拥塞避免，快速重传，快速恢复。</p><p>慢开始算法<br>慢开始算法，顾名思义，就是在传输开始时将发送窗口慢慢指数级扩大，从而避免一开始就传输大量数据导致网络拥塞。想必大家都下载过资源，每当我们开始下载的时候都会发现下载速度是慢慢提升的，而不是一蹴而就直接拉满带宽。</p><p>慢开始算法步骤具体如下</p><p>连接初始设置拥塞窗口（Congestion Window） 为 1 MSS（一个分段的最大数据量）<br>每过一个 RTT 就将窗口大小乘二<br>指数级增长肯定不能没有限制的，所以有一个阈值限制，当窗口大小大于阈值时就会启动拥塞避免算法。<br>拥塞避免算法<br>拥塞避免算法相比简单点，每过一个 RTT 窗口大小只加一，这样能够避免指数级增长导致网络拥塞，慢慢将大小调整到最佳值。</p><p>在传输过程中可能定时器超时的情况，这时候 TCP 会认为网络拥塞了，会马上进行以下步骤：</p><p>将阈值设为当前拥塞窗口的一半<br>将拥塞窗口设为 1 MSS<br>启动拥塞避免算法<br>快速重传<br>快速重传一般和快恢复一起出现。一旦接收端收到的报文出现失序的情况，接收端只会回复最后一个顺序正确的报文序号。如果发送端收到三个重复的 ACK，无需等待定时器超时而是直接启动快速重传算法。具体算法分为两种：</p><p>TCP Taho 实现如下</p><p>将阈值设为当前拥塞窗口的一半<br>将拥塞窗口设为 1 MSS<br>重新开始慢开始算法<br>TCP Reno 实现如下</p><p>拥塞窗口减半<br>将阈值设为当前拥塞窗口<br>进入快恢复阶段（重发对端需要的包，一旦收到一个新的 ACK 答复就退出该阶段），这种方式在丢失多个包的情况下就不那么好了<br>使用拥塞避免算法<br>TCP New Ren 改进后的快恢复<br>TCP New Reno 算法改进了之前 TCP Reno 算法的缺陷。在之前，快恢复中只要收到一个新的 ACK 包，就会退出快恢复。</p><p>在 TCP New Reno 中，TCP 发送方先记下三个重复 ACK 的分段的最大序号。</p><p>假如我有一个分段数据是 1 ~ 10 这十个序号的报文，其中丢失了序号为 3 和 7 的报文，那么该分段的最大序号就是 10。发送端只会收到 ACK 序号为 3 的应答。这时候重发序号为 3 的报文，接收方顺利接收的话就会发送 ACK 序号为 7 的应答。这时候 TCP 知道对端是有多个包未收到，会继续发送序号为 7 的报文，接收方顺利接收并会发送 ACK 序号为 11 的应答，这时发送端认为这个分段接收端已经顺利接收，接下来会退出快恢复阶段。</p><h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>这一章节内容很多，充斥了大量的术语，适合大家反复研读，已经把 TCP 中最核心最需要掌握的内容全盘托出了，如有哪里不明白的欢迎提问。</p><p>总结一下这一章节的内容：</p><p>建立连接需要三次握手，断开连接需要四次握手<br>滑动窗口解决了数据的丢包、顺序不对和流量控制问题<br>拥塞窗口实现了对流量的控制，保证在全天候环境下最优的传递数据</p><p>传输层传输协议分为UDP和TCP两种</p><p>　　UDP是无连接的协议，而TCP是可靠的有连接的协议，主要表现在：接收方会对收到的数据进行确认、发送方会重传接收方未确认的数据、接收方会将接收到数据按正确的顺序重新排序，并删除重复的数据、提供了控制拥挤的机制</p><p>　　由于HTTP协议使用的是TCP协议，为了方便通信，将HTTP请求报文按序号分为多个报文段(segment)，并对每个报文段进行封装。使用本地一个大于1024以上的随机TCP源端口(这里假设是1030)建立到目的服务器TCP80号端口(HTTPS协议对应的端口号是443)的连接，TCP源端口和目的端口被加入到报文段中，学名叫协议数据单元(Protocol Data Unit, PDU)。因TCP是一个可靠的传输控制协议，传输层还会加入序列号、确认号、窗口大小、校验和等参数，共添加20字节的头部信息</p><p>http<br>　　TCP协议是面向连接的，所以它在开始传输数据之前需要先建立连接。要建立或初始化一个连接，两端主机必须同步双方的初始序号。同步是通过交换连接建立数据分段和初始序号来完成的，在连接建立数据分段中包含一个SYN(同步)的控制位。同步需要双方都发送自己的初始序号，并且发送确认的ACK。此过程就是三次握手<br><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20201228124654.png"><br>　　第一次握手：主机A发往主机B，主机A的初始序号是X，设置SYN位，未设置ACK位</p><p>　　第二次握手：主机B发往主机A，主机B的初始序号是Y，确认号(ACK)是X+1，X+1确认号暗示己经收到主机A发往主机B的同步序号。设置SYN位和ACK位</p><p>　　第三次握手：主机A发往主机B，主机A的序号是X+1，确认号是Y+1，Y+1确认号暗示已经收到主机B发往主机A的同步序号。设置ACK位，未设置SYN位</p><p>　　三次握手解决的不仅仅有序号问题，还解决了包括窗口大小、MTU(Maximum Transmission Unit,最大传输单元)，以及所期望的网络延时等其他问题</p><p>http<br>　　构建TCP请求会增加大量的网络时延，常用的优化方式如下所示</p><p>　　（1）资源打包，合并请求</p><p>　　（2）多使用缓存，减少网络传输</p><p>　　（3）使用keep-alive建立持久连接</p><p>　　（4）使用多个域名，增加浏览器的资源并发加载数，或者使用HTTP2的管道化连接的多路复用技术</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(16)监控</title>
      <link href="2021/03/29/yck/(16)%E7%9B%91%E6%8E%A7/"/>
      <url>2021/03/29/yck/(16)%E7%9B%91%E6%8E%A7/</url>
      
        <content type="html"><![CDATA[<h1 id="24"><a href="#24" class="headerlink" title="24."></a>24.</h1><p>前端监控一般分为三种，分别为页面埋点、性能监控以及异常监控</p><p>这一章节我们将来学习这些监控相关的内容，但是基本不会涉及到代码，只是让大家了解下前端监控该用什么方式实现。毕竟大部分公司都只是使用到了第三方的监控工具，而不是选择自己造轮子</p><h3 id="页面埋点"><a href="#页面埋点" class="headerlink" title="页面埋点"></a>页面埋点</h3><p>页面埋点应该是大家最常写的监控了，一般起码会监控以下几个数据：</p><ul><li>PV / UV</li><li>停留时长</li><li>流量来源</li><li>用户交互</li></ul><p>一般的实现思路大致可以分为两种，分别为手写埋点和无埋点的方式</p><p>相信第一种方式也是大家最常用的方式，可以自主选择需要监控的数据然后在相应的地方写入代码。这种方式的灵活性很大，但是唯一的缺点就是工作量较大，每个需要监控的地方都得插入代码。</p><p>另一种无埋点的方式基本不需要开发者手写埋点了，而是统计所有的事件并且定时上报。这种方式虽然没有前一种方式繁琐了，但是因为统计的是所有事件，所以还需要后期过滤出需要的数据。</p><h3 id="性能监控"><a href="#性能监控" class="headerlink" title="性能监控"></a>性能监控</h3><p>性能监控可以帮助开发者了解在真实环境下页面的性能情况是如何的，可以直接使用浏览器自带的 Performance API 来实现这个功能。只需要调用 <code>performance.getEntriesByType(&#39;navigation&#39;) </code>这行代码就行了<br><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210114124738.png"></p><p>我们可以发现这行代码返回了一个数组，内部包含了相当多的信息，从数据开始在网络中传输到页面加载完成都提供了相应的数据。</p><h3 id="异常监控"><a href="#异常监控" class="headerlink" title="异常监控"></a>异常监控</h3><p>对于异常监控来说，以下两种监控是必不可少的，分别是代码报错以及接口异常上报</p><p>对于代码运行错误，通常的办法是使用 window.onerror 拦截报错。该方法能拦截到大部分的详细报错信息，但是也有例外</p><ul><li>对于跨域的代码运行错误会显示 Script error. 对于这种情况我们需要给 script 标签添加 crossorigin 属性</li><li>某些浏览器可能不显示调用栈信息，可以通过 arguments.callee.caller 来做栈递归</li></ul><p>对于异步代码来说，可以使用 catch 的方式捕获错误。比如 Promise 可以直接使用 catch 函数，async await 可以使用 try catch</p><p>但是要注意线上运行的代码都是压缩过的，需要在打包时生成 sourceMap 文件便于 debug</p><p>对于捕获的错误需要上传给服务器，通常可以通过 img 标签的 src 发起一个请求</p><p>另外接口异常就相对来说简单了，可以列举出出错的状态码。一旦出现此类的状态码就可以立即上报出错。接口异常上报可以让开发人员迅速知道有哪些接口出现了大面积的报错，以便迅速修复问题。</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(15)React 和 Vue</title>
      <link href="2021/03/29/yck/(15)React%E5%92%8CVue/"/>
      <url>2021/03/29/yck/(15)React%E5%92%8CVue/</url>
      
        <content type="html"><![CDATA[<h1 id="19-React-和-Vue-两大框架之间的相爱相杀"><a href="#19-React-和-Vue-两大框架之间的相爱相杀" class="headerlink" title="19.React 和 Vue 两大框架之间的相爱相杀"></a>19.React 和 Vue 两大框架之间的相爱相杀</h1><ul><li>MVVM 是什么</li><li>Virtual DOM 是什么</li><li>前端路由是如何跳转的</li><li>React 和 Vue 之间的区别</li></ul><h3 id="MVVM"><a href="#MVVM" class="headerlink" title="MVVM"></a>MVVM</h3><p>涉及面试题：什么是 MVVM？比之 MVC 有什么区别？</p><p>View 很简单，就是用户看到的视图</p><p>Model 很简单，一般是本地数据和数据库中的数据</p><h4 id="MVC"><a href="#MVC" class="headerlink" title="MVC"></a>MVC</h4><p>传统的 MVC 架构通常是使用控制器更新模型，视图从模型中获取数据去渲染。当用户有输入时，会通过控制器去更新模型，并且通知视图进行更新。</p><p><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210114114206.png"></p><p>MVC 有一个巨大的缺陷就是控制器承担的责任太大了，随着项目愈加复杂，控制器中的代码会越来越臃肿，导致出现不利于维护的情况。</p><h4 id="MVVM-1"><a href="#MVVM-1" class="headerlink" title="MVVM"></a>MVVM</h4><p>在 MVVM 架构中，引入了 ViewModel 的概念。ViewModel 只关心数据和业务的处理，不关心 View 如何处理数据，在这种情况下，View 和 Model 都可以独立出来，任何一方改变了也不一定需要改变另一方，并且可以将一些可复用的逻辑放在一个 ViewModel 中，让多个 View 复用这个 ViewModel。<br><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210114114324.png"></p><p>以 Vue 框架来举例，ViewModel 就是组件的实例。View 就是模板，Model 的话在引入 Vuex 的情况下是完全可以和组件分离的。</p><p>除了以上三个部分，其实在 MVVM 中还引入了一个隐式的 Binder 层，实现了 View 和 ViewModel 的绑定。隐式的 Binder 层就是 Vue 通过解析模板中的插值和指令从而实现 View 与 ViewModel 的绑定。</p><p>对于 MVVM 来说，最重要的不是通过双向绑定或者其他的方式将 View 与 ViewModel 绑定起来，而是通过 ViewModel 将视图中的状态和用户的行为分离出一个抽象，这才是 MVVM 的精髓。</p><h3 id="Virtual-DOM"><a href="#Virtual-DOM" class="headerlink" title="Virtual DOM"></a>Virtual DOM</h3><p>涉及面试题：什么是 Virtual DOM？为什么 Virtual DOM 比原生 DOM 快？</p><p>操作 DOM 是很慢的，为什么慢的原因已经在「浏览器渲染原理」章节中说过</p><p>那么相较于 DOM 来说，操作 JS 对象会快很多，并且我们也可以通过 JS 来模拟 DOM</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> ul = &#123;</span><br><span class="line">  tag: <span class="string">&#x27;ul&#x27;</span>,</span><br><span class="line">  props: &#123;</span><br><span class="line">    class: &#x27;list&#x27;</span><br><span class="line">  &#125;,</span><br><span class="line">  children: &#123;</span><br><span class="line">    tag: <span class="string">&#x27;li&#x27;</span>,</span><br><span class="line">    children: <span class="string">&#x27;1&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 上述代码对应的 DOM 就是</span></span><br><span class="line">&lt;ul <span class="class"><span class="keyword">class</span></span>=<span class="string">&#x27;list&#x27;</span>&gt;</span><br><span class="line">  &lt;li&gt;<span class="number">1</span>&lt;/li&gt;</span><br><span class="line">&lt;/ul&gt;</span><br></pre></td></tr></table></figure><p>那么既然 DOM 可以通过 JS 对象来模拟，反之也可以通过 JS 对象来渲染出对应的 DOM<br>难点在于如何判断新旧两个 JS 对象的最小差异并且实现局部更新 DOM</p><p>首先 DOM 是一个多叉树的结构，如果需要完整的对比两颗树的差异，时间复杂度会是 O(n ^ 3)React 团队优化了算法，实现了 O(n) 的复杂度，关键就是只对比同层的节点，而不是跨层对比，这也是考虑到在实际业务中很少会去跨层的移动 DOM 元素。</p><p>所以判断差异的算法就分为了两步</p><ul><li>首先从上至下，从左往右遍历对象，也就是树的深度遍历，这一步中会给每个节点添加索引，便于最后渲染差异</li><li>一旦节点有子元素，就去判断子元素是否有不同<br>在第一步算法中我们需要判断新旧节点的 tagName 是否相同，如果不相同的话就代表节点被替换了。如果没有更改 tagName 的话，就需要判断是否有子元素，有的话就进行第二步算法。</li></ul><p>在第二步算法中，需要判断原本的列表中是否有节点被移除，在新的列表中需要判断是否有新的节点加入，还需要判断节点是否有移动。</p><p>举个例子来说，假设页面中只有一个列表，我们对列表中的元素进行了变更</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 假设这里模拟一个 ul，其中包含了 5 个 li</span></span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]</span><br><span class="line"><span class="comment">// 这里替换上面的 li</span></span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">5</span>, <span class="number">4</span>]</span><br></pre></td></tr></table></figure><p>先前的 ul 中的第三个 li 被移除了，四五替换了位置。<br>在实际的算法中如何去识别改动的是哪个节点呢？这就引入了 key 这个属性，key用来给每一个节点打标志的，用于判断是否是同一个节点。</p><p>当然在判断以上差异的过程中，我们还需要判断节点的属性是否有变化等等。</p><p>当判断出以上的差异后，可以把差异记录下来。当对比完两棵树以后，就可以通过差异去局部更新 DOM，实现性能的最优化。</p><p>当然了 Virtual DOM 提高性能是其中一个优势，其实最大的优势还是在于：</p><p>将 Virtual DOM 作为一个兼容层，让我们还能对接非 Web 端的系统，实现跨端开发。<br>同样的，通过 Virtual DOM 我们可以渲染到其他的平台，比如实现 SSR、同构渲染等等。<br>实现组件的高度抽象化</p><h3 id="路由原理"><a href="#路由原理" class="headerlink" title="路由原理"></a>路由原理</h3><p>涉及面试题：前端路由原理？两种实现方式有什么区别？</p><p>前端路由实现起来其实很简单，本质就是监听 URL 的变化，然后匹配路由规则，显示相应的页面，并且无须刷新页面。目前前端使用的路由就只有两种实现方式</p><h4 id="Hash-模式"><a href="#Hash-模式" class="headerlink" title="Hash 模式"></a>Hash 模式</h4><p><code>www.test.com/#/</code> 就是 Hash URL，当 # 后面的哈希值发生变化时，可以通过 hashchange 事件来监听到 URL 的变化，从而进行跳转页面，并且无论哈希值如何变化，服务端接收到的 URL 请求永远是 <code>www.test.com</code></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">window</span>.addEventListener(<span class="string">&#x27;hashchange&#x27;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// ... 具体逻辑</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>Hash 模式相对来说更简单，并且兼容性也更好。</p><h4 id="History-模式"><a href="#History-模式" class="headerlink" title="History 模式"></a>History 模式</h4><p>History 模式是 HTML5 新推出的功能，主要使用 history.pushState 和 history.replaceState 改变 URL</p><p>通过 History 模式改变 URL 同样不会引起页面的刷新，只会更新浏览器的历史记录。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 新增历史记录</span></span><br><span class="line">history.pushState(stateObject, title, URL)</span><br><span class="line"><span class="comment">// 替换当前历史记录</span></span><br><span class="line">history.replaceState(stateObject, title, URL)</span><br></pre></td></tr></table></figure><p>当用户做出浏览器动作时，比如点击后退按钮时会触发 popState 事件</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">window</span>.addEventListener(<span class="string">&#x27;popstate&#x27;</span>, <span class="function"><span class="params">e</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// e.state 就是 pushState(stateObject) 中的 stateObject</span></span><br><span class="line">  <span class="built_in">console</span>.log(e.state)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><h4 id="两种模式对比"><a href="#两种模式对比" class="headerlink" title="两种模式对比"></a>两种模式对比</h4><ul><li>Hash 模式只可以更改 # 后面的内容，History 模式可以通过 API 设置任意的同源 URL</li><li>History 模式可以通过 API 添加任意类型的数据到历史记录中，Hash 模式只能更改哈希值，也就是字符串</li><li>Hash 模式无需后端配置，并且兼容性好。History 模式在用户手动输入地址或者刷新页面的时候会发起 URL 请求，后端需要配置 index.html 页面用于匹配不到静态资源的时候<h3 id="Vue-和-React-之间的区别"><a href="#Vue-和-React-之间的区别" class="headerlink" title="Vue 和 React 之间的区别"></a>Vue 和 React 之间的区别</h3>Vue 的表单可以使用 v-model 支持双向绑定，相比于 React 来说开发上更加方便，当然了 v-model 其实就是个语法糖，本质上和 React 写表单的方式没什么区别。</li></ul><p>改变数据方式不同，Vue 修改状态相比来说要简单许多，React 需要使用 setState 来改变状态，并且使用这个 API 也有一些坑点。并且 Vue 的底层使用了依赖追踪，页面更新渲染已经是最优的了，但是 React 还是需要用户手动去优化这方面的问题。</p><p>React 16以后，有些钩子函数会执行多次，这是因为引入 Fiber 的原因，这在后续的章节中会讲到。</p><p>React 需要使用 JSX，有一定的上手成本，并且需要一整套的工具链支持，但是完全可以通过 JS 来控制页面，更加的灵活。Vue 使用了模板语法，相比于 JSX 来说没有那么灵活，但是完全可以脱离工具链，通过直接编写 render 函数就能在浏览器中运行。</p><p>在生态上来说，两者其实没多大的差距，当然 React 的用户是远远高于 Vue 的。</p><p>小结</p><p>说到底，就算你证明了 A 比 B 牛逼，也不意味着你或者你的项目就牛逼了… 比起争这个，不如多想想怎么让自己变得更牛逼吧。</p><h1 id="22-React-常考基础知识点"><a href="#22-React-常考基础知识点" class="headerlink" title="22.React 常考基础知识点"></a>22.React 常考基础知识点</h1><h3 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期"></a>生命周期</h3><p>在 V16 版本中引入了 Fiber 机制。这个机制一定程度上的影响了部分生命周期的调用，并且也引入了新的 2 个 API 来解决问题，关于 Fiber 的内容将会在下一章节中讲到。</p><p>在之前的版本中，复杂的复合组件改动了最上层组件的 state，那么调用栈可能会很长</p><p>调用栈过长，再加上中间进行了复杂的操作，就可能导致长时间阻塞主线程，带来不好的用户体验。Fiber 就是为了解决该问题而生。</p><p>Fiber 本质上是一个虚拟的堆栈帧，新的调度器会按照优先级自由调度这些帧，从而将之前的同步渲染改成了异步渲染，在不影响体验的情况下去分段计算更新。</p><p>对于如何区别优先级，React 有自己的一套逻辑。对于动画这种实时性很高的东西，也就是 16 ms 必须渲染一次保证不卡顿的情况下，React 会每 16 ms（以内） 暂停一下更新，返回来继续渲染动画。</p><p>对于异步渲染，现在渲染有两个阶段：reconciliation 和 commit 。前者过程是可以打断的，后者不能暂停，会一直更新界面直到完成。</p><h4 id="Reconciliation-阶段"><a href="#Reconciliation-阶段" class="headerlink" title="Reconciliation 阶段"></a>Reconciliation 阶段</h4><ul><li>componentWillMount</li><li>componentWillReceiveProps</li><li>shouldComponentUpdate</li><li>componentWillUpdate</li></ul><h4 id="Commit-阶段"><a href="#Commit-阶段" class="headerlink" title="Commit 阶段"></a>Commit 阶段</h4><ul><li>componentDidMount</li><li>componentDidUpdate</li><li>componentWillUnmount</li></ul><p>因为 Reconciliation 可以被打断，所以 Reconciliation 阶段会执行的生命周期函数就可能会出现调用多次的情况，从而引起 Bug。由此对于 Reconciliation 阶段调用的几个函数，除了 shouldComponentUpdate 以外，其他都应该避免去使用，并且 V16 中也引入了新的 API 来解决这个问题。</p><p>getDerivedStateFromProps 用于替换 componentWillReceiveProps ，该函数会在初始化和 update 时被调用</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ExampleComponent</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  <span class="comment">// Initialize state in constructor,</span></span><br><span class="line">  <span class="comment">// Or with a property initializer.</span></span><br><span class="line">  state = &#123;&#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">static</span> <span class="function"><span class="title">getDerivedStateFromProps</span>(<span class="params">nextProps, prevState</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (prevState.someMirroredValue !== nextProps.someValue) &#123;</span><br><span class="line">      <span class="keyword">return</span> &#123;</span><br><span class="line">        derivedData: computeDerivedState(nextProps),</span><br><span class="line">        someMirroredValue: nextProps.someValue</span><br><span class="line">      &#125;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Return null to indicate no change to state.</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>getSnapshotBeforeUpdate 用于替换 componentWillUpdate ，该函数会在 update 后 DOM 更新前被调用，用于读取最新的 DOM 数据。</p><h3 id="setState"><a href="#setState" class="headerlink" title="setState"></a>setState</h3><p>setState 在 React 中是经常使用的一个 API，但是它存在一些的问题经常会导致初学者出错，核心原因就是因为这个 API 是异步的</p><p>首先 setState 的调用并不会马上引起 state 的改变，并且如果你一次调用了多个 setState ，那么结果可能并不如你期待的一样。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="title">handle</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">  <span class="comment">// 初始化 `count` 为 0</span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.state.count) <span class="comment">// -&gt; 0</span></span><br><span class="line">  <span class="built_in">this</span>.setState(&#123; <span class="attr">count</span>: <span class="built_in">this</span>.state.count + <span class="number">1</span> &#125;)</span><br><span class="line">  <span class="built_in">this</span>.setState(&#123; <span class="attr">count</span>: <span class="built_in">this</span>.state.count + <span class="number">1</span> &#125;)</span><br><span class="line">  <span class="built_in">this</span>.setState(&#123; <span class="attr">count</span>: <span class="built_in">this</span>.state.count + <span class="number">1</span> &#125;)</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.state.count) <span class="comment">// -&gt; 0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>第一，两次的打印都为 0，因为 setState 是个异步 API，只有同步代码运行完毕才会执行。setState 异步的原因我认为在于，setState 可能会导致 DOM 的重绘，如果调用一次就马上去进行重绘，那么调用多次就会造成不必要的性能损失。设计成异步的话，就可以将多次调用放入一个队列中，在恰当的时候统一进行更新过程。</p><p>第二，虽然调用了三次 setState ，但是 count 的值还是为 1。因为多次调用会合并为一次，只有当更新结束后 state 才会改变，三次调用等同于如下代码</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Object</span>.assign(  </span><br><span class="line">  &#123;&#125;,</span><br><span class="line">  &#123; <span class="attr">count</span>: <span class="built_in">this</span>.state.count + <span class="number">1</span> &#125;,</span><br><span class="line">  &#123; <span class="attr">count</span>: <span class="built_in">this</span>.state.count + <span class="number">1</span> &#125;,</span><br><span class="line">  &#123; <span class="attr">count</span>: <span class="built_in">this</span>.state.count + <span class="number">1</span> &#125;,</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>当然你也可以通过以下方式来实现调用三次 setState 使得 count 为 3</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="title">handle</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">  <span class="built_in">this</span>.setState(<span class="function">(<span class="params">prevState</span>) =&gt;</span> (&#123; <span class="attr">count</span>: prevState.count + <span class="number">1</span> &#125;))</span><br><span class="line">  <span class="built_in">this</span>.setState(<span class="function">(<span class="params">prevState</span>) =&gt;</span> (&#123; <span class="attr">count</span>: prevState.count + <span class="number">1</span> &#125;))</span><br><span class="line">  <span class="built_in">this</span>.setState(<span class="function">(<span class="params">prevState</span>) =&gt;</span> (&#123; <span class="attr">count</span>: prevState.count + <span class="number">1</span> &#125;))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果你想在每次调用 setState 后获得正确的 state ，可以通过如下代码实现</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="title">handle</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.setState(<span class="function">(<span class="params">prevState</span>) =&gt;</span> (&#123; <span class="attr">count</span>: prevState.count + <span class="number">1</span> &#125;), <span class="function">() =&gt;</span> &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="built_in">this</span>.state)</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="性能优化"><a href="#性能优化" class="headerlink" title="性能优化"></a>性能优化</h3><p>这小节内容集中在组件的性能优化上，这一方面的性能优化也集中在shouldComponentUpdate 这个钩子函数上做文章。</p><p>PS：下文中的 state 指代了 state 及 props</p><p>在 <strong>shouldComponentUpdate</strong> 函数中我们可以通过返回布尔值来决定当前组件是否需要更新。这层代码逻辑可以是简单地浅比较一下当前 state 和之前的 state 是否相同，也可以是判断某个值更新了才触发组件更新。一般来说不推荐完整地对比当前 state 和之前的 state 是否相同，因为组件更新触发可能会很频繁，这样的完整对比性能开销会有点大，可能会造成得不偿失的情况。</p><p>当然如果真的想完整对比当前 state 和之前的 state 是否相同，并且不影响性能也是行得通的，可以通过 immutable 或者 immer 这些库来生成不可变对象。这类库对于操作大规模的数据来说会提升不错的性能，并且一旦改变数据就会生成一个新的对象，对比前后 state 是否一致也就方便多了，同时也很推荐阅读下 immer 的源码实现。</p><p>另外如果只是单纯的浅比较一下，可以直接使用 PureComponent，底层就是实现了浅比较 state。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">PureComponent</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="title">render</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      &lt;div&gt;</span><br><span class="line">        PureComponent</span><br><span class="line">      &lt;/div&gt;</span><br><span class="line">    )</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>这时候你可能会考虑到函数组件就不能使用这种方式了，如果你使用 16.6.0 之后的版本的话，可以使用 React.memo 来实现相同的功能。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> Test = React.memo(<span class="function">() =&gt;</span> (</span><br><span class="line">    &lt;div&gt;</span><br><span class="line">        PureComponent</span><br><span class="line">    &lt;/div&gt;</span><br><span class="line">))</span><br></pre></td></tr></table></figure><p>通过这种方式我们就可以既实现了 shouldComponentUpdate 的浅比较，又能够使用函数组件。</p><h3 id="通信"><a href="#通信" class="headerlink" title="通信"></a>通信</h3><p>其实 React 中的组件通信基本和 Vue 中的一致</p><h4 id="父子通信"><a href="#父子通信" class="headerlink" title="父子通信"></a>父子通信</h4><p>父组件通过 props 传递数据给子组件，子组件通过调用父组件传来的函数传递数据给父组件</p><p>这是典型的单向数据流，父组件通过 props 传递数据，子组件不能直接修改 props， 而是必须通过调用父组件函数的方式告知父组件修改数据</p><h4 id="兄弟组件通信"><a href="#兄弟组件通信" class="headerlink" title="兄弟组件通信"></a>兄弟组件通信</h4><p>通过共同的父组件来管理状态和事件函数。比如说其中一个兄弟组件调用父组件传递过来的事件函数修改父组件中的状态，然后父组件将状态传递给另一个兄弟组件。</p><h4 id="跨多层次组件通信"><a href="#跨多层次组件通信" class="headerlink" title="跨多层次组件通信"></a>跨多层次组件通信</h4><p>这种情况可以使用 Context API。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 创建 Context，可以在开始就传入值</span></span><br><span class="line"><span class="keyword">const</span> StateContext = React.createContext()</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render () &#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      <span class="comment">// value 就是传入 Context 中的值</span></span><br><span class="line">      &lt;StateContext.Provider value=<span class="string">&#x27;yck&#x27;</span>&gt;</span><br><span class="line">        &lt;Child /&gt;</span><br><span class="line">      &lt;/StateContext.Provider&gt;</span><br><span class="line">    )</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render () &#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      &lt;ThemeContext.Consumer&gt;</span><br><span class="line">        <span class="comment">// 取出值</span></span><br><span class="line">        &#123;<span class="function"><span class="params">context</span> =&gt;</span> (</span><br><span class="line">          name is &#123; context &#125;</span><br><span class="line">        )&#125;</span><br><span class="line">      &lt;/ThemeContext.Consumer&gt;</span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="任意组件"><a href="#任意组件" class="headerlink" title="任意组件"></a>任意组件</h4><p>这种方式可以通过 Redux 或者 Event Bus 解决，另外如果你不怕麻烦的话，可以使用这种方式解决上述所有的通信情况</p><h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>React 的面试题还会经常考到 Virtual DOM 中的内容，所以这块内容大家也需要好好准备。</p><p>下一章节我们将来了解一些 React 的进阶知识内容。</p><h1 id="23-React-常考进阶知识点"><a href="#23-React-常考进阶知识点" class="headerlink" title="23.React 常考进阶知识点"></a>23.React 常考进阶知识点</h1><h3 id="HOC-是什么？相比-mixins-有什么优点？"><a href="#HOC-是什么？相比-mixins-有什么优点？" class="headerlink" title="HOC 是什么？相比 mixins 有什么优点？"></a>HOC 是什么？相比 mixins 有什么优点？</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">function add(a, b) &#123;</span><br><span class="line">    return a + b</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在如果我想给这个 add 函数添加一个输出结果的功能，那么你可能会考虑我直接使用 console.log 不就实现了么。说的没错，但是如果我们想做的更加优雅并且容易复用和扩展，我们可以这样去做：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">withLog</span> (<span class="params">fn</span>) </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">wrapper</span>(<span class="params">a, b</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">const</span> result = fn(a, b)</span><br><span class="line">        <span class="built_in">console</span>.log(result)</span><br><span class="line">        <span class="keyword">return</span> result</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> wrapper</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> withLogAdd = withLog(add)</span><br><span class="line">withLogAdd(<span class="number">1</span>, <span class="number">2</span>)</span><br></pre></td></tr></table></figure><p>这个做法在函数式编程里称之为高阶函数，大家都知道 React 的思想中是存在函数式编程的，高阶组件和高阶函数就是同一个东西。<br>实现一个函数，传入一个组件，然后在函数内部再实现一个函数去扩展传入的组件，最后返回一个新的组件，这就是高阶组件的概念，作用就是为了更好的复用代码。</p><p>其实 HOC 和 Vue 中的 mixins 作用是一致的，并且在早期 React 也是使用 mixins 的方式。但是在使用 class 的方式创建组件以后，mixins 的方式就不能使用了，并且其实 mixins 也是存在一些问题的，比如：</p><p>隐含了一些依赖，比如我在组件中写了某个 state 并且在 mixin 中使用了，就这存在了一个依赖关系。万一下次别人要移除它，就得去 mixin 中查找依赖</p><p>多个 mixin 中可能存在相同命名的函数，同时代码组件中也不能出现相同命名的函数，否则就是重写了，其实我一直觉得命名真的是一件麻烦事。。</p><p>雪球效应，虽然我一个组件还是使用着同一个 mixin，但是一个 mixin 会被多个组件使用，可能会存在需求使得 mixin 修改原本的函数或者新增更多的函数，这样可能就会产生一个维护成本<br>HOC 解决了这些问题，并且它们达成的效果也是一致的，同时也更加的政治正确（毕竟更加函数式了）。</p><h3 id="事件机制"><a href="#事件机制" class="headerlink" title="事件机制"></a>事件机制</h3><p>React 其实自己实现了一套事件机制，首先我们考虑一下以下代码：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> Test = <span class="function">(<span class="params">&#123; list, handleClick &#125;</span>) =&gt;</span> (&#123;</span><br><span class="line">    list.map(<span class="function">(<span class="params">item, index</span>) =&gt;</span> (</span><br><span class="line">        &lt;span onClick=&#123;handleClick&#125; key=&#123;index&#125;&gt;&#123;index&#125;&lt;/span&gt;</span><br><span class="line">    ))</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>以上类似代码想必大家经常会写到，但是你是否考虑过点击事件是否绑定在了每一个标签上？事实当然不是，JSX 上写的事件并没有绑定在对应的真实 DOM 上，而是通过事件代理的方式，将所有的事件都统一绑定在了 document 上。这样的方式不仅减少了内存消耗，还能在组件挂载销毁时统一订阅和移除事件。</p><p>另外冒泡到 document 上的事件也不是原生浏览器事件，而是 React 自己实现的合成事件（SyntheticEvent）。因此我们如果不想要事件冒泡的话，调用 event.stopPropagation 是无效的，而应该调用 event.preventDefault。</p><p>那么实现合成事件的目的是什么呢？总的来说在我看来好处有两点，分别是：</p><ul><li><p>合成事件首先抹平了浏览器之间的兼容问题，另外这是一个跨浏览器原生事件包装器，赋予了跨浏览器开发的能力</p></li><li><p>对于原生浏览器事件来说，浏览器会给监听器创建一个事件对象。如果你有很多的事件监听，那么就需要分配很多的事件对象，造成高额的内存分配问题。但是对于合成事件来说，有一个事件池专门来管理它们的创建和销毁，当事件需要被使用时，就会从池子中复用对象，事件回调结束后，就会销毁事件对象上的属性，从而便于下次复用事件对象。</p></li></ul>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(14)实现小型打包工具</title>
      <link href="2021/03/29/yck/(14)%E5%AE%9E%E7%8E%B0%E5%B0%8F%E5%9E%8B%E6%89%93%E5%8C%85%E5%B7%A5%E5%85%B7/"/>
      <url>2021/03/29/yck/(14)%E5%AE%9E%E7%8E%B0%E5%B0%8F%E5%9E%8B%E6%89%93%E5%8C%85%E5%B7%A5%E5%85%B7/</url>
      
        <content type="html"><![CDATA[<h1 id="18"><a href="#18" class="headerlink" title="18."></a>18.</h1><p>实现一个几十行的迷你打包工具，该工具实现以下两个功能</p><ul><li>将 ES6 转换为 ES5</li><li>支持在 JS 文件中 import CSS 文件</li></ul><p>通过这个工具的实现，可以理解到打包工具的原理到底是什么</p><p>实现<br>因为涉及到 ES6 转 ES5，所以我们首先需要安装一些 Babel 相关的工具</p><p><code>yarn add babylon babel-traverse babel-core babel-preset-env  </code></p><p>接下来我们将这些工具引入文件中</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">&#x27;fs&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">&#x27;path&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> babylon = <span class="built_in">require</span>(<span class="string">&#x27;babylon&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> traverse = <span class="built_in">require</span>(<span class="string">&#x27;babel-traverse&#x27;</span>).default</span><br><span class="line"><span class="keyword">const</span> &#123; transformFromAst &#125; = <span class="built_in">require</span>(<span class="string">&#x27;babel-core&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 首先，我们先来实现如何使用 Babel 转换代码</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">readCode</span>(<span class="params">filePath</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 读取文件内容</span></span><br><span class="line">  <span class="keyword">const</span> content = fs.readFileSync(filePath, <span class="string">&#x27;utf-8&#x27;</span>)</span><br><span class="line">  <span class="comment">// 生成 AST</span></span><br><span class="line">  <span class="keyword">const</span> ast = babylon.parse(content, &#123;</span><br><span class="line">    sourceType: <span class="string">&#x27;module&#x27;</span></span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="comment">// 寻找当前文件的依赖关系</span></span><br><span class="line">  <span class="keyword">const</span> dependencies = []</span><br><span class="line">  traverse(ast, &#123;</span><br><span class="line">    ImportDeclaration: <span class="function">(<span class="params">&#123; node &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">      dependencies.push(node.source.value)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="comment">// 通过 AST 将代码转为 ES5</span></span><br><span class="line">  <span class="keyword">const</span> &#123; code &#125; = transformFromAst(ast, <span class="literal">null</span>, &#123;</span><br><span class="line">    presets: [<span class="string">&#x27;env&#x27;</span>]</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    filePath,</span><br><span class="line">    dependencies,</span><br><span class="line">    code</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><ul><li>首先传入一个文件路径参数，然后通过 fs 将文件中的内容读取出来。</li><li>接下来通过 babylon 解析代码获取 AST，目的是为了分析代码中是否还引入了别的文件。</li><li>通过 dependencies 存储文件中的依赖，然后再将 AST 转换为 ES5 代码。</li><li>最后函数返回一个对象，对象中包含了当前文件路径、当前文件依赖和当前文件转换后的代码。</li></ul><p>接下来我们需要实现一个函数，这个函数的功能有以下几点</p><ul><li>调用 readCode 函数，传入入口文件；<br>分析入口文件的依赖；<br>识别 JS 和 CSS 文件；</li></ul><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getDependencies</span>(<span class="params">entry</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 读取入口文件</span></span><br><span class="line">  <span class="keyword">const</span> entryObject = readCode(entry)</span><br><span class="line">  <span class="keyword">const</span> dependencies = [entryObject]</span><br><span class="line">  <span class="comment">// 遍历所有文件依赖关系</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> asset <span class="keyword">of</span> dependencies) &#123;</span><br><span class="line">    <span class="comment">// 获得文件目录</span></span><br><span class="line">    <span class="keyword">const</span> dirname = path.dirname(asset.filePath)</span><br><span class="line">    <span class="comment">// 遍历当前文件依赖关系</span></span><br><span class="line">    asset.dependencies.forEach(<span class="function"><span class="params">relativePath</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="comment">// 获得绝对路径</span></span><br><span class="line">      <span class="keyword">const</span> absolutePath = path.join(dirname, relativePath)</span><br><span class="line">      <span class="comment">// CSS 文件逻辑就是将代码插入到 `style` 标签中</span></span><br><span class="line">      <span class="keyword">if</span> (<span class="regexp">/\.css$/</span>.test(absolutePath)) &#123;</span><br><span class="line">        <span class="keyword">const</span> content = fs.readFileSync(absolutePath, <span class="string">&#x27;utf-8&#x27;</span>)</span><br><span class="line">        <span class="keyword">const</span> code = <span class="string">`</span></span><br><span class="line"><span class="string">          const style = document.createElement(&#x27;style&#x27;)</span></span><br><span class="line"><span class="string">          style.innerText = <span class="subst">$&#123;<span class="built_in">JSON</span>.stringify(content).replace(<span class="regexp">/\\r\\n/g</span>, <span class="string">&#x27;&#x27;</span>)&#125;</span></span></span><br><span class="line"><span class="string">          document.head.appendChild(style)</span></span><br><span class="line"><span class="string">        `</span></span><br><span class="line">        dependencies.push(&#123;</span><br><span class="line">          filePath: absolutePath,</span><br><span class="line">          relativePath,</span><br><span class="line">          dependencies: [],</span><br><span class="line">          code</span><br><span class="line">        &#125;)</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// JS 代码需要继续查找是否有依赖关系</span></span><br><span class="line">        <span class="keyword">const</span> child = readCode(absolutePath)</span><br><span class="line">        child.relativePath = relativePath</span><br><span class="line">        dependencies.push(child)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> dependencies</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><ul><li>首先读取入口文件，然后创建一个数组存储代码中涉及到的所有文件</li><li>接下来遍历数组，一开始这个数组中只有入口文件，在遍历的过程中，如果入口文件有依赖其他的文件，就会被 push 到数组中</li><li>在遍历的过程中，先获得该文件对应的目录，然后遍历当前文件的依赖关系</li><li>在遍历当前文件依赖关系的过程中，首先生成依赖文件的绝对路径，然后判断当前文件是 CSS 文件还是 JS 文件</li><li>如果是 CSS 文件就不能用 Babel 去编译，只需要读取 CSS 文件中的代码，然后创建一个 style 标签，将代码插入进标签并且放入 head 中即可</li><li>如果是 JS 文件的话，需要分析 JS 文件是否还有别的依赖关系<br>最后将读取文件后的对象 push 进数组中</li></ul><p>现在我们已经获取到了所有的依赖文件，接下来就是实现打包的功能了</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bundle</span>(<span class="params">dependencies, entry</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> modules = <span class="string">&#x27;&#x27;</span></span><br><span class="line">  <span class="comment">// 构建函数参数，生成的结构为</span></span><br><span class="line">  <span class="comment">// &#123; &#x27;./entry.js&#x27;: function(module, exports, require) &#123; 代码 &#125; &#125;</span></span><br><span class="line">  dependencies.forEach(<span class="function"><span class="params">dep</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> filePath = dep.relativePath || entry</span><br><span class="line">    modules += <span class="string">`&#x27;<span class="subst">$&#123;filePath&#125;</span>&#x27;: (</span></span><br><span class="line"><span class="string">      function (module, exports, require) &#123; <span class="subst">$&#123;dep.code&#125;</span> &#125;</span></span><br><span class="line"><span class="string">    ),`</span></span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="comment">// 构建 require 函数，目的是为了获取模块暴露出来的内容</span></span><br><span class="line">  <span class="keyword">const</span> result = <span class="string">`</span></span><br><span class="line"><span class="string">    (function(modules) &#123;</span></span><br><span class="line"><span class="string">      function require(id) &#123;</span></span><br><span class="line"><span class="string">        const module = &#123; exports : &#123;&#125; &#125;</span></span><br><span class="line"><span class="string">        modules[id](module, module.exports, require)</span></span><br><span class="line"><span class="string">        return module.exports</span></span><br><span class="line"><span class="string">      &#125;</span></span><br><span class="line"><span class="string">      require(&#x27;<span class="subst">$&#123;entry&#125;</span>&#x27;)</span></span><br><span class="line"><span class="string">    &#125;)(&#123;<span class="subst">$&#123;modules&#125;</span>&#125;)</span></span><br><span class="line"><span class="string">  `</span></span><br><span class="line">  <span class="comment">// 当生成的内容写入到文件中</span></span><br><span class="line">  fs.writeFileSync(<span class="string">&#x27;./bundle.js&#x27;</span>, result)</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>这段代码需要结合着 Babel 转换后的代码来看，这样大家就能理解为什么需要这样写了</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// entry.js</span></span><br><span class="line"><span class="keyword">var</span> _a = <span class="built_in">require</span>(<span class="string">&#x27;./a.js&#x27;</span>)</span><br><span class="line"><span class="keyword">var</span> _a2 = _interopRequireDefault(_a)</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">_interopRequireDefault</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> obj &amp;&amp; obj.__esModule ? obj : &#123; <span class="attr">default</span>: obj &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(_a2.default)</span><br><span class="line"><span class="comment">// a.js</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(<span class="built_in">exports</span>, <span class="string">&#x27;__esModule&#x27;</span>, &#123;</span><br><span class="line">    value: <span class="literal">true</span></span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line"><span class="built_in">exports</span>.default = a</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>Babel 将我们 ES6 的模块化代码转换为了 CommonJS（如果你不熟悉 CommonJS 的话，可以阅读这一章节中关于 模块化的知识点） 的代码，但是浏览器是不支持 CommonJS 的，所以如果这段代码需要在浏览器环境下运行的话，我们需要自己实现 CommonJS 相关的代码，这就是 bundle 函数做的大部分事情。</p><p>接下来我们再来逐行解析 bundle 函数</p><p>首先遍历所有依赖文件，构建出一个函数参数对象<br>对象的属性就是当前文件的相对路径，属性值是一个函数，函数体是当前文件下的代码，函数接受三个参数 module、exports、 require</p><ul><li>module 参数对应 CommonJS 中的 module</li><li>exports 参数对应 CommonJS 中的 module.export</li><li>require 参数对应我们自己创建的 require 函数</li></ul><p>接下来就是构造一个使用参数的函数了，函数做的事情很简单，就是内部创建一个 require 函数，然后调用 require(entry)，也就是 require(‘./entry.js’)，这样就会从函数参数中找到 ./entry.js 对应的函数并执行，最后将导出的内容通过 module.export 的方式让外部获取到</p><p>最后再将打包出来的内容写入到单独的文件中</p><p>打包后的部分简化代码</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">;(<span class="function"><span class="keyword">function</span>(<span class="params">modules</span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">require</span>(<span class="params">id</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 构造一个 CommonJS 导出代码</span></span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">module</span> = &#123; <span class="attr">exports</span>: &#123;&#125; &#125;</span><br><span class="line">    <span class="comment">// 去参数中获取文件对应的函数并执行</span></span><br><span class="line">    modules[id](<span class="built_in">module</span>, <span class="built_in">module</span>.exports, <span class="built_in">require</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">module</span>.exports</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">require</span>(<span class="string">&#x27;./entry.js&#x27;</span>)</span><br><span class="line">&#125;)(&#123;</span><br><span class="line">  <span class="string">&#x27;./entry.js&#x27;</span>: <span class="function"><span class="keyword">function</span>(<span class="params"><span class="built_in">module</span>, <span class="built_in">exports</span>, <span class="built_in">require</span></span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 这里继续通过构造的 require 去找到 a.js 文件对应的函数</span></span><br><span class="line">    <span class="keyword">var</span> _a = <span class="built_in">require</span>(<span class="string">&#x27;./a.js&#x27;</span>)</span><br><span class="line">    <span class="built_in">console</span>.log(_a2.default)</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&#x27;./a.js&#x27;</span>: <span class="function"><span class="keyword">function</span>(<span class="params"><span class="built_in">module</span>, <span class="built_in">exports</span>, <span class="built_in">require</span></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line">    <span class="comment">// 将 require 函数中的变量 module 变成了这样的结构</span></span><br><span class="line">    <span class="comment">// module.exports = 1</span></span><br><span class="line">    <span class="comment">// 这样就能在外部取到导出的内容了</span></span><br><span class="line">    <span class="built_in">exports</span>.default = a</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 省略</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(13)Webpack 性能优化</title>
      <link href="2021/03/29/yck/(13)Webpack%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/"/>
      <url>2021/03/29/yck/(13)Webpack%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/</url>
      
        <content type="html"><![CDATA[<h1 id="17"><a href="#17" class="headerlink" title="17."></a>17.</h1><p>在这部分的内容中，我们会聚焦于以下两个知识点，并且每一个知识点都属于高频考点：</p><p>减少 Webpack 的打包时间、让 Webpack 打出来的包更小</p><h3 id="减少-Webpack-打包时间"><a href="#减少-Webpack-打包时间" class="headerlink" title="减少 Webpack 打包时间"></a>减少 Webpack 打包时间</h3><h4 id="优化-Loader"><a href="#优化-Loader" class="headerlink" title="优化 Loader"></a>优化 Loader</h4><p>对于 Loader 来说，影响打包效率首当其冲必属 Babel 。<br>因为 Babel 会将代码转为字符串生成 AST，然后对 AST 继续进行转变最后再生成新的代码，项目越大，转换代码越多，效率就越低</p><p>首先可以优化 Loader 的文件搜索范围</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  <span class="built_in">module</span>: &#123;</span><br><span class="line">    rules: [</span><br><span class="line">      &#123;</span><br><span class="line">        <span class="comment">// js 文件才使用 babel</span></span><br><span class="line">        test: <span class="regexp">/\.js$/</span>,</span><br><span class="line">        loader: <span class="string">&#x27;babel-loader&#x27;</span>,</span><br><span class="line">        <span class="comment">// 只在 src 文件夹下查找</span></span><br><span class="line">        include: [resolve(<span class="string">&#x27;src&#x27;</span>)],</span><br><span class="line">        <span class="comment">// 不会去查找的路径</span></span><br><span class="line">        exclude: <span class="regexp">/node_modules/</span></span><br><span class="line">      &#125;</span><br><span class="line">    ]</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>希望Babel只作用在 JS 代码，然后 node_modules 中使用的代码都是编译过的，所以没有必要再去处理一遍</p><p>当然这样做还不够，还可以将 Babel 编译过的文件缓存起来，下次只需要编译更改过的代码文件即可，可以大幅度加快打包时间</p><p><code>loader: &#39;babel-loader?cacheDirectory=true&#39;</code></p><h4 id="HappyPack"><a href="#HappyPack" class="headerlink" title="HappyPack"></a>HappyPack</h4><p>Node 是单线程运行，所以 Webpack 在打包的过程中也是单线程的，特别是在执行 Loader 的时候，长时间编译的任务很多，这样就会导致等待的情况。</p><p>HappyPack 可以将 Loader 的同步执行转换为并行的，能充分利用系统资源加快打包效率</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">module</span>: &#123;</span><br><span class="line">  loaders: [</span><br><span class="line">    &#123;</span><br><span class="line">      test: <span class="regexp">/\.js$/</span>,</span><br><span class="line">      include: [resolve(<span class="string">&#x27;src&#x27;</span>)],</span><br><span class="line">      exclude: <span class="regexp">/node_modules/</span>,</span><br><span class="line">      <span class="comment">// id 后面的内容对应下面</span></span><br><span class="line">      loader: <span class="string">&#x27;happypack/loader?id=happybabel&#x27;</span></span><br><span class="line">    &#125;</span><br><span class="line">  ]</span><br><span class="line">&#125;,</span><br><span class="line">plugins: [</span><br><span class="line">  <span class="keyword">new</span> HappyPack(&#123;</span><br><span class="line">    id: <span class="string">&#x27;happybabel&#x27;</span>,</span><br><span class="line">    loaders: [<span class="string">&#x27;babel-loader?cacheDirectory&#x27;</span>],</span><br><span class="line">    <span class="comment">// 开启 4 个线程</span></span><br><span class="line">    threads: <span class="number">4</span></span><br><span class="line">  &#125;)</span><br><span class="line">]</span><br></pre></td></tr></table></figure><h4 id="DllPlugin"><a href="#DllPlugin" class="headerlink" title="DllPlugin"></a>DllPlugin</h4><p>DllPlugin 可以将特定的类库提前打包然后引入,极大的减少打包类库的次数，只有当类库更新版本才有需要重新打包，并也实现了将公共代码抽离成单独文件的优化方案</p><p>如何使用 DllPlugin</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 单独配置在一个文件中</span></span><br><span class="line"><span class="comment">// webpack.dll.conf.js</span></span><br><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">&#x27;path&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> webpack = <span class="built_in">require</span>(<span class="string">&#x27;webpack&#x27;</span>)</span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  entry: &#123;</span><br><span class="line">    <span class="comment">// 想统一打包的类库</span></span><br><span class="line">    vendor: [<span class="string">&#x27;react&#x27;</span>]</span><br><span class="line">  &#125;,</span><br><span class="line">  output: &#123;</span><br><span class="line">    path: path.join(__dirname, <span class="string">&#x27;dist&#x27;</span>),</span><br><span class="line">    filename: <span class="string">&#x27;[name].dll.js&#x27;</span>,</span><br><span class="line">    library: <span class="string">&#x27;[name]-[hash]&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  plugins: [</span><br><span class="line">    <span class="keyword">new</span> webpack.DllPlugin(&#123;</span><br><span class="line">      <span class="comment">// name 必须和 output.library 一致</span></span><br><span class="line">      name: <span class="string">&#x27;[name]-[hash]&#x27;</span>,</span><br><span class="line">      <span class="comment">// 该属性需要与 DllReferencePlugin 中一致</span></span><br><span class="line">      context: __dirname,</span><br><span class="line">      path: path.join(__dirname, <span class="string">&#x27;dist&#x27;</span>, <span class="string">&#x27;[name]-manifest.json&#x27;</span>)</span><br><span class="line">    &#125;)</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>然后需要执行这个配置文件生成依赖文件，接下来使用 DllReferencePlugin 将依赖文件引入项目中</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// webpack.conf.js</span></span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  <span class="comment">// ...省略其他配置</span></span><br><span class="line">  plugins: [</span><br><span class="line">    <span class="keyword">new</span> webpack.DllReferencePlugin(&#123;</span><br><span class="line">      context: __dirname,</span><br><span class="line">      <span class="comment">// manifest 就是之前打包出来的 json 文件</span></span><br><span class="line">      manifest: <span class="built_in">require</span>(<span class="string">&#x27;./dist/vendor-manifest.json&#x27;</span>),</span><br><span class="line">    &#125;)</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="代码压缩"><a href="#代码压缩" class="headerlink" title="代码压缩"></a>代码压缩</h4><p>在 Webpack3 中，一般使用 UglifyJS 压缩代码，但这个是单线程运行的，为了加快效率，可以使用 webpack-parallel-uglify-plugin 来并行运行 UglifyJS，从而提高效率。</p><p>Webpack4 不需要以上这些操作，只需要将 mode 设置为 production 就可以默认开启以上功能。代码压缩也是必做的性能优化方。不止可以压缩 JS 代码，还可以压缩 HTML、CSS 代码，并且在压缩 JS 代码的过程中，还可以通过配置实现比如删除 console.log 这类代码的功能</p><h4 id="一些小的优化点"><a href="#一些小的优化点" class="headerlink" title="一些小的优化点"></a>一些小的优化点</h4><ul><li>resolve.extensions：用来表明文件后缀列表，默认查找顺序是 [‘.js’, ‘.json’]，如果导入文件没有添加后缀就会按照这个顺序查找文件。应该尽可能减少后缀列表长度，将出现频率高的后缀排在前面</li><li>resolve.alias：通过别名的方式来映射一个路径，让 Webpack 更快找到路径</li><li>module.noParse：如果确定一个文件下没有其他依赖，可以使用该属性让 Webpack 不扫描该文件，这种方式对于大型的类库很有帮助<h3 id="减少-Webpack-打包后的文件体积"><a href="#减少-Webpack-打包后的文件体积" class="headerlink" title="减少 Webpack 打包后的文件体积"></a>减少 Webpack 打包后的文件体积</h3><h4 id="按需加载"><a href="#按需加载" class="headerlink" title="按需加载"></a>按需加载</h4>想必大家在开发 SPA 项目的时候，项目中都会存在十几甚至更多的路由页面。如果将这些页面全部打包进一个 JS 文件的话，虽然将多个请求合并了，但是同样也加载了很多不需要的代码，耗费更长的时间。那么为了首页能更快地呈现给用户，肯定希望首页能加载的文件体积越小越好，这时候可以使用按需加载，将每个路由页面单独打包为一个文件。当然不仅仅路由可以按需加载，对于 loadash 这种大型类库同样可以使用这个功能。</li></ul><p>按需加载的代码实现这里就不详细展开了，因为鉴于用的框架不同，实现起来都是不一样的。当然了，虽然他们的用法可能不同，但是底层的机制都是一样的。都是当使用的时候再去下载对应文件，返回一个 Promise，当 Promise 成功以后去执行回调。</p><h4 id="Scope-Hoisting"><a href="#Scope-Hoisting" class="headerlink" title="Scope Hoisting"></a>Scope Hoisting</h4><p>Scope Hoisting 会分析出模块之间的依赖关系，尽可能把打包出来的模块合并到一个函数中。</p><p>比如希望打包两个文件</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// test.js</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> a = <span class="number">1</span></span><br><span class="line"><span class="comment">// index.js</span></span><br><span class="line"><span class="keyword">import</span> &#123; a &#125; <span class="keyword">from</span> <span class="string">&#x27;./test.js&#x27;</span></span><br><span class="line"><span class="comment">// 对于这种情况，我们打包出来的代码会类似这样</span></span><br><span class="line"></span><br><span class="line">[</span><br><span class="line">  <span class="comment">/* 0 */</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> (<span class="params"><span class="built_in">module</span>, <span class="built_in">exports</span>, <span class="built_in">require</span></span>) </span>&#123;</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">/* 1 */</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> (<span class="params"><span class="built_in">module</span>, <span class="built_in">exports</span>, <span class="built_in">require</span></span>) </span>&#123;</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">  &#125;</span><br><span class="line">]</span><br><span class="line"><span class="comment">// 但是如果使用 Scope Hoisting ，代码会尽可能合并到一个函数，变成这样的代码</span></span><br><span class="line">[</span><br><span class="line">  <span class="comment">/* 0 */</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> (<span class="params"><span class="built_in">module</span>, <span class="built_in">exports</span>, <span class="built_in">require</span></span>) </span>&#123;</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">  &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>这样的打包生成的代码明显比之前的少多了。如果在 Webpack4 中希望开启这个功能，只需要启用 optimization.concatenateModules 就可以了。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  optimization: &#123;</span><br><span class="line">    concatenateModules: <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="Tree-Shaking"><a href="#Tree-Shaking" class="headerlink" title="Tree Shaking"></a>Tree Shaking</h4><p>Tree Shaking 可以实现删除项目中未被引用的代码，比如</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// test.js</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> a = <span class="number">1</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> b = <span class="number">2</span></span><br><span class="line"><span class="comment">// index.js</span></span><br><span class="line"><span class="keyword">import</span> &#123; a &#125; <span class="keyword">from</span> <span class="string">&#x27;./test.js&#x27;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p>对于以上情况，test 文件中的变量 b 如果没有在项目中使用到的话，就不会被打包到文件中。<br>如果你使用 Webpack 4 的话，开启生产环境就会自动启动这个优化功能。</p><h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>在这一章节中，我们学习了如何使用 Webpack 去进行性能优化以及如何减少打包时间。</p><p>Webpack 的版本更新很快，各个版本之间实现优化的方式可能都会有区别，所以我没有使用过多的代码去展示如何实现一个功能。这一章节的重点是学习到我们可以通过什么方式去优化，具体的代码实现可以查找具体版本对应的代码即可。</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(12)性能优化</title>
      <link href="2021/03/29/yck/(12)%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/"/>
      <url>2021/03/29/yck/(12)%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/</url>
      
        <content type="html"><![CDATA[<h1 id="15-从-V8-中看-JS"><a href="#15-从-V8-中看-JS" class="headerlink" title="15.从 V8 中看 JS"></a>15.从 V8 中看 JS</h1><p>注意：该知识点属于性能优化领域。</p><p>性能问题越来越成为前端火热的话题，因为随着项目的逐步变大，性能问题也逐步体现出来。为了提高用户的体验，减少加载时间，工程师们想尽一切办法去优化细节。</p><p>掘金之前已经出过一本关于性能的小册</p><p>在这几个章节中不会提及浏览器、Webpack、网络协议这几块如何优化的内容，因为对应的模块中已经讲到了这部分的内容</p><p>在这一章节中我们将来学习如何让 V8 优化我们的代码，下一章节将会学习性能优化剩余的琐碎点，因为性能优化这个领域所涉及的内容都很碎片化。</p><p>在学习如何性能优化之前，我们先来了解下如何测试性能问题，毕竟是先有问题才会去想着该如何改进。</p><h3 id="针对-Chrome-的-V8-引擎的-JS-性能优化"><a href="#针对-Chrome-的-V8-引擎的-JS-性能优化" class="headerlink" title="针对 Chrome 的 V8 引擎的 JS 性能优化"></a>针对 Chrome 的 V8 引擎的 JS 性能优化</h3><p>JS 是编译型还是解释型语言其实并不固定。<br>首先 JS 需要有引擎才能运行起来，无论是浏览器还是在 Node 中，这是解释型语言的特性。<br>但是在 V8 引擎下，又引入了 TurboFan 编译器，他会在特定的情况下进行优化，将代码编译成执行效率更高的 Machine Code，当然这个编译器并不是 JS 必须需要的，只是为了提高代码执行性能，所以总的来说 JS 更偏向于解释型语言。</p><p>在这一过程中，JS 代码首先会解析为抽象语法树（AST），然后会通过解释器或者编译器转化为 <strong>Bytecode</strong> 或者 <strong>Machine Code</strong></p><p><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210114095540.png" alt="V8 转化代码的过程"><br>JS 会首先被解析为 AST，解析的过程其实是略慢的。代码越多，解析耗费时间越长，这也是需要压缩代码的原因之一。另外一种减少解析时间的方式是预解析，会作用于未执行的函数，这个我们下面再谈。</p><p>这里需要注意一点，对于函数来说，应该避免声明嵌套函数（类也是函数），因为这样会造成函数的重复解析。</p><p>然后 <strong>Ignition</strong> 负责将 AST 转化为 Bytecode，<strong>TurboFan</strong> 负责编译出优化后的 Machine Code，并且 Machine Code 在执行效率上优于 Bytecode<br><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210114100155.png"></p><p>那么什么情况下代码会编译为 Machine Code？</p><p>JS 是一门动态类型的语言，并且还有一大堆的规则。简单的加法运算代码，内部就需要考虑好几种规则，比如数字相加、字符串相加、对象和字符串相加等等。这样的情况也就势必导致了内部要增加很多判断逻辑，降低运行效率。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> x + x</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">test(<span class="number">1</span>)</span><br><span class="line">test(<span class="number">2</span>)</span><br><span class="line">test(<span class="number">3</span>)</span><br><span class="line">test(<span class="number">4</span>)</span><br></pre></td></tr></table></figure><p>对于以上代码来说，如果一个函数被多次调用并且参数一直传入 number 类型，那么 V8 就会认为该段代码可以编译为 Machine Code，因为你固定了类型，不需要再执行很多判断逻辑了。</p><p>但是如果一旦我们传入的参数类型改变，那么 Machine Code 就会被 DeOptimized 为 Bytecode，这样就有性能上的一个损耗了。所以如果我们希望代码能多的编译为 Machine Code 并且 DeOptimized 的次数减少，就应该尽可能保证传入的类型一致。</p><p>那么你可能会有一个疑问，到底优化前后有多少的提升呢，接下来我们就来实践测试一下到底有多少的提升。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> &#123; performance, PerformanceObserver &#125; = <span class="built_in">require</span>(<span class="string">&#x27;perf_hooks&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> x + x</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// node 10 中才有 PerformanceObserver</span></span><br><span class="line"><span class="comment">// 在这之前的 node 版本可以直接使用 performance 中的 API</span></span><br><span class="line"><span class="keyword">const</span> obs = <span class="keyword">new</span> PerformanceObserver(<span class="function">(<span class="params">list, observer</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(list.getEntries())</span><br><span class="line">  observer.disconnect()</span><br><span class="line">&#125;)</span><br><span class="line">obs.observe(&#123; <span class="attr">entryTypes</span>: [<span class="string">&#x27;measure&#x27;</span>], <span class="attr">buffered</span>: <span class="literal">true</span> &#125;)</span><br><span class="line"></span><br><span class="line">performance.mark(<span class="string">&#x27;start&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> number = <span class="number">10000000</span></span><br><span class="line"><span class="comment">// 不优化代码</span></span><br><span class="line">%NeverOptimizeFunction(test)</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (number--) &#123;</span><br><span class="line">  test(<span class="number">1</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">performance.mark(<span class="string">&#x27;end&#x27;</span>)</span><br><span class="line">performance.measure(<span class="string">&#x27;test&#x27;</span>, <span class="string">&#x27;start&#x27;</span>, <span class="string">&#x27;end&#x27;</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>以上代码中我们使用了 performance API，这个 API 在性能测试上十分好用。不仅可以用来测量代码的执行时间，还能用来测量各种网络连接中的时间消耗等等，并且这个 API 也可以在浏览器中使用。</p><p><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210114100247.png" alt="优化与不优化代码之间的巨大差距"></p><p>从上图中可以发现 V8 的性能优化有多强，只需要符合一定的规则书写代码，引擎底层就能自动优化代码</p><p>另外，编译器还有个骚操作 Lazy-Compile，当函数没有被执行的时候，会对函数进行一次预解析，直到代码被执行以后才会被解析编译。对于上述代码来说，test 函数需要被预解析一次，然后在调用的时候再被解析编译。但是对于这种函数马上就被调用的情况来说，预解析这个过程其实是多余的，那么有什么办法能够让代码不被预解析呢？</p><p>其实很简单，我们只需要给函数套上括号就可以了</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> x + x</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>但是不可能我们为了性能优化，给所有的函数都去套上括号，并且也不是所有函数都需要这样做。我们可以通过 optimize-js 实现这个功能，这个库会分析一些函数的使用情况，然后给需要的函数添加括号，当然这个库很久没人维护了，如果需要使用的话，还是需要测试过相关内容的。</p><h1 id="16-性能优化琐碎事"><a href="#16-性能优化琐碎事" class="headerlink" title="16.性能优化琐碎事"></a>16.性能优化琐碎事</h1><p>注意：该知识点属于性能优化领域。</p><p>总的来说性能优化这个领域的很多内容都很碎片化，这一章节我们将来学习这些碎片化的内容。</p><h3 id="图片优化"><a href="#图片优化" class="headerlink" title="图片优化"></a>图片优化</h3><h4 id="计算图片大小"><a href="#计算图片大小" class="headerlink" title="计算图片大小"></a>计算图片大小</h4><p>对于100 * 100 像素的图片来说，有 10000 个像素点，如果每个像素的值是 RGBA 存储的话，那么每个像素有 4 个通道，每个通道 1 个字节（8 位 = 1个字节），所以图片大小大概为 39KB（10000 * 1 * 4 / 1024）</p><p>但是在实际项目中，一张图片可能不需要使用那么多颜色去显示，我们可以通过减少每个像素的调色板来相应缩小图片的大小。</p><p>如何优化图片，有 2 个思路：减少像素点、减少每个像素点能够显示的颜色</p><h4 id="图片加载优化"><a href="#图片加载优化" class="headerlink" title="图片加载优化"></a>图片加载优化</h4><ul><li>不用图片：修饰类图片，可以用 CSS 去代替</li><li>移动端屏幕宽度就那么点，没有必要加载原图浪费带宽。一般图片都用 CDN 加载，可以计算出适配屏幕的宽度，然后去请求相应裁剪好的图片</li><li>小图使用 base64 格式</li><li>将多个图标文件整合到一张图片中（雪碧图）</li><li>选择正确的图片格式：<ul><li>对于支持 WebP 格式的浏览器尽量使用 WebP 格式。因为 WebP 具有更好的图像压缩算法，能带来更小的图片体积，而且拥有肉眼识别无差异的图像质量</li><li>小图使用 PNG，其实对于大部分图标这类图片，完全可以使用 SVG 代替</li><li>照片使用 JPEG</li></ul></li></ul><h3 id="DNS-预解析"><a href="#DNS-预解析" class="headerlink" title="DNS 预解析"></a>DNS 预解析</h3><p>DNS 解析也是需要时间的，可以通过预解析的方式来预先获得域名所对应的 IP。</p><p><code>&lt;link rel=&quot;dns-prefetch&quot; href=&quot;//yuchengkai.cn&quot;&gt;</code></p><h3 id="节流"><a href="#节流" class="headerlink" title="节流"></a>节流</h3><p>考虑一个场景，滚动事件中会发起网络请求，但是我们并不希望用户在滚动过程中一直发起请求，而是隔一段时间发起一次，对于这种情况我们就可以使用节流。</p><p>理解了节流的用途，我们就来实现下这个函数</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// func是用户传入需要防抖的函数</span></span><br><span class="line"><span class="comment">// wait是等待时间</span></span><br><span class="line"><span class="keyword">const</span> throttle = <span class="function">(<span class="params">func, wait = <span class="number">50</span></span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 上一次执行该函数的时间</span></span><br><span class="line">  <span class="keyword">let</span> lastTime = <span class="number">0</span></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">...args</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 当前时间</span></span><br><span class="line">    <span class="keyword">let</span> now = +<span class="keyword">new</span> <span class="built_in">Date</span>()</span><br><span class="line">    <span class="comment">// 将当前时间和上一次执行函数时间对比</span></span><br><span class="line">    <span class="comment">// 如果差值大于设置的等待时间就执行函数</span></span><br><span class="line">    <span class="keyword">if</span> (now - lastTime &gt; wait) &#123;</span><br><span class="line">      lastTime = now</span><br><span class="line">      func.apply(<span class="built_in">this</span>, args)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">setInterval</span>(</span><br><span class="line">  throttle(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="number">1</span>)</span><br><span class="line">  &#125;, <span class="number">500</span>),</span><br><span class="line">  <span class="number">1</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure><h3 id="防抖"><a href="#防抖" class="headerlink" title="防抖"></a>防抖</h3><p>考虑一个场景，有一个按钮点击会触发网络请求，但是我们并不希望每次点击都发起网络请求，而是当用户点击按钮一段时间后没有再次点击的情况才去发起网络请求，对于这种情况我们就可以使用防抖。</p><p>理解了防抖的用途，我们就来实现下这个函数</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// func是用户传入需要防抖的函数</span></span><br><span class="line"><span class="comment">// wait是等待时间</span></span><br><span class="line"><span class="keyword">const</span> debounce = <span class="function">(<span class="params">func, wait = <span class="number">50</span></span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 缓存一个定时器id</span></span><br><span class="line">  <span class="keyword">let</span> timer = <span class="number">0</span></span><br><span class="line">  <span class="comment">// 这里返回的函数是每次用户实际调用的防抖函数</span></span><br><span class="line">  <span class="comment">// 如果已经设定过定时器了就清空上一次的定时器</span></span><br><span class="line">  <span class="comment">// 开始一个新的定时器，延迟执行用户传入的方法</span></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">...args</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (timer) <span class="built_in">clearTimeout</span>(timer)</span><br><span class="line">    timer = <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      func.apply(<span class="built_in">this</span>, args)</span><br><span class="line">    &#125;, wait)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="预加载"><a href="#预加载" class="headerlink" title="预加载"></a>预加载</h3><p>在开发中，可能会遇到这样的情况。有些资源不需要马上用到，但是希望尽早获取，这时候就可以使用预加载。</p><p>预加载其实是声明式的 fetch ，强制浏览器请求资源，并且不会阻塞 onload 事件，可以使用以下代码开启预加载</p><p><code>&lt;link rel=&quot;preload&quot; href=&quot;http://example.com&quot;&gt;</code></p><p>预加载可以一定程度上降低首屏的加载时间，因为可以将一些不影响首屏但重要的文件延后加载，唯一缺点就是兼容性不好。</p><h3 id="预渲染"><a href="#预渲染" class="headerlink" title="预渲染"></a>预渲染</h3><p>可以通过预渲染将下载的文件预先在后台渲染，可以使用以下代码开启预渲染</p><p><code>&lt;link rel=&quot;prerender&quot; href=&quot;http://example.com&quot;&gt; </code></p><p>预渲染虽然可以提高页面的加载速度，但是要确保该页面大概率会被用户在之后打开，否则就是白白浪费资源去渲染。</p><h3 id="懒执行"><a href="#懒执行" class="headerlink" title="懒执行"></a>懒执行</h3><p>懒执行就是将某些逻辑延迟到使用时再计算。该技术可以用于首屏优化，对于某些耗时逻辑并不需要在首屏就使用的，就可以使用懒执行。懒执行需要唤醒，一般可以通过定时器或者事件的调用来唤醒。</p><h3 id="懒加载"><a href="#懒加载" class="headerlink" title="懒加载"></a>懒加载</h3><p>懒加载就是将不关键的资源延后加载。</p><p>懒加载的原理就是只加载自定义区域（通常是可视区域，但也可以是即将进入可视区域）内需要加载的东西。对于图片来说，先设置图片标签的 src 属性为一张占位图，将真实的图片资源放入一个自定义属性中，当进入自定义区域时，就将自定义属性替换为 src 属性，这样图片就会去下载资源，实现了图片懒加载。</p><p>懒加载不仅可以用于图片，也可以使用在别的资源上。比如进入可视区域才开始播放视频等等。</p><h3 id="CDN"><a href="#CDN" class="headerlink" title="CDN"></a>CDN</h3><p>CDN 的原理是尽可能的在各个地方分布机房缓存数据，这样即使我们的根服务器远在国外，在国内的用户也可以通过国内的机房迅速加载资源。</p><p>因此，我们可以将静态资源尽量使用 CDN 加载，由于浏览器对于单个域名有并发请求上限，可以考虑使用多个 CDN 域名。并且对于 CDN 加载静态资源需要注意 CDN 域名要与主站不同，否则每次请求都会带上主站的 Cookie，平白消耗流量。</p><p>小结<br>这些碎片化的性能优化点看似很短，但是却能在出现性能问题时简单高效的提高性能，并且好几个点都是面试高频考点，比如节流、防抖。如果你还没有在项目中使用过这些技术，可以尝试着用到项目中，体验下功效。</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(11)安全防范知识点</title>
      <link href="2021/03/29/yck/(11)%E7%BD%91%E7%BB%9C%E5%AE%89%E5%85%A8/"/>
      <url>2021/03/29/yck/(11)%E7%BD%91%E7%BB%9C%E5%AE%89%E5%85%A8/</url>
      
        <content type="html"><![CDATA[<h1 id="14"><a href="#14" class="headerlink" title="14."></a>14.</h1><h3 id="XSS"><a href="#XSS" class="headerlink" title="XSS"></a>XSS</h3><p><strong>涉及面试题：什么是 XSS 攻击？如何防范 XSS 攻击？什么是 CSP？</strong></p><p><strong>XSS</strong>：Cross Site Scripting 攻击者将代码注入网页中执行</p><p>XSS 分为两类：</p><p><strong>持久型</strong>：代码被服务端写入数据库，这种攻击危害性很大，导致大量用户都受到攻击<br>（对于评论功能就得防范持久型 XSS ）</p><p><strong>非持久型</strong>：通过修改 URL 参数加入攻击代码，诱导用户访问链接从而攻击<br>（举个例子，页面需要从 URL 获取参数作为内容，如果不过滤会导致攻击代码被执行）</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">&lt;!-- http:<span class="comment">//www.domain.com?name=&lt;script&gt;alert(1)&lt;/script&gt; --&gt;</span></span><br><span class="line">&lt;div&gt;&#123;&#123;name&#125;&#125;&lt;/div&gt;                                                  </span><br></pre></td></tr></table></figure><p>但是对于这种攻击方式来说，如果用户使用 Chrome 这类浏览器的话，浏览器就能自动帮助用户防御攻击。但是我们不能因此就不防御此类攻击了，因为我不能确保用户都使用了该类浏览器。</p><p>对于 XSS 攻击来说，通常有两种方式可以用来防御。</p><h4 id="转义字符"><a href="#转义字符" class="headerlink" title="转义字符"></a>转义字符</h4><p>首先，对于用户的输入应该永远不信任，最普遍的做法就是转义输入输出的内容，对于引号、尖括号、斜杠进行转义</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">escape</span>(<span class="params">str</span>) </span>&#123;</span><br><span class="line">  str = str.replace(<span class="regexp">/&amp;/g</span>, <span class="string">&#x27;&amp;amp;&#x27;</span>)</span><br><span class="line">  str = str.replace(<span class="regexp">/&lt;/g</span>, <span class="string">&#x27;&amp;lt;&#x27;</span>)</span><br><span class="line">  str = str.replace(<span class="regexp">/&gt;/g</span>, <span class="string">&#x27;&amp;gt;&#x27;</span>)</span><br><span class="line">  str = str.replace(<span class="regexp">/&quot;/g</span>, <span class="string">&#x27;&amp;quto;&#x27;</span>)</span><br><span class="line">  str = str.replace(<span class="regexp">/&#x27;/g</span>, <span class="string">&#x27;&amp;#39;&#x27;</span>)</span><br><span class="line">  str = str.replace(<span class="regexp">/`/g</span>, <span class="string">&#x27;&amp;#96;&#x27;</span>)</span><br><span class="line">  str = str.replace(<span class="regexp">/\//g</span>, <span class="string">&#x27;&amp;#x2F;&#x27;</span>)</span><br><span class="line">  <span class="keyword">return</span> str</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过转义可以将攻击代码 <code>&lt;script&gt;alert(1)&lt;/script&gt;</code> 变成</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// -&gt; &amp;lt;script&amp;gt;alert(1)&amp;lt;&amp;#x2F;script&amp;gt;</span></span><br><span class="line"><span class="built_in">escape</span>(<span class="string">&#x27;&lt;script&gt;alert(1)&lt;/script&gt;&#x27;</span>)</span><br></pre></td></tr></table></figure><p>但是对于显示富文本来说，显然不能通过上面的办法转义所有字符，因为这样会把需要的格式也过滤掉。对于这种情况，通常采用<strong>白名单过滤</strong>(或黑名单过滤)，考虑到需要过滤的标签和标签属性实在太多，更推荐使用白名单</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> xss = <span class="built_in">require</span>(<span class="string">&#x27;xss&#x27;</span>)</span><br><span class="line"><span class="keyword">let</span> html = xss(<span class="string">&#x27;&lt;h1 id=&quot;title&quot;&gt;XSS Demo&lt;/h1&gt;&lt;script&gt;alert(&quot;xss&quot;);&lt;/script&gt;&#x27;</span>)</span><br><span class="line"><span class="comment">// -&gt; &lt;h1&gt;XSS Demo&lt;/h1&gt;&amp;lt;script&amp;gt;alert(&quot;xss&quot;);&amp;lt;/script&amp;gt;</span></span><br><span class="line"><span class="built_in">console</span>.log(html)</span><br></pre></td></tr></table></figure><p>以上示例使用了 js-xss 来实现，可以看到在输出中保留了 h1 标签且过滤了 script 标签</p><h4 id="CSP"><a href="#CSP" class="headerlink" title="CSP"></a>CSP</h4><p>CSP 本质是建立白名单，开发者告诉浏览器哪些外部资源可以执行。只需要配置规则，如何拦截是由浏览器实现，从而减少 XSS 攻击</p><p><strong>开启 CSP</strong>：</p><p>设置 HTTP Header 的 Content-Security-Policy</p><p>设置 meta 标签的方式 <code>&lt; meta http-equiv=&quot;Content-Security-Policy&quot; &gt;</code></p><p><strong>以设置 HTTP Header 来举例</strong></p><p>只允许加载本站资源</p><p><code>Content-Security-Policy: default-src ‘self’</code></p><p>只允许加载 HTTPS 协议图片</p><p><code>Content-Security-Policy: img-src https://*</code></p><p>允许加载任何来源框架</p><p><code>Content-Security-Policy: child-src &#39;none&#39;</code></p><p>对于这种方式来说，只要开发者配置了正确的规则，那么即使网站存在漏洞，攻击者也不能执行它的攻击代码，并且 CSP 的兼容性也不错。</p><h3 id="CSRF-Cross-site-request-forgery-跨站请求伪造"><a href="#CSRF-Cross-site-request-forgery-跨站请求伪造" class="headerlink" title="CSRF: Cross-site request forgery 跨站请求伪造"></a>CSRF: Cross-site request forgery 跨站请求伪造</h3><p>网站诱导用户点击发起请求，用户处于登录态的话，获取cookie信息模拟用户行为</p><p><strong>涉及面试题：什么是 CSRF 攻击？如何防范 CSRF 攻击？</strong></p><p><strong>原理</strong>：攻击者构造后端请求地址，诱导用户点击发起请求。如果用户处于登录态，后端会进行相应的逻辑</p><p>举个例子，网站有一个通过 GET 请求提交用户评论的接口，攻击者可以在钓鱼网站中加入图片，图片地址就是评论接口</p><p><code>&lt;img src=&quot;http://www.domain.com/xxx?comment=&#39;attack&#39;&quot;/&gt;</code></p><p>使用 POST 方式提交请求是不是就没有这个问题了呢？其实并不是，使用这种方式也不是百分百安全的，攻击者同样可以诱导用户进入某个页面，在页面中通过表单提交 POST 请求</p><h4 id="如何防御"><a href="#如何防御" class="headerlink" title="如何防御"></a>如何防御</h4><p>防范 CSRF 的几种规则：Get 请求不对数据修改、不让第三方网站访问到用户 Cookie、阻止第三方网站请求接口、请求时附带验证信息，比如验证码或者 Token</p><h4 id="SameSite"><a href="#SameSite" class="headerlink" title="SameSite"></a>SameSite</h4><p>对 Cookie 设置 SameSite 属性，表示 Cookie 不随着跨域请求发送</p><h4 id="验证-Referer"><a href="#验证-Referer" class="headerlink" title="验证 Referer"></a>验证 Referer</h4><p>通过验证 Referer 判断该请求是否为第三方网站发起</p><h4 id="Token"><a href="#Token" class="headerlink" title="Token"></a>Token</h4><p>服务器下发一个随机 Token，每次发起请求将 Token 携带上，服务器验证 Token</p><h3 id="点击劫持"><a href="#点击劫持" class="headerlink" title="点击劫持"></a>点击劫持</h3><p><strong>涉及面试题：什么是点击劫持？如何防范点击劫持？</strong></p><p>点击劫持是一种视觉欺骗的攻击手段。<br>攻击者将网站通过 iframe 嵌套的方式嵌入自己的网页，并将 iframe 设置为透明，在页面中透出按钮诱导用户点击</p><p>防御方法有两种。</p><h4 id="X-FRAME-OPTIONS"><a href="#X-FRAME-OPTIONS" class="headerlink" title="X-FRAME-OPTIONS"></a>X-FRAME-OPTIONS</h4><p>X-FRAME-OPTIONS 是一个 HTTP 响应头，防御用 iframe 嵌套的点击劫持攻击</p><p>该响应头有三个值可选：</p><ul><li><p>DENY，页面不允许通过 iframe 展示</p></li><li><p>SAMEORIGIN，页面可以在相同域名下通过 iframe 展示</p></li><li><p>ALLOW-FROM，页面可以在指定来源的 iframe 中展示</p></li></ul><h4 id="JS-防御"><a href="#JS-防御" class="headerlink" title="JS 防御"></a>JS 防御</h4><p>远古浏览器只有通过 JS 的方式防御点击劫持</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">&lt;head&gt;</span><br><span class="line">  &lt;style id=<span class="string">&quot;click-jack&quot;</span>&gt;</span><br><span class="line">    html &#123;</span><br><span class="line">      display: none !important;</span><br><span class="line">    &#125;</span><br><span class="line">  &lt;/style&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">  &lt;script&gt;</span><br><span class="line">    <span class="keyword">if</span> (self == top) &#123;</span><br><span class="line">      <span class="keyword">var</span> style = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;click-jack&#x27;</span>)</span><br><span class="line">      <span class="built_in">document</span>.body.removeChild(style)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      top.location = self.location</span><br><span class="line">    &#125;</span><br><span class="line">  &lt;/script&gt;</span><br><span class="line">&lt;/body&gt;</span><br></pre></td></tr></table></figure><p>以上代码的作用就是当通过 iframe 的方式加载页面时，攻击者的网页直接不显示所有内容</p><h3 id="中间人攻击"><a href="#中间人攻击" class="headerlink" title="中间人攻击"></a>中间人攻击</h3><p>涉及面试题：什么是中间人攻击？如何防范中间人攻击？</p><p><strong>定义</strong>：攻击方同时与服务端和客户端建立连接，获得双方的通信信息，还能修改通信信息<br>不建议使用公共 Wi-Fi，因为很可能就会发生中间人攻击</p><p><strong>防御中间人攻击</strong>：增加安全通道来传输信息，例如HTTPS</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(10)浏览器渲染原理</title>
      <link href="2021/03/29/yck/(10)%E6%B5%8F%E8%A7%88%E5%99%A8%E6%B8%B2%E6%9F%93/"/>
      <url>2021/03/29/yck/(10)%E6%B5%8F%E8%A7%88%E5%99%A8%E6%B8%B2%E6%9F%93/</url>
      
        <content type="html"><![CDATA[<h1 id="13"><a href="#13" class="headerlink" title="13."></a>13.</h1><p>渲染原理在面试中答得好，也是一个能与其他候选人拉开差距的一点</p><p>注意：该章节都是一个面试题</p><p><strong>渲染引擎</strong>：Firefox Gecko， Chrome &amp; Safari WebKit</p><h3 id="浏览器接收到-HTML-文件并转换为-DOM-树"><a href="#浏览器接收到-HTML-文件并转换为-DOM-树" class="headerlink" title="浏览器接收到 HTML 文件并转换为 DOM 树"></a>浏览器接收到 HTML 文件并转换为 DOM 树</h3><p>打开网页时，浏览器请求对应的 HTML 文件<br>JS、CSS、HTML 文件是字符串，浏览器接收到0 和 1的字节数据以后，它会将字节数据转换为字符串</p><p><strong>字节数据 =&gt; 字符串 =(词法分析，标记化)=&gt; 标记(Token) =&gt; Node =&gt; DOM Tree</strong></p><h3 id="将-CSS-文件转换为-CSSOM-树"><a href="#将-CSS-文件转换为-CSSOM-树" class="headerlink" title="将 CSS 文件转换为 CSSOM 树"></a>将 CSS 文件转换为 CSSOM 树</h3><p><strong>字节数据 =&gt; 字符串 =(词法分析，标记化)=&gt; 标记(Token) =&gt; Node =&gt;     CSSOM</strong></p><p>生成渲染树</p><p>在这一过程中，不是简单的将两者合并就行了。渲染树只会包括需要显示的节点和这些节点的样式信息，如果某个节点是 display: none 的，那么就不会在渲染树中显示。</p><p>当浏览器生成渲染树以后，就会根据渲染树来进行布局（也可以叫做回流），然后调用 GPU 绘制，合成图层，显示在屏幕上。对于这一部分的内容因为过于底层，还涉及到了硬件相关的知识，这里就不再继续展开内容了。</p><p>那么通过以上内容，我们已经详细了解到了浏览器从接收文件到将内容渲染在屏幕上的这一过程。接下来，我们将会来学习上半部分遗留下来的一些知识点。</p><h3 id="为什么操作-DOM-慢"><a href="#为什么操作-DOM-慢" class="headerlink" title="为什么操作 DOM 慢"></a>为什么操作 DOM 慢</h3><p>因为 DOM 是属于渲染引擎的东西，而 JS 是 JS 引擎的东西。</p><p>JS 操作 DOM 涉及两个线程间的通信，势必带来性能损耗。</p><p>操作 DOM 次数多就等同于一直在进行线程间的通信，并且操作 DOM 会带来重绘回流<br>所以导致性能问题</p><p><strong>经典面试题：插入几万个 DOM，如何实现页面不卡顿？</strong></p><p>首先肯定不能一次性全部插入，重点应该是如何分批次部分渲染 DOM</p><p>1.requestAnimationFrame循环的插入 DOM</p><p>2.虚拟滚动（virtualized scroller）</p><p>原理：只渲染可视区域内容，非可见区域完全不渲染，用户滚动实时替换渲染的内容</p><h3 id="什么情况阻塞渲染"><a href="#什么情况阻塞渲染" class="headerlink" title="什么情况阻塞渲染"></a>什么情况阻塞渲染</h3><p>渲染的前提是生成渲染树，所以 HTML 和 CSS 肯定会阻塞渲染</p><p>1.如果想渲染快，应该降低一开始需要渲染的文件大小，并且扁平层级，优化选择器</p><p>2.当浏览器解析 script 标签时，会暂停构建 DOM。如果想首屏渲染得快，就不应该在首屏加载 JS 文件，这也是建议将 script 放在 body 标签底部的原因</p><p>除了放在底部，还可以给 script 标签添加 defer 或者 async 属性</p><p>defer 属性表示JS 文件会并行下载，但是会在 HTML 解析完成后顺序执行</p><p>async 属性表示 JS 文件下载和解析不会阻塞渲染</p><h3 id="重绘（Repaint）和回流（Reflow）"><a href="#重绘（Repaint）和回流（Reflow）" class="headerlink" title="重绘（Repaint）和回流（Reflow）"></a>重绘（Repaint）和回流（Reflow）</h3><p>重绘和回流会在设置节点样式时频繁出现，同时影响性能</p><p>重绘：节点需要更改外观而不影响布局，比如改变 color </p><p>回流：布局或者几何属性需要改变</p><p>回流必定重绘，重绘不一定回流，回流所需的成本比重绘高的多，改变子节点可能导致父节点回流</p><p>以下动作会导致性能问题：<br>改变 window 大小\字体，添加或删除样式，文字改变，定位或者浮动，盒模型</p><p><strong>很多人不知道，重绘和回流和 Eventloop 有关</strong>：</p><p>1.Eventloop 执行完 Microtasks 后，会判断 document 是否更新（浏览器是 60Hz 的刷新率，每 16.6ms 才会更新一次）</p><p>2.然后判断是否有 resize 或者 scroll 事件，有的话触发事件，所以  resize 和 scroll 事件也是至少 16ms 才触发一次，并自带节流功能</p><p>3.判断是否触发了 media query</p><p>4.更新动画并且发送事件</p><p>5.判断是否有全屏操作事件</p><p>6.执行 requestAnimationFrame 回调</p><p>7.执行 IntersectionObserver 回调，该方法用于判断元素是否可见，可以用于懒加载<br>8.更新界面</p><p>9.以上是一帧中可能会做的事情。如果在一帧中有空闲时间，就会执行 requestIdleCallback </p><h3 id="减少重绘和回流"><a href="#减少重绘和回流" class="headerlink" title="减少重绘和回流"></a>减少重绘和回流</h3><p>1.使用 transform 替代 top</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">&lt;div <span class="class"><span class="keyword">class</span></span>=<span class="string">&quot;test&quot;</span>&gt;&lt;/div&gt;&lt;style&gt;   </span><br><span class="line">.test &#123;     </span><br><span class="line">  position: absolute;     </span><br><span class="line">  top: 10px;     </span><br><span class="line">  width: 100px;     </span><br><span class="line">  height: 100px;     </span><br><span class="line">  background: red;   </span><br><span class="line">&#125;</span><br><span class="line">&lt;/style&gt;</span><br><span class="line"></span><br><span class="line">&lt;script&gt;   </span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;     <span class="comment">// 引起回流     </span></span><br><span class="line">  <span class="built_in">document</span>.querySelector(<span class="string">&#x27;.test&#x27;</span>).style.top = <span class="string">&#x27;100px&#x27;</span></span><br><span class="line">  &#125;, <span class="number">1000</span>)</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure><p>2.使用 visibility 替换 display: none ，因为前者只引起重绘，后者引发回流（改变了布局）</p><p>3.不要把节点属性值放在循环里当成循环里的变量</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">1000</span>; i++) &#123;     </span><br><span class="line">  <span class="comment">// 获取 offsetTop 会导致回流，因为需要去获取正确的值     </span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">document</span>.querySelector(<span class="string">&#x27;.test&#x27;</span>).style.offsetTop) </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>4.不要使用 table 布局，可能造成整个 table 的重新布局</p><p>5.动画实现的速度选择，速度越快，回流越多，可以选择使用 requestAnimationFrame</p><p>6.CSS 选择符从右往左匹配查找，避免节点层级过多</p><p>7.将频繁重绘或者回流的节点设置为图层，图层能阻止该节点的渲染行为影响别的节点。比如对于 video 标签，浏览器会自动将该节点变为图层</p><p><strong>设置节点为图层的方式</strong>：通过以下几个属性生成新图层</p><p>will-change</p><p>video、iframe 标签</p><h3 id="思考题：不考虑缓存和优化网络协议的前提下，通过哪些方式最快渲染页面，也就是常说的关键渲染路径，这部分也是性能优化中的一块内容。"><a href="#思考题：不考虑缓存和优化网络协议的前提下，通过哪些方式最快渲染页面，也就是常说的关键渲染路径，这部分也是性能优化中的一块内容。" class="headerlink" title="思考题：不考虑缓存和优化网络协议的前提下，通过哪些方式最快渲染页面，也就是常说的关键渲染路径，这部分也是性能优化中的一块内容。"></a>思考题：不考虑缓存和优化网络协议的前提下，通过哪些方式最快渲染页面，也就是常说的关键渲染路径，这部分也是性能优化中的一块内容。</h3><p>怎么测量有没有加快渲染速度呢</p><p>发生 DOMContentLoaded 事件后会生成渲染树，生成渲染树就可以进行渲染了，这一过程更大程度上和硬件有关系了</p><p>提示如何加速：<br>从文件大小、script 标签使用、CSS、HTML 的代码书写、下载内容是否需要在首屏使用来考虑</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(9)浏览器缓存机制</title>
      <link href="2021/03/29/yck/(9)%E6%B5%8F%E8%A7%88%E5%99%A8%E7%BC%93%E5%AD%98/"/>
      <url>2021/03/29/yck/(9)%E6%B5%8F%E8%A7%88%E5%99%A8%E7%BC%93%E5%AD%98/</url>
      
        <content type="html"><![CDATA[<h3 id="12"><a href="#12" class="headerlink" title="12."></a>12.</h3><p>注意：该知识点属于性能优化领域，并且整一章节都是一个面试题。</p><p>缓存是性能优化中简单高效的一种方式，可以显著减少网络传输所带来的损耗</p><p>对于数据请求来说，可以分为发起网络请求、后端处理、浏览器响应三个步骤<br>浏览器缓存帮助我们在第一和第三步骤中优化性能</p><p>比如说直接使用缓存而不发起请求，或者发起了请求但后端存储的数据和前端一致，那么就没有必要再将数据回传回来，这样就减少了响应数据。</p><h4 id="缓存位置"><a href="#缓存位置" class="headerlink" title="缓存位置"></a>缓存位置</h4><p>从缓存位置来说分为四种，并且有优先级，当依次查找缓存且都没有命中的时候才请求网络</p><p>Service Worker</p><p>Memory Cache</p><p>Disk Cache</p><p>Push Cache</p><p>网络请求</p><h4 id="Service-Worker"><a href="#Service-Worker" class="headerlink" title="Service Worker"></a>Service Worker</h4><p>Service Worker 的缓存与其他缓存机制不同，它可以自由控制缓存哪些文件、如何匹配缓存、如何读取缓存，并且缓存是持续性的。</p><p>Service Worker 没有命中缓存的时候，调用 fetch 函数获取数据</p><p>如果没有在 Service Worker 命中缓存的话，会根据缓存查找优先级去查找数据</p><p>但是不管从 Memory Cache 还是从网络请求中获取数据，浏览器都显示从 Service Worker 中获取的内容</p><h4 id="Memory-Cache"><a href="#Memory-Cache" class="headerlink" title="Memory Cache"></a>Memory Cache</h4><p>读取内存中的数据比磁盘快，可是持续性短，会随着进程的释放而释放。 一旦关闭 Tab 页面，内存的缓存也就被释放了。</p><h4 id="Disk-Cache"><a href="#Disk-Cache" class="headerlink" title="Disk Cache"></a>Disk Cache</h4><p>读取速度慢点，但是比之 Memory Cache 胜在容量和存储时效性上</p><p>在所有浏览器缓存中，Disk Cache 覆盖面最大</p><p>它会根据 HTTP Header 的字段判断哪些资源需要缓存，哪些资源可以不请求直接使用，哪些资源已经过期需要重新请求。并且即使在跨站点的情况下，相同地址的资源一旦被硬盘缓存下来，就不会再次去请求数据</p><h4 id="Push-Cache"><a href="#Push-Cache" class="headerlink" title="Push Cache"></a>Push Cache</h4><p>Push Cache 是 HTTP/2 中的内容，当以上三种缓存都没有命中时，它才会被使用。并且缓存时间也很短暂，只在会话（Session）中存在，一旦会话结束就被释放。</p><p>所有的资源都能被推送，但是 Edge 和 Safari 浏览器兼容性不好</p><p>可以推送 no-cache 和 no-store 的资源</p><p>一旦连接被关闭，Push Cache 就被释放</p><p>多个页面可以使用相同的 HTTP/2 连接，也就是使用同样的缓存</p><p>Push Cache 的缓存只能被使用一次</p><p>浏览器可以拒绝接受已经存在的资源推送</p><p>你可以给其他域名推送资源</p><h4 id="缓存策略"><a href="#缓存策略" class="headerlink" title="缓存策略"></a>缓存策略</h4><p>通常缓存策略分为两种：强缓存和协商缓存，缓存策略通过设置 HTTP Header 实现</p><h4 id="强缓存"><a href="#强缓存" class="headerlink" title="强缓存"></a>强缓存</h4><p>强缓存通过设置两种 HTTP Header 实现：Expires 和 Cache-Control </p><p>强缓存表示在缓存期间不需要请求，state code 为 200</p><p>Expires<br>Expires: Wed, 22 Oct 2018 08:41:00 GMT</p><p>Expires 是 HTTP/1 的产物，表示资源会在 22 Oct 2018 08:41:00 后过期，需要再次请求。</p><p>Expires 受限于本地时间，如果修改了本地时间，可能会造成缓存失效</p><p>Cache-control</p><p>Cache-control: max-age=30</p><p>Cache-Control 出现于 HTTP/1.1，优先级高于 Expires </p><p>该属性值表示资源会在 30 秒后过期，需要再次请求</p><p>Cache-Control 可以在请求头或者响应头中设置，并且可以组合使用多种指令</p><h4 id="协商缓存"><a href="#协商缓存" class="headerlink" title="协商缓存"></a>协商缓存</h4><p>如果缓存过期了，就需要发起请求验证资源是否更新</p><p>协商缓存通过设置两种 HTTP Header 实现：Last-Modified 和 ETag </p><p>当浏览器请求验证资源时，如果资源没改变，服务端返回 304 状态码，并更新缓存有效期</p><p>Last-Modified 和 If-Modified-Since</p><p>Last-Modified 表示本地文件最后修改日期，If-Modified-Since 会将 Last-Modified 的值发送给服务器，询问服务器在该日期后资源是否有更新，有更新的话就会将新的资源发送回来，否则返回 304 状态码。</p><p>Last-Modified 的弊端：</p><p>如果本地打开缓存文件，即使没有修改， Last-Modified还是会被修改，服务端不能命中缓存导致发送相同的资源</p><p> Last-Modified 以秒计时，如果在不可感知的时间内修改文件，服务端会认为资源还是命中了，不会返回正确的资源</p><p>因为以上弊端，在 HTTP / 1.1 出现了 ETag </p><p>ETag 和 If-None-Match</p><p>ETag 类似于文件指纹，If-None-Match 会将当前 ETag 发送给服务器，询问该资源 ETag 是否变动，有变动的话就将新的资源发送回来。并且 ETag 优先级比 Last-Modified 高。</p><p>如果缓存策略没设置，浏览器会怎么处理？</p><p>浏览器会采用一个启发式的算法，（响应头的 Date  -  Last-Modified 的 10% ）作为缓存时间</p><h4 id="实际场景应用缓存策略"><a href="#实际场景应用缓存策略" class="headerlink" title="实际场景应用缓存策略"></a>实际场景应用缓存策略</h4><p>频繁变动的资源</p><p>首先使用 Cache-Control: no-cache 使浏览器每次都请求服务器，然后配合 ETag 或者 Last-Modified 来验证资源是否有效。这样的做法虽然不能节省请求数量，但是能显著减少响应数据大小。</p><p>代码文件</p><p>HTML 文件一般不缓存或者缓存时间很短</p><p>现在使用工具来打包代码，可以对文件名进行哈希处理，代码修改后才会生成新的文件名<br>给代码文件设置缓存有效期一年 Cache-Control: max-age=31536000<br>只有当 HTML 文件引入的文件名发生改变才会下载最新的代码文件，否则使用缓存</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(8)浏览器基础知识点及常考面试题</title>
      <link href="2021/03/29/yck/(8)%E6%B5%8F%E8%A7%88%E5%99%A8%E5%9F%BA%E7%A1%80/"/>
      <url>2021/03/29/yck/(8)%E6%B5%8F%E8%A7%88%E5%99%A8%E5%9F%BA%E7%A1%80/</url>
      
        <content type="html"><![CDATA[<h1 id="11"><a href="#11" class="headerlink" title="11."></a>11.</h1><h2 id="事件机制"><a href="#事件机制" class="headerlink" title="事件机制"></a>事件机制</h2><p>涉及面试题：事件的触发过程是怎么样的？知道什么是事件代理嘛？</p><p>事件是文档和浏览器窗口中发生的特定的交互瞬间</p><p>IE的事件流是冒泡流(event bubbling)：事开始时由具体元素接收，逐级向上传播到不具体的节点</p><p>Netscape是捕获流(event capturing)：不具体节点向下传播至具体元素</p><p>事件触发有三个阶段：</p><p>1.window 往事件触发处传播，遇到注册的捕获事件会触发</p><p>2.传播到事件触发处时触发注册的事件</p><p>3.从事件触发处往 window 传播，遇到注册的冒泡事件会触发</p><p>特例，如果给 body 的子节点同时注册冒泡和捕获事件，事件触发会按照注册的顺序执行</p><p>注册事件</p><p>通常使用 addEventListener 注册事件，第三个参数可以是布尔值或对象</p><p>对于布尔值 useCapture 参数，默认值为 false ，useCapture 决定事件是捕获还是冒泡</p><p>对于对象参数，可以使用以下几个属性</p><p>capture：布尔值，和 useCapture 作用一样</p><p>once：布尔值，值为 true 表示该回调只会调用一次，调用后移除监听</p><p>passive：布尔值，表示永远不会调用 preventDefault</p><p>如果希望事件只触发在目标上，可以使用 stopPropagation 阻止事件的进一步传播</p><p>stopPropagation 可以用来阻止事件冒泡和捕获</p><p>stopImmediatePropagation 同样也能阻止事件，还能阻止该事件目标执行别的注册事件</p><p><strong>事件代理：</strong><br>例如：li的事件注册在ul上</p><p>如果节点中的子节点是动态生成的，那么子节点的事件应该注册在父节点上</p><p>事件代理相较于直接注册的优点：节省内存、不需要注销事件</p><h2 id="跨域"><a href="#跨域" class="headerlink" title="跨域"></a>跨域</h2><p>涉及面试题：什么是跨域？为什么浏览器要使用同源策略？有几种方式解决跨域问题？了解预检请求嘛</p><h4 id="同源策略"><a href="#同源策略" class="headerlink" title="同源策略"></a>同源策略</h4><p>如果<strong>协议、域名或者端口</strong>有一个不同就是跨域，Ajax 请求会失败</p><p>因为浏览器出于安全考虑，有同源策略</p><p>具体的安全考虑： 防止 CSRF 攻击（Cross-site request forgery，利用用户的登录态发起恶意请求）</p><p>没有同源策略，A 网站可以被任意其他来源的 Ajax 访问到内容；如果 A 网站存在登录态，对方可以通过 Ajax 获得你的任何信息</p><p>跨域的请求其实已经发出去了，但是浏览器拦截了响应。</p><p>通过表单的方式可以发起跨域请求，为什么 Ajax 就不会?</p><p>因为跨域是为了阻止用户读取到另一个域名下的内容，Ajax 可以获取响应，浏览器认为这不安全，所以拦截了响应。但是表单并不会获取新的内容，所以可以发起跨域请求。</p><p>同时也说明了跨域并不能完全阻止 CSRF，因为请求毕竟是发出去了</p><h4 id="解决跨域问题的方式"><a href="#解决跨域问题的方式" class="headerlink" title="解决跨域问题的方式"></a>解决跨域问题的方式</h4><h4 id="JSONP"><a href="#JSONP" class="headerlink" title="JSONP"></a>JSONP</h4><p>利用<code>script</code>标签没有跨域限制的漏洞，指向需要访问的地址并提供回调函数来接收数据<br>JSONP 使用简单且兼容性不错，但是只限于 get 请求</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">jsonp</span>(<span class="params">url, jsonpCallback, success</span>) </span>&#123;   </span><br><span class="line">  <span class="keyword">let</span> script = <span class="built_in">document</span>.createElement(<span class="string">&#x27;script&#x27;</span>)   </span><br><span class="line">  script.src = url   </span><br><span class="line">  script.async = <span class="literal">true</span>   </span><br><span class="line">  script.type = <span class="string">&#x27;text/javascript&#x27;</span>   </span><br><span class="line">  <span class="built_in">window</span>[jsonpCallback] = <span class="function"><span class="keyword">function</span>(<span class="params">data</span>) </span>&#123;     </span><br><span class="line">    success &amp;&amp; success(data)   </span><br><span class="line">  &#125;   </span><br><span class="line">  <span class="built_in">document</span>.body.appendChild(script) &#125; </span><br><span class="line">  jsonp(<span class="string">&#x27;http://xxx&#x27;</span>, <span class="string">&#x27;callback&#x27;</span>, <span class="function"><span class="keyword">function</span>(<span class="params">value</span>) </span>&#123;   </span><br><span class="line">    <span class="built_in">console</span>.log(value) </span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><h4 id="CORS"><a href="#CORS" class="headerlink" title="CORS"></a>CORS</h4><p>服务端设置 Access-Control-Allow-Origin 开启 CORS</p><p>该属性表示哪些域名可以访问资源，如果设置通配符则表示所有网站都可以访问资源。</p><p>通过这种方式解决跨域问题的话，会在发送请求时出现两种情况，分别为简单请求和复杂请求</p><h4 id="简单请求"><a href="#简单请求" class="headerlink" title="简单请求"></a>简单请求</h4><p>以 Ajax 为例，当满足以下条件时，会触发简单请求</p><p>使用下列方法之一：GET\HEAD\POST</p><p>Content-Type 的值仅限于下列三者之一：<br>text/plain、<br>multipart/form-data、<br>application/x-www-form-urlencoded、</p><p>请求中的任意 XMLHttpRequestUpload 对象均没有注册任何事件监听器</p><p>XMLHttpRequestUpload 对象可以使用 XMLHttpRequest.upload 属性访问</p><h4 id="复杂请求"><a href="#复杂请求" class="headerlink" title="复杂请求"></a>复杂请求</h4><p>首先发起预检请求，该请求是 option 方法，通过该请求知道服务端是否允许跨域请求<br>对于预检请求，Node 设置 CORS会遇到一个坑</p><p>以 express 框架举例：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">app.use(<span class="function">(<span class="params">req, res, next</span>) =&gt;</span> &#123;   </span><br><span class="line">  res.header(<span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span>, <span class="string">&#x27;*&#x27;</span>)   </span><br><span class="line">  res.header(<span class="string">&#x27;Access-Control-Allow-Methods&#x27;</span>, <span class="string">&#x27;PUT, GET, POST, DELETE, OPTIONS&#x27;</span>)   </span><br><span class="line">  res.header(     </span><br><span class="line">    <span class="string">&#x27;Access-Control-Allow-Headers&#x27;</span>,     </span><br><span class="line">    <span class="string">&#x27;Origin, X-Requested-With, Content-Type, Accept, Authorization, Access-Control-Allow-Credentials&#x27;</span>)   </span><br><span class="line">  next() &#125;)</span><br></pre></td></tr></table></figure><p>该请求会验证你的 Authorization 字段，没有的话就会报错。</p><p>当前端发起了复杂请求后，你会发现就算你代码是正确的，返回结果也永远是报错的。因为预检请求也会进入回调中，也会触发 next 方法，因为预检请求并不包含 Authorization 字段，所以服务端会报错。</p><p>想解决这个问题很简单，只需要在回调中过滤 option 方法即可</p><p><code>res.statusCode = 204res.setHeader(&#39;Content-Length&#39;, &#39;0&#39;) res.end()</code></p><p>document.domain</p><p>该方式只能用于二级域名相同的情况下，比如 a.test.com 和 b.test.com 适用于该方式。</p><p>只需要给页面添加 document.domain = ‘test.com’ 表示二级域名都相同就可以实现跨域</p><p>postMessage</p><p>这种方式通常用于获取嵌入页面中的第三方页面数据。一个页面发送消息，另一个页面判断来源并接收消息</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 发送消息端</span></span><br><span class="line"><span class="built_in">window</span>.parent.postMessage(<span class="string">&#x27;message&#x27;</span>, <span class="string">&#x27;http://test.com&#x27;</span>)</span><br><span class="line"><span class="comment">// 接收消息端</span></span><br><span class="line"><span class="keyword">var</span> mc = <span class="keyword">new</span> MessageChannel() </span><br><span class="line">mc.addEventListener(<span class="string">&#x27;message&#x27;</span>, <span class="function"><span class="params">event</span> =&gt;</span> &#123;   </span><br><span class="line">  <span class="keyword">var</span> origin = event.origin || event.originalEvent.origin  </span><br><span class="line">  <span class="keyword">if</span> (origin === <span class="string">&#x27;http://test.com&#x27;</span>) &#123;     </span><br><span class="line">     <span class="built_in">console</span>.log(<span class="string">&#x27;验证通过&#x27;</span>)   </span><br><span class="line">  &#125; </span><br><span class="line">&#125;)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="存储"><a href="#存储" class="headerlink" title="存储"></a>存储</h3><p>涉及面试题：有几种方式实现存储功能，分别有什么优缺点？什么是 Service Worker？</p><h4 id="cookie，localStorage，sessionStorage，indexDB"><a href="#cookie，localStorage，sessionStorage，indexDB" class="headerlink" title="cookie，localStorage，sessionStorage，indexDB"></a>cookie，localStorage，sessionStorage，indexDB</h4><p>cookie 已经不建议用于存储</p><p>如果没有大量存储需求，可以使用 localStorage 和 sessionStorage </p><p>对于不怎么改变的数据尽量使用 localStorage ，否则用 sessionStorage </p><p>cookie安全性</p><p>value：保存登录态的话要加密</p><p>http-only：不能通过js访问，减少xss(Cross Site Scripting) 跨站脚本攻击</p><p>xss：使用js程序盗用cookie</p><p>secure：智能在HTTPS协议的请求中携带</p><p>same-site：规定浏览器不能在跨域请求中携带Cookie，减少CSRF攻击</p><h4 id="Service-Worker"><a href="#Service-Worker" class="headerlink" title="Service Worker"></a>Service Worker</h4><p>Service Worker 是运行在浏览器背后的独立线程，一般用来实现缓存功能</p><p>传输协议必须为 HTTPS，因为 Service Worker 涉及请求拦截，所以保障安全</p><p>Service Worker 实现缓存的三个步骤：</p><p>1.注册 Service Worker</p><p>2.监听到 install 事件以后缓存文件</p><p>3.下次访问的时候通过拦截请求的方式查询是否存在缓存，存在则读取缓存，否则请求数据</p><p>源码待续。。。</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(7)JS 思考题</title>
      <link href="2021/03/29/yck/(7)JS%E6%80%9D%E8%80%83%E9%A2%98/"/>
      <url>2021/03/29/yck/(7)JS%E6%80%9D%E8%80%83%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="10"><a href="#10" class="headerlink" title="10."></a>10.</h1><h3 id="思考题一：JS-分为哪两大类型？都有什么各自的特点？你该如何判断正确的类型？"><a href="#思考题一：JS-分为哪两大类型？都有什么各自的特点？你该如何判断正确的类型？" class="headerlink" title="思考题一：JS 分为哪两大类型？都有什么各自的特点？你该如何判断正确的类型？"></a>思考题一：JS 分为哪两大类型？都有什么各自的特点？你该如何判断正确的类型？</h3><p>1.对于原始类型来说，可以指出 null 和 number 存在的问题。</p><p>对于对象类型来说，可以从垃圾回收的角度去切入，也可以说一下对象类型存在深浅拷贝的问题</p><p>2.对于判断类型来说，可以去对比 typeof 和 instanceof 的区别</p><p>可以指出 instanceof 判断类型不是完全准确。</p><h3 id="思考题二：你理解的原型是什么？"><a href="#思考题二：你理解的原型是什么？" class="headerlink" title="思考题二：你理解的原型是什么？"></a>思考题二：你理解的原型是什么？</h3><p>起码说出原型小节中的总结内容，然后指出一些小点，比如不是所有函数都有 prototype 属性</p><p>然后引申出原型链的概念，提出如何使用原型实现继承，继而可以引申出 ES6 中的 class 实现继承</p><h3 id="思考题三：bind、call-和-apply-各自有什么区别？"><a href="#思考题三：bind、call-和-apply-各自有什么区别？" class="headerlink" title="思考题三：bind、call 和 apply 各自有什么区别？"></a>思考题三：bind、call 和 apply 各自有什么区别？</h3><p>首先说出三者的不同，如果自己实现过其中的函数，可以说出自己的思路</p><p>然后聊一聊 this 的内容，有几种规则判断 this 到底是什么，this 规则会涉及到 new，那么最后可以说下自己对于 new 的理解</p><h3 id="思考题四：ES6-中有使用过什么？"><a href="#思考题四：ES6-中有使用过什么？" class="headerlink" title="思考题四：ES6 中有使用过什么？"></a>思考题四：ES6 中有使用过什么？</h3><p>思路引导：</p><p>比如说说 class，那么 class 又可以拉回到原型的问题</p><p>可以说说 promise，那么线就被拉到了异步的内容</p><p>可以说说 proxy，那么如果你使用过 Vue 这个框架，就可以谈谈响应式原理的内容</p><p>同样可以说说 let 这些声明变量的语法，可以谈及与 var 的不同，说到提升的内容</p><h3 id="思考题五：JS-是如何运行的？"><a href="#思考题五：JS-是如何运行的？" class="headerlink" title="思考题五：JS 是如何运行的？"></a>思考题五：JS 是如何运行的？</h3><p>可以先说 JS 是单线程运行，说说你理解的线程和进程的区别。</p><p>然后讲到执行栈，接下来的内容就是涉及 Eventloop 了，微任务和宏任务的区别，哪些是微任务，哪些又是宏任务</p><p>还可以谈及浏览器和 Node 的 Eventloop 的不同，最后聊一聊 JS 中的垃圾回收</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(6)JS 进阶知识点及常考面试题</title>
      <link href="2021/03/29/yck/(6)JS%E8%BF%9B%E9%98%B6/"/>
      <url>2021/03/29/yck/(6)JS%E8%BF%9B%E9%98%B6/</url>
      
        <content type="html"><![CDATA[<h1 id="8"><a href="#8" class="headerlink" title="8."></a>8.</h1><h3 id="手写-call、apply-及-bind-函数"><a href="#手写-call、apply-及-bind-函数" class="headerlink" title="手写 call、apply 及 bind 函数"></a>手写 call、apply 及 bind 函数</h3><p><strong>涉及面试题：call、apply 及 bind 函数内部实现是怎么样的？</strong><br><strong>用法</strong>：都是用于改变函数体内this的指向<br><strong>bind</strong>: 返回新函数, this指向第一个参数obj，后面的参数传入原函数<br><code>var testObj = test.bind(obj, ...);</code><br>使用new 操作符调用绑定函数时，该参数无效<br><strong>apply和call的调用返回函数执行结果</strong><br><strong>apply</strong>：<br><code>fun.apply(thisArg, [argsArray])</code><br><strong>call</strong>：<br><code>fun.call(thisArg, arg1, arg2, ...)</code></p><p><strong>总结</strong><br>如果传递的参数不多，使用<code>fn.call(thisObj, arg1, arg2 ...)</code><br>如果传递的参数很多，调用<code>fn.apply(thisObj, [arg1, arg2 ...])</code><br>如果新函数长期绑定某个函数给某个对象使用，则使用<br><code>const newFn = fn.bind(thisObj);</code><br><code>newFn(arg1, arg2...)</code></p><h4 id="call"><a href="#call" class="headerlink" title="call"></a>call</h4><p>不传入第一个参数，那么上下文默认为 window<br>改变了 this 指向，让新的对象可以执行该函数，并能接受参数</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Function</span>.prototype.myCall = <span class="function"><span class="keyword">function</span>(<span class="params">context</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> <span class="built_in">this</span> !== <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">TypeError</span>(<span class="string">&#x27;Error&#x27;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  context = context || <span class="built_in">window</span></span><br><span class="line">  context.fn = <span class="built_in">this</span></span><br><span class="line">  <span class="keyword">const</span> args = [...arguments].slice(<span class="number">1</span>)</span><br><span class="line">  <span class="keyword">const</span> result = context.fn(...args)</span><br><span class="line">  <span class="keyword">delete</span> context.fn</span><br><span class="line">  <span class="keyword">return</span> result</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>首先 context 为可选参数，如果不传的话默认上下文为 window</p><p>接下来给 context 创建 fn 属性，并将值设置为需要调用的函数</p><p>因为 call 可以传入多个参数作为调用函数的参数，所以需要将参数剥离出来</p><p>然后调用函数并将对象上的函数删除</p><p>以上就是实现 call 的思路，apply 的实现也类似，区别在于对参数的处理，所以就不一一分析思路了</p><h4 id="apply"><a href="#apply" class="headerlink" title="apply"></a>apply</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Function</span>.prototype.myApply = <span class="function"><span class="keyword">function</span>(<span class="params">context</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> <span class="built_in">this</span> !== <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">TypeError</span>(<span class="string">&#x27;Error&#x27;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  context = context || <span class="built_in">window</span></span><br><span class="line">  context.fn = <span class="built_in">this</span></span><br><span class="line">  <span class="keyword">let</span> result</span><br><span class="line">  <span class="comment">// 处理参数和 call 有区别</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">arguments</span>[<span class="number">1</span>]) &#123;</span><br><span class="line">    result = context.fn(...arguments[<span class="number">1</span>])</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    result = context.fn()</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">delete</span> context.fn</span><br><span class="line">  <span class="keyword">return</span> result</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="bind"><a href="#bind" class="headerlink" title="bind"></a>bind</h4><p>bind 的实现对比其他两个函数略微地复杂了一点，因为 bind 需要返回一个函数，需要判断一些边界问题</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Function</span>.prototype.myBind = <span class="function"><span class="keyword">function</span> (<span class="params">context</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> <span class="built_in">this</span> !== <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">TypeError</span>(<span class="string">&#x27;Error&#x27;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">const</span> _this = <span class="built_in">this</span></span><br><span class="line">  <span class="keyword">const</span> args = [...arguments].slice(<span class="number">1</span>)</span><br><span class="line">  <span class="comment">// 返回一个函数</span></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> <span class="title">F</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 因为返回了一个函数，我们可以 new F()，所以需要判断</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span> <span class="keyword">instanceof</span> F) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> _this(...args, ...arguments)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> _this.apply(context, args.concat(...arguments))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>以下是对实现的分析：</p><p>bind 返回一个函数</p><p>函数有两种方式调用，直接调用，一种是通过 new 的方式，我们先来说直接调用的方式</p><p>对于直接调用来说，这里选择apply </p><p>对于参数需要注意以下情况：因为 bind 可以实现 f.bind(obj, 1)(2)</p><p>所以需要将两边的参数拼接起来，于是就有了这样的实现 args.concat(…arguments)</p><p>通过 new 的方式，new 不会被任何方式改变 this，所以需要忽略传入的 this</p><h3 id="new"><a href="#new" class="headerlink" title="new"></a>new</h3><p><strong>涉及面试题：new 的原理是什么？通过 new 的方式创建对象和通过字面量创建有什么区别？</strong></p><p>调用 new 的过程：生成对象、链接到原型、绑定 this、返回对象</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">create</span>(<span class="params"></span>) </span>&#123;   </span><br><span class="line">    <span class="comment">// 1.create empty obj   </span></span><br><span class="line">    <span class="keyword">let</span> obj = &#123;&#125;    </span><br><span class="line">    <span class="comment">// 2.get Constructor   </span></span><br><span class="line">    <span class="keyword">let</span> Con = [].shift.call(<span class="built_in">arguments</span>)  <span class="comment">// arguments: array[func]   </span></span><br><span class="line">    <span class="comment">// 3.set prototype of empty obj </span></span><br><span class="line">    obj.__proto__ = Con.prototype</span><br><span class="line">    <span class="comment">// 4.bind &quot;this&quot; &amp; call Constructor   </span></span><br><span class="line">    <span class="keyword">let</span> result = Con.apply(obj, <span class="built_in">arguments</span>)</span><br><span class="line">    <span class="comment">// 5.determine wherther the value of return is Object   </span></span><br><span class="line">    <span class="keyword">return</span> result <span class="keyword">instanceof</span> <span class="built_in">Object</span> ? result : obj &#125;</span><br></pre></td></tr></table></figure><p>对于创建对象来说，更推荐使用字面量的方式创建对象（无论性能上还是可读性）</p><p>因为使用 new Object() 创建对象需要通过作用域链一层层找到 Object，但使用字面量的方式就没这个问题</p><h3 id="instanceof-的原理"><a href="#instanceof-的原理" class="headerlink" title="instanceof 的原理"></a>instanceof 的原理</h3><p>instanceof 可以判断对象类型，内部机制是通过判断对象原型链中是不是能找到类型的prototype</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">myInstanceof</span>(<span class="params">left, right</span>) </span>&#123;   </span><br><span class="line">    <span class="keyword">let</span> prototype = right.prototype   </span><br><span class="line">    left = left.__proto__   </span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">true</span>) &#123;     </span><br><span class="line">        <span class="keyword">if</span> (left === <span class="literal">null</span> || left === <span class="literal">undefined</span>)       </span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>     </span><br><span class="line">        <span class="keyword">if</span> (prototype === left)       </span><br><span class="line">            <span class="keyword">return</span> trueleft = left.__proto__   </span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="为什么-0-1-0-2-0-3"><a href="#为什么-0-1-0-2-0-3" class="headerlink" title="为什么 0.1 + 0.2 != 0.3"></a>为什么 0.1 + 0.2 != 0.3</h3><p>涉及面试题：为什么 0.1 + 0.2 != 0.3？如何解决这个问题？</p><p>因为0.1 在二进制中无限循环，JS采用的浮点数标准会裁剪掉这种数字，造成<strong>精度丢失</strong></p><p><strong>解决方法</strong>：parseFloat((0.1 + 0.2).toFixed(10)) === 0.3 // true</p><h3 id="垃圾回收机制-Garbage-collection"><a href="#垃圾回收机制-Garbage-collection" class="headerlink" title="垃圾回收机制  Garbage collection"></a>垃圾回收机制  Garbage collection</h3><p>涉及面试题：V8 下的垃圾回收机制是怎么样的？</p><p>V8 实现了准确式 GC，GC 算法采用了分代式GC机制，因此，V8 将内存（堆）分为新生代和老生代两部分</p><p><strong>新生代算法</strong></p><p>特点是对象存活短，新生代的空间分为 From 和 To ，一个使用，另一个空闲</p><p>新对象被放入 From，当 From 被占满时，GC启动，检查 From 存活的对象，并复制到 To 中，失活对象销毁</p><p>当复制完成后将 From 和 To 互换，GC结束</p><p><strong>老生代算法</strong></p><p>存活时间长且数量多，使用了两个算法，分别是标记清除算法和标记压缩算法</p><p>什么情况下对象会出现在老生代空间中：</p><p>新生代的对象已经经历过一次 Scavenge 算法</p><p>To 的对象占比大小超过 25 %</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(5)Event Loop</title>
      <link href="2021/03/29/yck/(5)EventLoop/"/>
      <url>2021/03/29/yck/(5)EventLoop/</url>
      
        <content type="html"><![CDATA[<h1 id="7"><a href="#7" class="headerlink" title="7."></a>7.</h1><h3 id="进程与线程"><a href="#进程与线程" class="headerlink" title="进程与线程"></a>进程与线程</h3><p>涉及面试题：进程与线程区别？JS 单线程带来的好处？</p><p><strong>CPU 工作时间片的一个描述</strong></p><p>进程：描述了 CPU 在运行指令及加载和保存上下文所需的时间，代表一个程序</p><p>线程：进程中更小的单位，描述了执行一段指令所需的时间</p><p>浏览器中，一个 Tab 页是一个进程，一个进程中有多个线程，比如渲染线程、JS 引擎线程、HTTP 请求线程等。发起一个请求就是创建一个线程，请求结束后线程销毁</p><p>JS 运行的时候可能会阻止 UI 渲染，说明了两个线程是互斥</p><p>因为 JS 可以修改 DOM， JS 执行时 UI 线程还在工作会导致不能安全的渲染 UI</p><p>单线程的好处：可以节省内存，节约上下文切换时间，没有锁的问题</p><h3 id="执行栈"><a href="#执行栈" class="headerlink" title="执行栈"></a>执行栈</h3><p><strong>涉及面试题：什么是执行栈？</strong></p><p>执行栈：存储函数调用的栈结构，遵循先进后出的原则。</p><h3 id="浏览器中的-Event-Loop"><a href="#浏览器中的-Event-Loop" class="headerlink" title="浏览器中的 Event Loop"></a>浏览器中的 Event Loop</h3><p>涉及面试题：异步代码执行顺序？解释一下什么是 Event Loop ？</p><p>执行 JS 代码就是往执行栈中放入函数，遇到异步代码时会被挂起并在需要执行的时候加入到队列中</p><p>一旦执行栈为空，Event Loop 就从 Task 队列拿出需要执行的代码并放入执行栈中执行</p><p>所以本质上JS 的异步还是同步行为</p><p>不同的任务源会被分配到不同的 Task 队列中，任务源分为 微任务和 宏任务</p><p>ES6 规范中microtask 称为 jobs，macrotask 称为 task</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;script start&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">async1</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">await</span> async2()</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;async1 end&#x27;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">async2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;async2 end&#x27;</span>)</span><br><span class="line">&#125;</span><br><span class="line">async1()</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;setTimeout&#x27;</span>)</span><br><span class="line">&#125;, <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Promise&#x27;</span>)</span><br><span class="line">  resolve()</span><br><span class="line">&#125;)</span><br><span class="line">  .then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;promise1&#x27;</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">  .then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;promise2&#x27;</span>)</span><br><span class="line">  &#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;script end&#x27;</span>)</span><br><span class="line"><span class="comment">// script start =&gt; async2 end =&gt; Promise =&gt; script end </span></span><br><span class="line"><span class="comment">// =&gt; promise1 =&gt; promise2 =&gt; async1 end =&gt; setTimeout</span></span><br></pre></td></tr></table></figure><p>上述代码的 async 和 await 的执行顺序。</p><p>调用 async1 时，马上输出 async2 end，并返回一个 Promise</p><p>接下来在遇到 await的时候就让出线程开始执行 async1 外的代码</p><p>所以可以把 await 看成是让出线程的标志。</p><p>然后当同步代码执行完毕以后，执行异步代码，又会回到 await 的位置执行返回的 Promise 的 resolve 函数，这又会把 resolve 丢到微任务队列中</p><p>接下来去执行 then 中的回调</p><p>当两个 then 中的回调执行完毕以后，又会回到 await 的位置处理返回值</p><p>这时候可以看成是 Promise.resolve(返回值).then()</p><p>然后 await 后的代码全部被包裹进了 then 的回调中</p><p>所以 console.log(‘async1 end’) 会优先执行于 setTimeout</p><p>如果你觉得上面这段解释还是有点绕，那么我把 async 的这两个函数改造成你一定能理解的代码</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;async2 end&#x27;</span>)</span><br><span class="line">  <span class="comment">// Promise.resolve() 将代码插入微任务队列尾部</span></span><br><span class="line">  <span class="comment">// resolve 再次插入微任务队列尾部</span></span><br><span class="line">  resolve(<span class="built_in">Promise</span>.resolve())</span><br><span class="line">&#125;).then(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;async1 end&#x27;</span>)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>如果 await 后面跟着 Promise 的话，async1 end 需要等待三个 tick 才能执行到。</p><p>那么其实这个性能相对来说还是略慢的，所以 V8 团队借鉴了 Node 8 中的一个 Bug，在引擎底层将三次 tick 减少到了二次 tick。但是这种做法其实是违法了规范的，当然规范也是可以更改的，这是 V8 团队的一个 PR，目前已被同意这种做法。</p><h4 id="Event-Loop-执行顺序如下所示："><a href="#Event-Loop-执行顺序如下所示：" class="headerlink" title="Event Loop 执行顺序如下所示："></a>Event Loop 执行顺序如下所示：</h4><p><strong>同步(宏),检查异步,执行微任务，下一轮loop执行异步(宏)</strong></p><p>执行同步代码，这属于宏任务</p><p>执行完同步代码后，执行栈为空，查询是否有异步代码需要执行</p><p>执行微任务</p><p>执行完微任务后，如有必要会渲染页面</p><p>然后开始下一轮 Event Loop，执行宏任务中的异步代码，也就是 setTimeout 中的回调函数</p><p>以上代码虽然 setTimeout 写在 Promise 之前，但是因为 Promise 属于微任务而 setTimeout 属于宏任务，所以会有以上的打印。</p><p>微任务包括 process.nextTick ，promise ，MutationObserver。<br>宏任务包括 script ， setTimeout ，setInterval ，setImmediate ，I/O ，UI rendering</p><p>这里很多人会有个误区，认为微任务快于宏任务，其实是错误的。因为宏任务包括了 script，浏览器会先执行一个宏任务，接下来有异步代码的话才会先执行微任务。</p><h3 id="Node-中的-Event-Loop"><a href="#Node-中的-Event-Loop" class="headerlink" title="Node 中的 Event Loop"></a>Node 中的 Event Loop</h3>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(4)手写 Promise</title>
      <link href="2021/03/29/yck/(4)%E6%89%8B%E5%86%99Promise/"/>
      <url>2021/03/29/yck/(4)%E6%89%8B%E5%86%99Promise/</url>
      
        <content type="html"><![CDATA[<h1 id="6"><a href="#6" class="headerlink" title="6."></a>6.</h1><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> PENDING = <span class="string">&#x27;pending&#x27;</span></span><br><span class="line"><span class="keyword">const</span> RESOLVED = <span class="string">&#x27;resolved&#x27;</span></span><br><span class="line"><span class="keyword">const</span> REJECTED = <span class="string">&#x27;rejected&#x27;</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MyPromise</span>(<span class="params">fn</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> that = <span class="built_in">this</span></span><br><span class="line">    that.state = PENDING  <span class="comment">// 一开始 Promise 的状态应该是 pending</span></span><br><span class="line">    that.value = <span class="literal">null</span>  <span class="comment">// 保存 resolve 或者 reject 中传入的值</span></span><br><span class="line">    that.resolvedCallbacks = []  <span class="comment">// 保存 then 中的回调</span></span><br><span class="line">    that.rejectedCallbacks = []  <span class="comment">// 保存 then 中的回调</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">resolve</span>(<span class="params">value</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (that.state === PENDING) &#123;</span><br><span class="line">            that.state = RESOLVED</span><br><span class="line">            that.value = value</span><br><span class="line">            that.resolvedCallbacks.map(<span class="function"><span class="params">cb</span> =&gt;</span> cb(that.value)) <span class="comment">//遍历回调数组并执行 </span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">reject</span>(<span class="params">value</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (that.state === PENDING) &#123;</span><br><span class="line">            that.state = REJECTED</span><br><span class="line">            that.value = value</span><br><span class="line">            that.rejectedCallbacks.map(<span class="function"><span class="params">cb</span> =&gt;</span> cb(that.value))</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      fn(resolve, reject)</span><br><span class="line">    &#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">      reject(e)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">MyPromise.prototype.then = <span class="function"><span class="keyword">function</span>(<span class="params">onFulfilled, onRejected</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> that = <span class="built_in">this</span></span><br><span class="line">    <span class="comment">// 判断两个参数是否为函数类型</span></span><br><span class="line">    <span class="comment">// 当参数不是函数时，创建一个函数赋值给对应参数，实现了透传</span></span><br><span class="line">    onFulfilled = <span class="keyword">typeof</span> onFulfilled === <span class="string">&#x27;function&#x27;</span> ? onFulfilled : <span class="function"><span class="params">v</span> =&gt;</span> v</span><br><span class="line">    onRejected = </span><br><span class="line">        <span class="keyword">typeof</span> onRejected === <span class="string">&#x27;function&#x27;</span> ? onRejected : <span class="function"><span class="params">r</span> =&gt;</span> &#123; <span class="keyword">throw</span> r &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果状态是等待态的话，就往回调函数中 push 函数</span></span><br><span class="line">    <span class="keyword">if</span> (that.state === PENDING) &#123;</span><br><span class="line">        that.resolvedCallbacks.push(onFulfilled)</span><br><span class="line">        that.rejectedCallbacks.push(onRejected)</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 当状态不是等待态时，就去执行相对应的函数</span></span><br><span class="line">    <span class="keyword">if</span> (that.state === RESOLVED) &#123;</span><br><span class="line">        onFulfilled(that.value)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (that.state === REJECTED) &#123;</span><br><span class="line">        onRejected(that.value)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(3)JS 异步编程及常考面试题</title>
      <link href="2021/03/29/yck/(3)JS%E5%BC%82%E6%AD%A5/"/>
      <url>2021/03/29/yck/(3)JS%E5%BC%82%E6%AD%A5/</url>
      
        <content type="html"><![CDATA[<h1 id="5"><a href="#5" class="headerlink" title="5."></a>5.</h1><p>异步编程是 JS 中至关重要的内容</p><h3 id="并发（concurrency）和并行（-parallelism）区别"><a href="#并发（concurrency）和并行（-parallelism）区别" class="headerlink" title="并发（concurrency）和并行（ parallelism）区别"></a>并发（concurrency）和并行（ parallelism）区别</h3><p><strong>涉及面试题：并发与并行的区别？</strong></p><p>并发是宏观概念：任务 A 和任务 B在一段时间内通过任务间切换完成</p><p>并行是微观概念： CPU 存在两个核心，同时完成任务 A、B</p><h3 id="回调函数（Callback）"><a href="#回调函数（Callback）" class="headerlink" title="回调函数（Callback）"></a>回调函数（Callback）</h3><p>涉及面试题：什么是回调函数？回调函数有什么缺点？如何解决回调地狱问题？</p><p>回调函数致命的弱点，容易写出回调地狱（Callback hell）</p><p>回调地狱的根本问题就是：</p><p>嵌套函数存在耦合性，一旦改动就会牵一发而动全身</p><p>嵌套函数一多，就很难处理错误</p><p>回调函数的缺点：不能使用 try catch 捕获错误，不能直接 return</p><h3 id="Generator"><a href="#Generator" class="headerlink" title="Generator"></a>Generator</h3><p>涉及面试题：你理解的 Generator 是什么？<br>首先 Generator 函数调用和普通函数不同，它会返回一个迭代器<br>执行next（） 传入的参数等于上一个 yield 的返回值，<br>如果不传参，yield 返回 undefined</p><h3 id="Promise"><a href="#Promise" class="headerlink" title="Promise"></a>Promise</h3><p>涉及面试题：Promise 的特点是什么，分别有什么优缺点？什么是 Promise 链？Promise 构造函数执行和 then 函数执行有什么区别？</p><p>三种状态：等待中（pending）；完成了 （resolved）；拒绝了（rejected）</p><p>链式调用：每次调用 then 之后返回一个全新的 Promise</p><p>如果在 then 中 使用return， return 的值会被 Promise.resolve() 包装</p><p>优点：解决了回调地狱</p><p>缺点：无法取消 Promise，错误需要通过回调函数捕获</p><p>Promise.prototype.then():</p><p>定义在原型对象Promise.prototype上</p><p>作用是为 Promise 实例添加状态改变时的回调函数</p><p>第一个参数是resolved的回调函数，第二个参数（可选）是rejected的回调函数</p><h3 id="async-及-await：异步终极解决方案"><a href="#async-及-await：异步终极解决方案" class="headerlink" title="async 及 await：异步终极解决方案"></a>async 及 await：异步终极解决方案</h3><p>涉及面试题：async 及 await 的特点，它们的优点和缺点分别是什么？await 原理是什么？</p><p>函数加上 async ，就会返回一个 Promise；await 只能配套 async 使用</p><p>返回值使用 Promise.resolve() 包裹，和 then 中处理返回值一样</p><p>优势：处理 then 调用链时能清晰写出代码，毕竟写一大堆 then 也很恶心，优雅解决回调地狱</p><p>缺点： await 将异步代码改造成同步代码，多个异步代码没有依赖性使用了await 会降低性能</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> a = <span class="number">0</span></span><br><span class="line"><span class="keyword">let</span> b = <span class="keyword">async</span> () =&gt; &#123;   </span><br><span class="line">  a = a + <span class="keyword">await</span> <span class="number">10</span>   </span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;2&#x27;</span>, a) <span class="comment">// -&gt; &#x27;2&#x27; 10&#125; </span></span><br><span class="line">  b() </span><br><span class="line">  a++</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;1&#x27;</span>, a) <span class="comment">// -&gt; &#x27;1&#x27; 1</span></span><br></pre></td></tr></table></figure><p>函数 b 先执行，执行到 await时，因为后来的表达式不返回 Promise ，就会包装成 Promise.reslove(返回值)，然后执行函数外的同步代码，然后执行异步代码</p><p>因为 await 内部实现了 generator ，generator 保留堆栈中东西，所以a = 0 保存下来</p><p>这时候 a = 0 + 10</p><p>await原理：  generator 加上 Promise 的语法糖，且内部实现了自动执行 generator</p><h3 id="常用定时器函数"><a href="#常用定时器函数" class="headerlink" title="常用定时器函数"></a>常用定时器函数</h3><p>涉及面试题：setTimeout、setInterval、requestAnimationFrame 各有什么特点？</p><p>setInterval作用和 setTimeout 一致，只是该函数是每隔一段时间执行一次回调函数</p><p>通常不建议 setInterval，因为不能保证在预期时间执行任务，并且存在执行累积的问题</p><p>循环定时器的需求可以通过 requestAnimationFrame 实现</p>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(2)ES6 知识点及常考面试题</title>
      <link href="2021/03/29/yck/(2)ES6/"/>
      <url>2021/03/29/yck/(2)ES6/</url>
      
        <content type="html"><![CDATA[<h3 id="var、let-及-const-区别"><a href="#var、let-及-const-区别" class="headerlink" title="var、let 及 const 区别"></a>var、let 及 const 区别</h3><p><strong>涉及面试题：什么是提升？什么是暂时性死区？var、let 及 const 区别？</strong></p><ul><li>var 存在提升，能在声明之前使用。var 在全局作用域下声明变量会导致变量挂载在 window 上</li><li>let、const 因为暂时性死区，不能在声明前使用</li><li>let 和 const 作用基本一致，但是后者声明的变量不能再次赋值</li><li>暂时性死区：<br>块级作用域内存在let命令，它所声明的变量就绑定这个区域，不再受外部的影响</li></ul><ul><li>提升（hoisting）<ul><li>提升声明：变量没有被声明，但却可以使用</li><li>提升函数：变量声明在函数之后，变量也会是函数，这说明函数提升优先于变量提升</li></ul></li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">console.log(a) &#x2F;&#x2F; ƒ a() </span><br><span class="line">&#123;&#125;</span><br><span class="line">function a() &#123;&#125;</span><br><span class="line">var a &#x3D; 1</span><br></pre></td></tr></table></figure><p>var声明的变量会被提升到作用域的顶部</p><p>在全局作用域下使用 let 和 const 声明变量，变量并不会被挂载到 window 上</p><p>提升存在的根本原因是为了解决函数间互相调用</p><p>函数提升优先于变量提升，函数提升把函数挪到作用域顶部，变量提升把声明挪到作用域顶部</p><h3 id="原型继承和-Class-继承"><a href="#原型继承和-Class-继承" class="headerlink" title="原型继承和 Class 继承"></a>原型继承和 Class 继承</h3><p><strong>涉及面试题：原型如何实现继承？Class 如何实现继承？Class 本质是什么？</strong></p><ul><li>JS 中并不存在类，class 只是语法糖，本质还是函数</li></ul><h4 id="组合继承"><a href="#组合继承" class="headerlink" title="组合继承"></a>组合继承</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Parent</span>(<span class="params">value</span>) </span>&#123; </span><br><span class="line">  <span class="built_in">this</span>.val = value </span><br><span class="line">&#125; </span><br><span class="line">Parent.prototype.getValue = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; </span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.val) </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Child</span>(<span class="params">value</span>) </span>&#123; </span><br><span class="line">  Parent.call(<span class="built_in">this</span>, value) </span><br><span class="line">&#125; </span><br><span class="line">Child.prototype = <span class="keyword">new</span> Parent() </span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> child = <span class="keyword">new</span> Child(<span class="number">1</span>) </span><br><span class="line">child.getValue() <span class="comment">// 1 </span></span><br><span class="line">child <span class="keyword">instanceof</span> Parent <span class="comment">// true</span></span><br></pre></td></tr></table></figure><p>在子类的构造函数中通过 Parent.call(this) 继承父类的属性，然后改变子类的原型为 new Parent() 来继承父类的函数</p><p>优点：构造函数可以传参，不会与父类引用属性共享，复用父类的函数<br>缺点：继承父类函数时调用了父类构造函数，导致子类的原型上多了不需要的父类属性，存在内存的浪费</p><h4 id="寄生组合继承"><a href="#寄生组合继承" class="headerlink" title="寄生组合继承"></a>寄生组合继承</h4><p>对组合继承进行优化，组合继承缺点在于继承父类函数时调用了构造函数，优化掉这点就行</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Parent</span>(<span class="params">value</span>) </span>&#123; <span class="built_in">this</span>.val = value &#125; </span><br><span class="line">Parent.prototype.getValue = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; </span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.val) </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Child</span>(<span class="params">value</span>) </span>&#123; Parent.call(<span class="built_in">this</span>, value) &#125; </span><br><span class="line"></span><br><span class="line">Child.prototype = <span class="built_in">Object</span>.create(Parent.prototype, &#123;   </span><br><span class="line">  <span class="title">constructor</span>: &#123;     </span><br><span class="line">    value: Child,     </span><br><span class="line">    enumerable: <span class="literal">false</span>,     </span><br><span class="line">    writable: <span class="literal">true</span>,    </span><br><span class="line">    configurable: <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">const</span> child = <span class="keyword">new</span> Child(<span class="number">1</span>) child.getValue() <span class="comment">// 1 child instanceof Parent // true</span></span><br></pre></td></tr></table></figure><p>Object.create(proto, [properties])：创建新对象，使用现有对象提供新对象的proto</p><p>既解决了无用的父类属性问题，还能正确的找到子类的构造函数</p><h4 id="Class-继承"><a href="#Class-继承" class="headerlink" title="Class 继承"></a>Class 继承</h4><p>class 实现继承的核心在于使用 extends 表明继承自哪个父类，并且在子类构造函数中必须调用 super，因为这段代码可以看成 Parent.call(this, value)。</p><h3 id="模块化"><a href="#模块化" class="headerlink" title="模块化"></a>模块化</h3><p><strong>涉及面试题：为什么要使用模块化？都有哪几种方式可以实现模块化，各有什么特点？</strong></p><p>1.解决命名冲突</p><p>2.提供复用性</p><p>3.提高代码可维护性</p><h3 id="立即执行函数"><a href="#立即执行函数" class="headerlink" title="立即执行函数"></a>立即执行函数</h3><p>早期使用立即执行函数实现模块化，通过函数作用域解决命名冲突、污染全局作用域的问题</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">globalVariable</span>)</span>&#123;</span><br><span class="line">   globalVariable.test = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line">   <span class="comment">// ... 声明各种变量、函数都不会污染全局作用域</span></span><br><span class="line">&#125;)(globalVariable)</span><br></pre></td></tr></table></figure><h3 id="CommonJS"><a href="#CommonJS" class="headerlink" title="CommonJS"></a>CommonJS</h3><p>用法：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// a.js</span></span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">    a: <span class="number">1</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// or </span></span><br><span class="line"><span class="built_in">exports</span>.a = <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// b.js</span></span><br><span class="line"><span class="keyword">var</span> <span class="built_in">module</span> = <span class="built_in">require</span>(<span class="string">&#x27;./a.js&#x27;</span>)</span><br><span class="line"><span class="built_in">module</span>.a <span class="comment">// -&gt; log 1</span></span><br></pre></td></tr></table></figure><h4 id="exports和module-exports"><a href="#exports和module-exports" class="headerlink" title="exports和module.exports"></a>exports和module.exports</h4><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> <span class="built_in">module</span> = <span class="built_in">require</span>(<span class="string">&#x27;./a.js&#x27;</span>)</span><br><span class="line"><span class="built_in">module</span>.a </span><br><span class="line"><span class="comment">// 这里其实就是包装了一层立即执行函数，这样就不会污染全局变量了，</span></span><br><span class="line"><span class="comment">// 重要的是 module 这里，module 是 Node 独有的一个变量</span></span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">    a: <span class="number">1</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// module 基本实现</span></span><br><span class="line"><span class="keyword">var</span> <span class="built_in">module</span> = &#123;</span><br><span class="line">  id: <span class="string">&#x27;xxxx&#x27;</span>, <span class="comment">// 我总得知道怎么去找到他吧</span></span><br><span class="line">  <span class="built_in">exports</span>: &#123;&#125; <span class="comment">// exports 就是个空对象</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 这个是为什么 exports 和 module.exports 用法相似的原因</span></span><br><span class="line"><span class="keyword">var</span> <span class="built_in">exports</span> = <span class="built_in">module</span>.exports </span><br><span class="line"><span class="keyword">var</span> load = <span class="function"><span class="keyword">function</span> (<span class="params"><span class="built_in">module</span></span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 导出的东西</span></span><br><span class="line">    <span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line">    <span class="built_in">module</span>.exports = a</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">module</span>.exports</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 然后当我 require 的时候去找到独特的</span></span><br><span class="line"><span class="comment">// id，然后将要使用的东西用立即执行函数包装下，over</span></span><br></pre></td></tr></table></figure><ul><li>不能对 exports 直接赋值。因为 var exports = module.exports 表明了 exports 和 module.exports 享有相同地址</li><li>通过改变对象的属性值会对两者都起效，但是直接对 exports 赋值会导致两者不指向同一个地址，修改并不会对 module.exports 起效</li></ul><h3 id="ES-Module"><a href="#ES-Module" class="headerlink" title="ES Module"></a>ES Module</h3><ul><li>ES Module 是原生实现的模块化方案</li><li>与 CommonJS 有以下几个区别<ul><li>Common动态同步，导出是值拷贝；ES Module异步，导出和导入是实时绑定</li></ul></li><li>CommonJS 动态导入， require(${path}/xx.js)，后者暂不支持，但已有提案</li><li>CommonJS 同步导入，因为用于服务端，文件都在本地，同步导入卡住主线程影响不大</li><li>ES Module异步导入，因为用于浏览器，下载文件同步导入会对渲染有影响</li><li>CommonJS 在导出时都是值拷贝，导入的值不会改变，如果想更新值，必须重新导入一次</li><li>ES Module 采用实时绑定，导入导出的值都指向同一个内存地址</li><li>ES Module 会编译成 require/exports 来执行<figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 引入模块 API</span></span><br><span class="line"><span class="keyword">import</span> XXX <span class="keyword">from</span> <span class="string">&#x27;./a.js&#x27;</span></span><br><span class="line"><span class="keyword">import</span> &#123; XXX &#125; <span class="keyword">from</span> <span class="string">&#x27;./a.js&#x27;</span></span><br><span class="line"><span class="comment">// 导出模块 API</span></span><br><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="Proxy"><a href="#Proxy" class="headerlink" title="Proxy"></a>Proxy</h3><p><strong>涉及面试题：Proxy 可以实现什么功能？</strong></p><ul><li>Proxy 是 ES6 新增的功能，用来自定义对象中的操作</li><li>let p = new Proxy(target, handler)<ul><li>target 代表添加代理的对象，handler 自定义对象中的操作，比如 set 或 get 函数</li></ul></li><li>通过 Proxy 来实现一个数据响应式<figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> onWatch = <span class="function">(<span class="params">obj, setBind, getLogger</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> handler = &#123;</span><br><span class="line">    <span class="function"><span class="title">get</span>(<span class="params">target, property, receiver</span>)</span> &#123;</span><br><span class="line">      getLogger(target, property)</span><br><span class="line">      <span class="keyword">return</span> <span class="built_in">Reflect</span>.get(target, property, receiver)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="function"><span class="title">set</span>(<span class="params">target, property, value, receiver</span>)</span> &#123;</span><br><span class="line">      setBind(value, property)</span><br><span class="line">      <span class="keyword">return</span> <span class="built_in">Reflect</span>.set(target, property, value)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Proxy</span>(obj, handler)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> obj = &#123; <span class="attr">a</span>: <span class="number">1</span> &#125;</span><br><span class="line"><span class="keyword">let</span> p = onWatch(</span><br><span class="line">  obj,</span><br><span class="line">  (v, property) =&gt; &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`监听到属性<span class="subst">$&#123;property&#125;</span>改变为<span class="subst">$&#123;v&#125;</span>`</span>)</span><br><span class="line">  &#125;,</span><br><span class="line">  (target, property) =&gt; &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`&#x27;<span class="subst">$&#123;property&#125;</span>&#x27; = <span class="subst">$&#123;target[property]&#125;</span>`</span>)</span><br><span class="line">  &#125;</span><br><span class="line">)</span><br><span class="line">p.a = <span class="number">2</span> <span class="comment">// 监听到属性a改变</span></span><br><span class="line">p.a <span class="comment">// &#x27;a&#x27; = 2</span></span><br></pre></td></tr></table></figure></li><li>Vue3.0 用 Proxy 替换原本的API,原因在于 Proxy 无需一层层递归为每个属性添加代理，一次即可完成操作，性能更好，并且原本的实现有一些数据更新不能监听到</li><li>Proxy 可以完美监听到任何方式的数据改变，唯一缺陷可能是浏览器的兼容性不好</li></ul><h3 id="map-filter-reduce"><a href="#map-filter-reduce" class="headerlink" title="map, filter, reduce"></a>map, filter, reduce</h3><p><strong>涉及面试题：map, filter, reduce 各自有什么作用？</strong></p><ul><li>map 作用是生成新数组，遍历原数组，将每个元素拿出来做变换然后放入新数组</li></ul><p><code>[1, 2, 3].map(v =&gt; v + 1) // -&gt; [2, 3, 4]</code></p><p>map 的回调函数接受三个参数，分别  是当前索引元素，索引，原数组</p><p>[‘1’,’2’,’3’].map(parseInt)</p><p>第一轮遍历 parseInt(‘1’, 0) -&gt; 1</p><p>第二轮遍历 parseInt(‘2’, 1) -&gt; NaN</p><p>第三轮遍历 parseInt(‘3’, 2) -&gt; <strong>NaN</strong></p><ul><li>filter 的作用是删除数组中不需要的元素，生成新数组时将返回值为 true 的元素放入</li></ul><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> array = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">4</span>, <span class="number">6</span>]</span><br><span class="line"><span class="keyword">let</span> newArray = array.filter(<span class="function"><span class="params">item</span> =&gt;</span> item !== <span class="number">6</span>)</span><br><span class="line"><span class="built_in">console</span>.log(newArray) <span class="comment">// [1, 2, 4]</span></span><br></pre></td></tr></table></figure><p>filter 的回调函数也接受三个参数，用处也相同</p><ul><li>reduce 可以将数组中的元素通过回调函数最终转换为一个值</li></ul><p>实现将函数的元素全部相加得到一个值</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="keyword">let</span> total = <span class="number">0</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; arr.length; i++) &#123;   </span><br><span class="line">  total += arr[i] </span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(total) <span class="comment">//6 </span></span><br></pre></td></tr></table></figure><p>使用 reduce </p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="keyword">const</span> sum = arr.reduce(<span class="function">(<span class="params">acc, current</span>) =&gt;</span> acc + current, <span class="number">0</span>)</span><br><span class="line"><span class="built_in">console</span>.log(sum)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>reduce 接受两个参数，回调函数和初始值<br>回调函数接受四个参数，分别为累计值、当前元素、当前索引、原数组</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="keyword">const</span> mapArray = arr.map(<span class="function"><span class="params">value</span> =&gt;</span> value * <span class="number">2</span>)</span><br><span class="line"><span class="keyword">const</span> reduceArray = arr.reduce(<span class="function">(<span class="params">acc, current</span>) =&gt;</span> &#123;   </span><br><span class="line">  acc.push(current * <span class="number">2</span>)   </span><br><span class="line">  <span class="keyword">return</span> acc &#125;, []</span><br><span class="line">) </span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(mapArray, reduceArray) <span class="comment">// [2, 4, 6]</span></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>(1)JS 基础知识点及常考面试</title>
      <link href="2021/03/29/yck/(1)JS%20%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%82%B9%E5%8F%8A%E5%B8%B8%E8%80%83%E9%9D%A2%E8%AF%95/"/>
      <url>2021/03/29/yck/(1)JS%20%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%82%B9%E5%8F%8A%E5%B8%B8%E8%80%83%E9%9D%A2%E8%AF%95/</url>
      
        <content type="html"><![CDATA[<h1 id="1-JS-基础知识点及常考面试题"><a href="#1-JS-基础知识点及常考面试题" class="headerlink" title="1.JS 基础知识点及常考面试题"></a>1.JS 基础知识点及常考面试题</h1><h3 id="原始（Primitive）类型"><a href="#原始（Primitive）类型" class="headerlink" title="原始（Primitive）类型"></a>原始（Primitive）类型</h3><p><strong>涉及面试题：原始类型有哪几种？null 是对象嘛？</strong></p><p>6种：boolean、null、undefined、number、string、symbol</p><p><strong>原始类型</strong>存储值，没有函数可以调用</p><p>‘1’.toString() 的’1’ 不是原始类型，而是强制转换成 String 类型(对象类型)</p><p><strong>number</strong> 是浮点类型，比如 0.1 + 0.2 !== 0.3是为true</p><p><strong>string</strong> 不可变，无论在 string 类型上调用何种方法，都不会对值有改变。</p><p><strong>typeof null</strong> 会输出 object，错误判断</p><h3 id="对象（Object）类型"><a href="#对象（Object）类型" class="headerlink" title="对象（Object）类型"></a>对象（Object）类型</h3><p><strong>涉及面试题：对象类型和原始类型的不同之处？函数参数是对象会发生什么问题？</strong></p><p>原始类型存储值，对象类型存储<strong>地址</strong>（指针）</p><p>函数传参传递的是对象指针的副本</p><h3 id="typeof-vs-instanceof"><a href="#typeof-vs-instanceof" class="headerlink" title="typeof vs instanceof"></a>typeof vs instanceof</h3><p><strong>涉及面试题：typeof 是否能正确判断类型？instanceof 能正确判断对象的原理是什么？</strong></p><ul><li>typeof 对于原始类型，除了 null 都可以显示正确类型</li><li>typeof 对于对象，除了函数都会显示 object</li><li>instanceof 可以判断对象的正确类型（通过<strong>原型链</strong>）<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> Person = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> Person()</span><br><span class="line">p1 <span class="keyword">instanceof</span> Person <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> str = <span class="string">&#x27;hello world&#x27;</span></span><br><span class="line">str <span class="keyword">instanceof</span> <span class="built_in">String</span> <span class="comment">// false</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> str1 = <span class="keyword">new</span> <span class="built_in">String</span>(<span class="string">&#x27;hello world&#x27;</span>)</span><br><span class="line">str1 <span class="keyword">instanceof</span> <span class="built_in">String</span> <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li></ul><h3 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h3><p>JS 中类型转换只有三种情况，分别是：</p><ul><li>1.其他类型转换为布尔值<ul><li>undefined， null， false， NaN， ‘’， 0， -0一律转为false</li><li>其他所有值都转为 true，包括对象</li></ul></li><li>2.其他类型转换为数字<ul><li>空数组为0，单个数字元素的数组转换为该数字，其他情况NaN</li><li>null转为0，引用类型转为数字为NaN<figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="literal">true</span> + <span class="literal">true</span> <span class="comment">// 2</span></span><br><span class="line">[<span class="string">&#x27;1&#x27;</span>] == <span class="number">1</span> <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li></ul></li><li>3.转换为字符串</li></ul><ul><li>四则运算符<ul><li>如果一方为字符串，那么另一方也转换为字符串</li><li>如果一方不是字符串或者数字，那么会将它转换为数字或者字符串<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span> + <span class="string">&#x27;1&#x27;</span> <span class="comment">// &#x27;11&#x27;</span></span><br><span class="line"><span class="literal">true</span> + <span class="literal">true</span> <span class="comment">// 2</span></span><br><span class="line"><span class="number">4</span> + [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>] <span class="comment">// &quot;41,2,3&quot;</span></span><br></pre></td></tr></table></figure></li></ul></li></ul><h3 id="this"><a href="#this" class="headerlink" title="this"></a>this</h3><p><strong>涉及面试题：如何正确判断 this？箭头函数的 this 是什么？</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.a)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line"><span class="comment">// 1.直接调用 foo ，this 是window</span></span><br><span class="line">foo()</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> obj = &#123;</span><br><span class="line">  a: <span class="number">2</span>,</span><br><span class="line">  foo: foo</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 2.谁调用了函数，谁就是 this</span></span><br><span class="line">obj.foo()</span><br><span class="line"><span class="comment">// 3.对于 new 来说，this 绑定在 c 上面，不会被改变</span></span><br><span class="line"><span class="keyword">const</span> c = <span class="keyword">new</span> foo()</span><br></pre></td></tr></table></figure><ul><li><p>箭头函数没有 this ，this 取决<strong>包裹箭头函数的第一个普通函数的 this</strong>，bind 这类函数是无效的</p></li><li><p>bind等改变上下文的函数，this取决于第一个参数，如果为空，this就是 window</p></li><li><p>如果对一个函数多次 bind，this 由<strong>第一次 bind 决定</strong></p></li></ul><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;&#125; </span><br><span class="line"><span class="keyword">let</span> fn = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; </span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>) </span><br><span class="line">&#125; </span><br><span class="line">fn.bind().bind(a)() <span class="comment">// this指向windows</span></span><br><span class="line"><span class="comment">// fn.bind().bind(a) 等于 </span></span><br><span class="line"><span class="keyword">let</span> fn2 = <span class="function"><span class="keyword">function</span> <span class="title">fn1</span>(<span class="params"></span>) </span>&#123;   </span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;     </span><br><span class="line">    <span class="keyword">return</span> fn.apply() <span class="comment">// Window  </span></span><br><span class="line">  &#125;.apply(a) </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line">fn2()</span><br></pre></td></tr></table></figure><h3 id="vs"><a href="#vs" class="headerlink" title="== vs ==="></a>== vs ===</h3><p><strong>涉及面试题：== 和 === 有什么区别？</strong></p><ul><li>==对比的类型不一样的话，就进行类型转换</li><li>1.首先判断类型是否相同，相同的话比大小</li><li>2.类型不相同进行<strong>类型转换</strong></li><li>3.判断是否对比 null 和 undefined，是的话返回 true<figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="literal">null</span> == <span class="literal">undefined</span> <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li><li>4.判断是否为 string 和 number，是的话将字符串转换为 number<figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span> == <span class="string">&#x27;1&#x27;</span> <span class="comment">// 1 ==  1, true</span></span><br></pre></td></tr></table></figure></li></ul><p>5.判断是否包含boolean，把 boolean 转为 number 再判断</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;1&#x27;</span> == <span class="literal">true</span> <span class="comment">//&#x27;1&#x27; ==  1  =&gt;  1  ==  1</span></span><br></pre></td></tr></table></figure><p>6.判断是否是object和（string | number | symbol），是则把 object 转为原始类型</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;1&#x27;</span> == &#123; <span class="attr">name</span>: <span class="string">&#x27;yck&#x27;</span> &#125;</span><br><span class="line"><span class="string">&#x27;1&#x27;</span> == <span class="string">&#x27;[object Object]&#x27;</span></span><br></pre></td></tr></table></figure><p><strong>=== 判断两者类型和值是否相同</strong></p><h3 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h3><p><strong>涉及面试题：什么是闭包？</strong></p><ul><li>闭包定义：函数 A 内有函数 B，函数 B 可以访问函数 A 的变量，函数 B 是闭包</li><li>特点：间接访问函数内部的变量, 且该变量不会销毁</li><li>作用：隐藏变量，避免全局污染</li><li>缺点：变量不会被GC回收，造成内存消耗，不恰当使用会造成内存泄漏<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">person</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> age = <span class="number">18</span>;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">cat</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        age++;</span><br><span class="line">        <span class="built_in">console</span>.log(age);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> cat;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> per = person();<span class="comment">//per相当于函数cat</span></span><br><span class="line">per();<span class="comment">// 19 即cat() 每次调用不再经过age的初始值，一直增加</span></span><br><span class="line">per();<span class="comment">// 20</span></span><br></pre></td></tr></table></figure></li></ul><p><strong>相关面试题：循环中使用闭包解决var定义函数的问题</strong></p><ul><li><p>1.闭包</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">for (var i &#x3D; 1; i &lt;&#x3D; 5; i++) &#123;     </span><br><span class="line">    function(j) &#123;         </span><br><span class="line">        setTimeout(()&#x3D;&gt;console.log(j), j*1000)     </span><br><span class="line">    &#125;(i) </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>2.setTimeout，第三个参数会被当成 timer 函数的参数传入</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">for (var i &#x3D; 1; i &lt;&#x3D; 5; i++) &#123;   </span><br><span class="line">    setTimeout(     </span><br><span class="line">        function timer(j) &#123;console.log(j)&#125;, </span><br><span class="line">        i * 1000, </span><br><span class="line">        i</span><br><span class="line">    ) </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>3.用 let 定义 i ，最推荐</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">for (let i &#x3D; 1; i &lt;&#x3D; 5; i++) &#123;   </span><br><span class="line">    setTimeout(</span><br><span class="line">        function timer() &#123;console.log(i)&#125;, </span><br><span class="line">        i * 1000) </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="深浅拷贝"><a href="#深浅拷贝" class="headerlink" title="深浅拷贝"></a>深浅拷贝</h3><p><strong>涉及面试题：什么是浅拷贝？如何实现浅拷贝？什么是深拷贝？如何实现深拷贝？</strong></p><ul><li>对象类型在赋值过程其实是复制了地址，如果改变了对象A, 指向同一地址的B也被改变</li><li>通常在开发中不希望出现这样的问题，可以使用浅拷贝解决</li></ul><p><strong>浅拷贝：</strong></p><ul><li>1.Object.assign：只拷贝所有属性到新对象，如果属性值是对象，只拷贝地址</li><li>2.通过展开运算符 … 实现浅拷贝<figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;<span class="attr">age</span>: <span class="number">1</span>&#125;</span><br><span class="line"><span class="keyword">let</span> b = <span class="built_in">Object</span>.assign(&#123;&#125;, a) </span><br><span class="line">a.age = <span class="number">2</span></span><br><span class="line"><span class="built_in">console</span>.log(b.age) <span class="comment">// 1</span></span><br><span class="line"><span class="comment">// or</span></span><br><span class="line"><span class="keyword">let</span> c = &#123;<span class="attr">age</span>: <span class="number">3</span>&#125;</span><br><span class="line"><span class="keyword">let</span> d = &#123; ...c &#125;</span><br></pre></td></tr></table></figure></li></ul><p><strong>深拷贝</strong></p><ul><li>可以通过 JSON.parse(JSON.stringify(object)) 解决<figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> obj_b = <span class="built_in">JSON</span>.parse(<span class="built_in">JSON</span>.stringify(obj_a))</span><br></pre></td></tr></table></figure></li><li>存在问题：复制过程碰到函数、undefined、symbol时会被忽略，不能解决循环引用的对象</li><li>自实现深拷贝：推荐使用lodash的深拷贝函数</li></ul><h3 id="原型"><a href="#原型" class="headerlink" title="原型"></a>原型</h3><p><strong>涉及面试题：如何理解原型？如何理解原型链？</strong></p><ul><li>原型：原型是一个对象，这个对象中包含了很多函数可供根据原型创造出来的实例所使用</li><li><code>__proto__</code><ul><li>object 有 <code>__proto__</code> 属性指向原型，不推荐使用（早期为了访问内部属性<strong>prototype</strong>而实现）</li></ul></li><li><code>prototype</code><ul><li><strong>constructor</strong>有 <strong>prototype</strong> 属性，属性值和<code>__proto__</code>一样，即指向原型，而原型的 <strong>constructor 指回构造函数</strong></li><li>不是所有函数都具有这个属性，例如Function.prototype.bind()</li><li>普通函数来说<code>prototype</code>没什么用处，而构造函数的<code>prototype</code>非常有用，因为它指向了实例的原型<br><img src="https://cdn.jsdelivr.net/gh/Hendry5479/learn-react/img/20210306232339.png"></li></ul></li><li>原型链：<code>__proto__</code> 将对象和原型连接起来组成了原型链</li></ul>]]></content>
      
      
      <categories>
          
          <category> Interview </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试之道 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>大前端</title>
      <link href="2021/03/28/index/"/>
      <url>2021/03/28/index/</url>
      
        <content type="html"><![CDATA[<p>@vue/cli-service-global<br>vue service</p><p>hexo博客<br>hexo s 运行<br>npm i hexo-deployer-git -S</p><h3 id="docker"><a href="#docker" class="headerlink" title="docker"></a>docker</h3><p>Docker Hub是官方提供的镜像仓库</p><h4 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h4><ul><li><p>启动docker：service docker start</p></li><li><p>拉取mongo镜像：docker pull mongo</p><ul><li>默认端口27017</li></ul></li><li><p>启动镜像：docker run -d –name some-mongo -p 10050:27017 mongo</p><ul><li>-d代表后台运行，跑在服务器的10050端口，映射到容器内的27017端口</li></ul></li><li><p>查看已启动的镜像docker ps</p></li><li><p>查看本地所有镜像：docker images</p></li><li><p>停止运行：docker stop</p></li><li><p>删除镜像：docker rmi，-f表示强制删除，尽量先删除容器再删除镜像</p></li><li><p>列举运行中的Docker容器：docker ps</p><ul><li>-a列表所有的容器，-f过滤，-q只列表容器的id</li></ul></li><li><p>查看docker的版本信息：docker version，查看Docker的系统信息：docker info </p></li><li><p>查看日志：docker logs -f xxx</p></li><li><p>进入容器终端：docker exec -it redis-test /bin/bash</p></li></ul><p>关闭防火墙<br>ubuntu: service ufw stop<br>cenos: service firewalld stop</p><p>放行端口<br>firewall-cmd –zone=public –add-port=10050/tcp –permanent</p><h4 id="docker-compose"><a href="#docker-compose" class="headerlink" title="docker-compose"></a>docker-compose</h4><p>不需要手动运行多个服务<br>compose将容器分为三层，yml是一个工厂，工程下有多个服务，每个服务定义可以多个容器实例</p><ol><li>配置docker-compose.yml文件<br>build：Compose 会利用它自动构建镜像，该值可以是路径，也可以是对象，用于指定 Dockerfile 参数<br>image：指定服务所使用的镜像<br>ports：对外暴露的端口定义，和 expose 对应<figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.1&quot;</span></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">redis-test:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">&quot;redis&quot;</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">&quot;redis-test&quot;</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">15001</span><span class="string">:6379</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">/home/redistest:/data</span></span><br><span class="line">    <span class="attr">command:</span> [<span class="string">&quot;redis-server&quot;</span>, <span class="string">&quot;--requirepass&quot;</span>, <span class="string">&quot;123456&quot;</span>]</span><br><span class="line"></span><br><span class="line">  <span class="attr">mongo:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mongo</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">MONGO_INITDB_ROOT_USERNAME:</span> <span class="string">root</span></span><br><span class="line">      <span class="attr">MONGO_INITDB_ROOT_PASSWORD:</span> <span class="string">example</span></span><br><span class="line">    <span class="attr">port:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">27017</span><span class="string">:27017</span></span><br><span class="line">  <span class="attr">web:</span></span><br><span class="line">    <span class="attr">build:</span> <span class="string">.</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">&quot;5000:5000&quot;</span></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">&quot;redis:alpine&quot;</span></span><br><span class="line">  <span class="attr">mysql1:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mysql</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">MYSQL_ROOT_PASSWORD=123456</span></span><br><span class="line">    <span class="attr">port:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">28002</span><span class="string">:3306</span></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure></li><li>构建、启动容器：docker-compose up -d</li></ol><ul><li>常用命令</li><li>列出所有运行容器：docker-compose ps</li><li>查看服务日志输出：docker-compose logs</li><li>启动指定服务已存在的容器：docker-compose start eureka</li></ul><p>创建mysql</p><p>doclever:接口管理平台</p><p>webpack</p><p>使用HMR<br>npm i webpack-dev-server<br>babel-loader<br>@babel/core<br>@babel/preset-env<br>@babel/plugin-transform-runtime </p><p>@babel/runtime -S</p><p>新增.babelrc</p><p>gulp</p><p>gulp-uglify<br>gulp-rename , del<br>gulp-autoprefixer<br>gulp-sass<br>gulp-load-plugins</p><p>yarn add gulp-sass gulp-autoprefixer gulp-load-plugins gulp-uglify del -D</p><p>browsersync</p><p>nvm<br>nvm install nvm use</p><p>yeoman<br>yo generator</p><p>nrm<br>nrm ls  nrm use xxx</p><p>vue<br>vue.config.js</p><p>检查配置：vue inspect &gt; output.js</p><p>koa<br>koa-helmet<br>app.use(xxx)</p><p>koa-static</p><p>nodemon<br>script中配置，开发中监视</p><p>clean-webpack-plugin<br>webpack-node-externals<br>@babel/core<br>@babel/node<br>@babel/preset-env<br>babel-loader<br>cross-env 设置环境变量</p><p>框架&amp;库<br>类库：Jquery，Lodash<br>JS库：React，Vue，Angular<br>工具：Webpack，Jest，ESLint，Template，Echarts</p><p>UI框架选型<br>1.场景（响应式）<br>2.组件丰富，成熟模板，方便集成<br>Layui，Ant design，iView<br>移动ui：mint ui，cube ui，Vant</p><p>Layui</p><p>验证码：svg-captcha</p><p>api项目笔记<br>  –inspect=xxx 若已占用可以添加此项</p><p>表单校验<br>vee-validate Vuelidate<br>Vue.use()</p><p>待解决<br>:class=”{‘form-group-error’:$v.name.$error}”</p><p>/* webpackChunkName:”lodash” */<br>webpack进行代码分割时，为分割后的代码命名<br>import异步加载再加上webpack的魔法注释实现了懒加载。webpack分割代码，加载时按需加载实现懒加载</p><p>解决端口占用问题<br>netstat -ano | findstr xxxx<br>tasklist | findstr xxxx<br>taskkill -F -PID xxxx</p><p>nodemailer</p><p>NoSQL<br>常见场景和设计方法<br>内嵌：数组内嵌，减少了关联查询，适合单类描述的属性和不经常变化的属性，如果是拓展属性或者需要单独访问的则不合适<br>父子引用：<br>  父引用：存在一对多的情况，放在同一文档中，以数组形式存放<br>  子引用：存在一对多的情况，反向引用，例如文章里的评论<br>  数据内容非常多，或者需要单独访问，则不适合内嵌<br>反范式：<br>  范式指特定的用法，反范式指不走寻常路<br>  是否有提升性能的空间，数据提升是否庞大<br>  先考虑读写比，再考虑反范式<br>双向关联的场景及设计原则</p><p>mongo<br>备份和恢复<br>docker exec -it mongotest_mongo_1 mongodump -h localhost -u root -p example -o /tmp/test</p><p>mongorestore</p><p>redis会分为16个databases，在redis服务中将数据隔离，</p><p>redis 命令<br>auth 123456<br>echo “Hello”<br>ping pong<br>quit<br>exit 退出镜像<br>select 0<br>get set<br>keys test*<br>exists test<br>del test</p><p>JWT<br>Header<br>Payload<br>Signature</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">&#123; <span class="string">&quot;alg&quot;</span>: <span class="string">&quot;HS256&quot;</span>, <span class="string">&quot;typ&quot;</span>: <span class="string">&quot;JWT&quot;</span>&#125;</span><br><span class="line">&#123;<span class="string">&quot;name&quot;</span>: <span class="string">&quot;hendry&quot;</span>&#125;</span><br><span class="line">HMACSHA256(BASE64uRLeNCODE(header) + <span class="string">&#x27;.&#x27;</span> + base64(payload)), secret)</span><br></pre></td></tr></table></figure><p>JWT特点：<br>JWT防CSRF，即伪造请求，带上cookie<br>适合移动应用<br>无状态<br>刷新和过期时间，payload不适宜过大，中间人攻击，</p><p>vee-validate i18n</p><p>uuid</p><p>blue-bird</p><p>npm-run-all<br>设置两个脚本命令，合并</p><p>网站SEO重要的一个部分是网站的性能优化<br>·资源压缩，请求压缩<br>设置缓存，设置DNS预处理<br>使用CDN</p><p>升级vue-cli 4<br>npm i -g @vue/cli<br>vue upgrade</p><p>长列表请求注意事项<br>1.<br>2.清空原有数据<br>3.防止重复点击<br>4.尾页状态码</p><p>npm-check-updates</p><p>添加eslint支持<br>npx eslint –init</p><p>插播技巧<br>Ctrl + F2：快速选择一块内的某个字符<br>shift + end跳转到末尾</p><p>mongoose</p><p>populate：进行join查询,筛选出需要的字段</p><p>db.getCollection(“posts”).updateMany({}, {$set: {“uid”: “604bf752596bef533c9258f8”}})</p><p>@mixin @include</p><p>调试sass<br>cnpm i -S sass<br>npx sass test.scss</p><p>$test: 3333;<br>@warn $test;<br>@debug ‘hello: #{$test}’;</p><p>$spacers: </p><p>map-merge($map1: {}, $map2: {})</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="2021/03/28/hello-world/"/>
      <url>2021/03/28/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> front </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
